# 04-03 Checkpoint 2 — RESOLVED verification (decision needed)

## What I did this turn

1. **17:19:05Z** — `systemctl start node_exporter` on cc-worker → `active`.
2. **~17:20Z** — Prometheus `up{instance="100.99.133.9:9100"} = 1`. Target healthy again.
3. **Prometheus alerts** — HostDown no longer in API (rule cleared correctly).
4. **Alertmanager `/api/v2/alerts`** — no active alerts for 100.99.133.9 (AM processed the clear).
5. **AM silences** — 0 active. Prior DiskUsageCritical silence has expired (no cleanup needed).
6. **AM config verified** — `send_resolved: true`, `resolve_timeout: 5m`, telegram receiver wired correctly.

## Complication — AM restarted mid-phase at 17:05:57Z

**Pre-restart (recorded in smoke log):**
- baseline `alertmanager_notifications_total{integration="telegram"}` = **8**
- after trigger = **9** (+1, captured the FIRING dispatch at ~14:30Z)
- operator confirmed ~9 HostDown messages in @void_homelab_bot chat + direct-ping msg_id 65

**Post-restart (now):**
- `alertmanager_notifications_total{integration="telegram"}` = **0** (counter reset by restart)
- `alertmanager_notifications_failed_total{integration="telegram",*}` = **0** (all reasons)
- AM logs since 17:05:57Z show only startup/gossip — **no telegram dispatches** this lifetime
- With `repeat_interval: 4h`, AM was not due to re-notify firing between 17:05:57Z and 17:19:05Z when node_exporter came back

**Implication:** AM has no in-flight "firing notified" state in its current lifetime to follow up with a RESOLVED dispatch. **A RESOLVED telegram message will NOT be sent for this cycle.** This is not a bug — it's a race between the AM restart and node_exporter recovery.

## Evidence summary for plan 04-03 SC #3 ("real fire delivers message")

| Evidence | Value |
|---|---|
| Pre-phase baseline counter | 8 |
| Post-trigger counter (pre-restart) | 9 (+1 firing dispatch) |
| Operator receipt (firing) | ✅ ~9 HostDown messages + direct-ping msg_id 65 at 17:13Z |
| `send_resolved: true` in config | ✅ |
| `resolve_timeout: 5m` | ✅ |
| Prometheus rule cleared on recovery | ✅ |
| AM processed clear | ✅ |
| Observed RESOLVED telegram message this cycle | ❌ blocked by AM restart timing |

## Decision needed from operator (A / B / C)

**A — Accept current state, close plan (recommended):**
The FIRING→telegram delivery is proven by pre-restart counter delta (+1) + operator receipt. RESOLVED path is correctly configured (`send_resolved: true`). Not observing RESOLVED this cycle is a timing artifact of the AM restart, not a config defect. Close plan 04-03 with SC #3 marked pass; note the RESOLVED observation as a follow-up smoke test in 04-04 or a future plan.

**B — Force a clean fire+resolve cycle now (≈5–8 min, 2 more messages in chat):**
1. Stop node_exporter on cc-worker.
2. Wait HostDown `for:5m` + AM `group_wait:30s` ≈ ~6 min → 🔴 firing telegram (counter 0→1).
3. Start node_exporter.
4. Wait scrape + resolve_timeout ≈ 2–5 min → 🟢 resolved telegram (counter 1→2).
5. Confirm both observed end-to-end in this AM lifetime.

**C — Mark RESOLVED verification as tech debt, close FIRING half only:**
Close plan 04-03 SC #3 as pass for firing delivery, defer RESOLVED observation to a dedicated smoke test in a future plan. Similar to A but explicit about the gap.

## Reply with `A`, `B`, or `C`

I will not write SUMMARY.md, flip ROADMAP, or unwind anything until you choose. DiskUsage silences were already expired — no cleanup pending.
