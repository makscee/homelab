---
phase: 12-infra-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lockb
  - .gitignore
  - apps/admin/package.json
  - apps/admin/tsconfig.json
  - apps/admin/next.config.mjs
  - apps/admin/postcss.config.mjs
  - apps/admin/tailwind.config.ts
  - apps/admin/components.json
  - apps/admin/.env.example
  - apps/admin/app/layout.tsx
  - apps/admin/app/page.tsx
  - apps/admin/app/globals.css
  - apps/admin/bun.lockb
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-07]
must_haves:
  truths:
    - "Running `bun install --frozen-lockfile` at repo root resolves all deps with zero lockfile drift"
    - "Running `cd apps/admin && bun run build` produces a `.next/` standalone output"
    - "Running `bun audit --audit-level high` from repo root exits 0 (no HIGH/CRITICAL)"
    - "Next.js is pinned to 15.2.4 (or higher 15.x patch containing CVE-2025-66478 fix)"
  artifacts:
    - path: "package.json"
      provides: "Bun workspaces root"
      contains: '"workspaces"'
    - path: "apps/admin/package.json"
      provides: "Next.js app manifest with pinned versions"
      contains: '"next": "15.2.4"'
    - path: "apps/admin/next.config.mjs"
      provides: "Next standalone output + nonce-ready config"
      contains: 'output: "standalone"'
    - path: "bun.lockb"
      provides: "Deterministic lockfile at repo root (P-17)"
    - path: ".gitignore"
      provides: "Exclude node_modules/.next/.env.local"
      contains: "apps/*/.next"
    - path: "apps/admin/tailwind.config.ts"
      provides: "Tailwind config with dark-mode-by-default CSS-variable theme tokens"
  key_links:
    - from: "apps/admin/package.json"
      to: "root bun.lockb"
      via: "Bun workspaces resolution"
      pattern: '"workspaces".*"apps/\*"'
    - from: "apps/admin/next.config.mjs"
      to: "Ansible deploy pipeline"
      via: "standalone output consumed by systemd ExecStart"
      pattern: 'output.*standalone'
---

<objective>
Create the Bun workspaces root and scaffold `apps/admin/` Next.js 15.2.4 + React 19.2.5 + TypeScript + Tailwind + shadcn CLI. This plan ONLY lays down the toolchain, pinned deps, and an empty app that builds green. Auth, layout, middleware, and deploy come in later plans.

Purpose: Deliver SC #3 (bun audit clean, Next.js CVE-2025-66478 pinned) and the prerequisite scaffold SC #1/#5 build on.
Output: Repo builds locally. `apps/admin/` is a working Next.js app with empty `/` route. No auth, no middleware, no layout yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-infra-foundation/12-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.gitignore

<interfaces>
<!-- Root package.json declares workspaces. App package.json pins EXACT versions — executor must not bump. -->
<!-- PITFALLS: P-01 (Next.js 15.2.4 CVE), P-05 (Bun native modules — smoke on mcow in Plan 09), P-17 (commit bun.lockb in the same commit as package.json changes) -->
<!-- CONTEXT: D-12-01 (versions), D-12-02 (lockfile discipline), D-12-03 (no DB this phase), D-12-29 (Bun workspaces), D-12-30 (.gitignore) -->

Target root package.json shape:
```json
{
  "name": "homelab",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "build": "cd apps/admin && bun run build",
    "dev": "cd apps/admin && bun run dev",
    "lint": "cd apps/admin && bun run lint",
    "audit": "bun audit --audit-level high"
  }
}
```

Target apps/admin/package.json dependencies (EXACT pins — do not bump):
- "next": "15.2.4"
- "react": "19.2.5"
- "react-dom": "19.2.5"
- "zod": "3.24.1"  (or latest 3.24.x patch; 3.24.x band only — NOT 4.x per D-12-01)
- "tailwindcss": "^3.4.0"
- "autoprefixer": "^10.4.0"
- "postcss": "^8.4.0"
- devDependencies include "typescript" "^5.6.0", "@types/node", "@types/react", "@types/react-dom", "eslint" "^9", "eslint-config-next" "15.2.4"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bun workspaces root + .gitignore</name>
  <files>package.json, .gitignore, bun.lockb</files>
  <read_first>
    - `.gitignore` (current root) — see what's already ignored; additive only
    - `package.json` (check if one exists at root — if not, create)
    - `.planning/phases/12-infra-foundation/12-CONTEXT.md` §decisions D-12-29, D-12-30
    - `.planning/research/PITFALLS.md` P-17 (bun.lockb commit discipline)
  </read_first>
  <action>
1. Create `package.json` at repo root with EXACT content:
```json
{
  "name": "homelab",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "build": "cd apps/admin && bun run build",
    "dev": "cd apps/admin && bun run dev",
    "lint": "cd apps/admin && bun run lint",
    "audit": "bun audit --audit-level high"
  }
}
```
2. Append to `.gitignore` (create section `# v3.0 Next.js admin app`):
```
# v3.0 Next.js admin app
apps/*/node_modules
apps/*/.next
apps/*/.turbo
apps/*/next-env.d.ts
apps/*/.env.local
apps/*/.env.*.local
```
   Do NOT ignore `bun.lockb` (per D-12-02 / P-17 — it MUST be committed).
3. After Task 2 completes `bun install`, commit the generated `bun.lockb` at repo root in the same commit as `package.json` (P-17 — lockfile travels with manifest changes).
  </action>
  <verify>
    <automated>test -f package.json && grep -q '"workspaces"' package.json && grep -q 'apps/\*/\.next' .gitignore && ! grep -qE '^bun\.lockb$' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at repo root with `"workspaces": ["apps/*"]`
    - `package.json` has `"private": true` (prevents accidental publish)
    - `.gitignore` contains `apps/*/node_modules` and `apps/*/.next`
    - `.gitignore` does NOT have `bun.lockb` in any ignore pattern
    - `package.json` has `audit` script running `bun audit --audit-level high`
  </acceptance_criteria>
  <done>Repo is a Bun workspace; .gitignore prevents build artifact commits; lockfile will be committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold apps/admin Next.js app with pinned versions + shadcn init</name>
  <files>
    apps/admin/package.json,
    apps/admin/tsconfig.json,
    apps/admin/next.config.mjs,
    apps/admin/postcss.config.mjs,
    apps/admin/tailwind.config.ts,
    apps/admin/components.json,
    apps/admin/.env.example,
    apps/admin/app/layout.tsx,
    apps/admin/app/page.tsx,
    apps/admin/app/globals.css,
    apps/admin/lib/utils.ts,
    apps/admin/next-env.d.ts,
    bun.lockb
  </files>
  <read_first>
    - `.planning/phases/12-infra-foundation/12-CONTEXT.md` §decisions D-12-01 (versions), D-12-03 (no DB), D-12-21 (dark mode default), D-12-29 (workspaces)
    - `.planning/research/STACK.md` — dependency table for Next.js / React / Zod / Tailwind / shadcn exact versions
    - `.planning/research/PITFALLS.md` P-01 (CVE-2025-66478), P-05 (Bun native modules)
    - `/Users/admin/hub/workspace/homelab/CLAUDE.md` — "simplest solution first; do not guess APIs; check real signatures"
    - Context7 `mcp__context7__resolve-library-id` + `mcp__context7__get-library-docs` for `/vercel/next.js` (topic: "app router config standalone output") — verify 15.2.4 config surface
  </read_first>
  <action>
Run context7 first to verify Next 15.2.4 config surface.

1. Create `apps/admin/package.json` with EXACT pinned versions (per D-12-01 — do NOT bump):
```json
{
  "name": "@homelab/admin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3847 -H 127.0.0.1",
    "build": "next build",
    "start": "next start -p 3847 -H 127.0.0.1",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "15.2.4",
    "react": "19.2.5",
    "react-dom": "19.2.5",
    "zod": "3.24.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5.6.0",
    "eslint": "^9",
    "eslint-config-next": "15.2.4",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
```
   Rationale for dev/start: D-12-04 binds `127.0.0.1:3847`. If Zod 3.24.1 does not exist on npm, use highest available 3.24.x (NOT 3.25 and NEVER 4.x).

2. Create `apps/admin/tsconfig.json` matching Next 15 defaults (strict: true, moduleResolution: "Bundler", paths `"@/*": ["./*"]`, jsx: "preserve", include `["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"]`).

3. Create `apps/admin/next.config.mjs`:
```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  reactStrictMode: true,
  poweredByHeader: false,
  experimental: {
    // Nonce-based CSP via middleware (Plan 04). No runtime config here.
  },
};
export default nextConfig;
```

4. Create `apps/admin/tailwind.config.ts` with `darkMode: 'class'`, content glob `['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}']`, and a `theme.extend.colors` block wired to CSS variables (shadcn convention). NO theme toggle (D-12-21: dark mode default, no toggle).

5. Create `apps/admin/postcss.config.mjs`:
```js
export default { plugins: { tailwindcss: {}, autoprefixer: {} } };
```

6. Create `apps/admin/components.json` (shadcn config) targeting `"style": "default"`, `"baseColor": "slate"`, `"cssVariables": true`, `"rsc": true`, `"tsx": true`, aliases `"components": "@/components"`, `"utils": "@/lib/utils"`.

7. Create `apps/admin/lib/utils.ts`:
```ts
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```

8. Create `apps/admin/app/globals.css` with shadcn CSS variables for dark theme as DEFAULT on `:root` (not `.dark`). Include Tailwind directives. CSS variables land on `:root` so the toggle can be added later without refactor (D-12-21).

9. Create minimal `apps/admin/app/layout.tsx`:
```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Homelab Admin",
  description: "Homelab operator dashboard",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className="min-h-screen bg-background text-foreground antialiased">
        {children}
      </body>
    </html>
  );
}
```

10. Create `apps/admin/app/page.tsx` — temporary placeholder (real layout lands in Plan 05):
```tsx
export default function Home() {
  return <main className="p-8">Homelab Admin — scaffold OK</main>;
}
```

11. Create `apps/admin/.env.example` (commit this; `.env.local` stays gitignored):
```
# Copy to apps/admin/.env.local for local dev (gitignored)
AUTH_URL=http://localhost:3847
AUTH_SECRET=generate-with-openssl-rand-base64-32
HOMELAB_ADMIN_GITHUB_OAUTH_CLIENT_ID=
HOMELAB_ADMIN_GITHUB_OAUTH_CLIENT_SECRET=
HOMELAB_ADMIN_ALLOWED_GITHUB_LOGINS=makscee
```

12. Run from repo root: `bun install` — this creates/updates root `bun.lockb`. Commit the lockfile together with all package.json changes (P-17).

13. Run `cd apps/admin && bun run build` — MUST succeed. Build failure here blocks everything downstream.

14. Run `bun audit --audit-level high` from repo root — MUST exit 0. If HIGH/CRITICAL vulns exist, surface them and halt.
  </action>
  <verify>
    <automated>cd apps/admin && bun install --frozen-lockfile && bun run build && cd .. && bun audit --audit-level high</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/package.json` contains `"next": "15.2.4"` (EXACT string match per D-12-01 / P-01)
    - `apps/admin/package.json` contains `"react": "19.2.5"` and `"react-dom": "19.2.5"`
    - `apps/admin/package.json` `zod` version matches `^3\.24\.` (NOT 3.25, NOT 4.x)
    - `apps/admin/package.json` dev script binds port 3847 on 127.0.0.1 (`next dev -p 3847 -H 127.0.0.1`)
    - `apps/admin/next.config.mjs` contains `output: 'standalone'` (required by Ansible deploy in Plan 09)
    - `cd apps/admin && bun run build` exits 0
    - `bun audit --audit-level high` exits 0 at repo root
    - `bun.lockb` exists at repo root and is staged (not gitignored)
    - `apps/admin/tailwind.config.ts` contains `darkMode: 'class'`
    - `apps/admin/app/layout.tsx` sets `<html lang="en" className="dark">` (dark mode default per D-12-21)
    - `apps/admin/.env.example` exists with `HOMELAB_ADMIN_ALLOWED_GITHUB_LOGINS=makscee` as default
  </acceptance_criteria>
  <done>Empty Next.js app builds green with pinned CVE-patched versions, dark mode by default, Bun workspaces resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/Bun registry → repo | Untrusted dep code enters supply chain at `bun install` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | Next.js RSC (CVE-2025-66478) | mitigate | Pin Next.js 15.2.4 in `apps/admin/package.json` — EXACT string match validated in acceptance criteria (P-01) |
| T-12-01-02 | Tampering | Transitive dep with HIGH/CRITICAL CVE | mitigate | `bun audit --audit-level high` gate in Task 2; script wired into root package.json; blocks deploy (Plan 10 re-runs before deploy) |
| T-12-01-03 | Tampering | Lockfile drift between dev and prod | mitigate | `bun.lockb` committed at repo root (D-12-02 / P-17); Ansible deploy (Plan 09) uses `--frozen-lockfile` |
| T-12-01-04 | Information Disclosure | `.env.local` committed by accident | mitigate | `.gitignore` blocks `apps/*/.env.local`; `.env.example` is the only committed env template |

Residual risk: LOW — no runtime components yet; threat surface limited to supply chain.
</threat_model>

<verification>
- `bun install --frozen-lockfile` resolves deterministically
- `bun run build` succeeds with `output: 'standalone'` directory layout
- `bun audit --audit-level high` exits 0
- Next.js version pinned to 15.2.4 (manual grep of package.json)
</verification>

<success_criteria>
- SC #3 anchor: Next.js pinned to CVE-2025-66478-patched 15.2.4; `bun audit` clean
- Scaffold ready for Plan 04 (auth middleware) and Plan 05 (layout)
</success_criteria>

<output>
After completion, create `.planning/phases/12-infra-foundation/12-01-SUMMARY.md` documenting:
- Exact Next.js / React / Zod patch versions installed
- `bun audit` output summary
- Any Bun install warnings (P-05: note any native module warnings — full smoke test on mcow is Plan 09's job)
</output>
