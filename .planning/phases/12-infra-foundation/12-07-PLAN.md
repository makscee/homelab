---
phase: 12-infra-foundation
plan: 07
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - servers/mcow/homelab-admin.service
  - servers/mcow/inventory.md
autonomous: true
requirements: [INFRA-01, INFRA-06]
must_haves:
  truths:
    - "servers/mcow/homelab-admin.service runs the app as User=homelab-admin Group=homelab-admin (NOT root)"
    - "The unit's hardening directives match lxc-probe.md recommendation (privileged vs unprivileged branch)"
    - "EnvironmentFile=/etc/homelab-admin/env — no secrets in the unit file itself"
    - "ExecStart binds 127.0.0.1:3847 (via env PORT=3847 HOSTNAME=127.0.0.1 consumed by next start)"
    - "Restart=on-failure with RestartSec=5 — service self-heals on crash"
    - "servers/mcow/inventory.md registers port 3847 + the new service"
  artifacts:
    - path: "servers/mcow/homelab-admin.service"
      provides: "systemd unit — authored in-repo, Ansible copies to /etc/systemd/system/ in Plan 09"
      contains: "User=homelab-admin"
    - path: "servers/mcow/inventory.md"
      provides: "Port + service registry update"
      contains: "3847"
  key_links:
    - from: "servers/mcow/homelab-admin.service"
      to: "servers/mcow/lxc-probe.md (Plan 03)"
      via: "Hardening block transcribed from probe recommendation"
    - from: "servers/mcow/homelab-admin.service"
      to: "/etc/homelab-admin/env (Plan 08 writes)"
      via: "EnvironmentFile directive"
---

<objective>
Author the systemd unit file in-repo (`servers/mcow/homelab-admin.service`) matching the mcow LXC privilege level discovered in Plan 03. Ansible Plan 09 copies it verbatim to `/etc/systemd/system/` on mcow. Update `servers/mcow/inventory.md` with port 3847 and the new service row.

Purpose: SC #1 backbone — the unit that runs the app. SC #5 (idempotency) depends on having the unit file in-repo so Ansible can compare + short-circuit.
Output: Two files updated. No deploy yet — Plan 09 handles install + start + smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-infra-foundation/12-CONTEXT.md
@.planning/research/PITFALLS.md
@servers/mcow/lxc-probe.md
@servers/mcow/voidnet-api.service
@servers/mcow/inventory.md
@ansible/group_vars/all.yml

<interfaces>
<!-- CONTEXT: D-12-04 (127.0.0.1:3847), D-12-06 homelab-admin canonical path, D-12-25 (User=homelab-admin, not root), D-12-26 (hardening conditional on probe), D-12-27 (ExecStart path), D-12-28 (Restart=on-failure) -->
<!-- PITFALLS: P-15 (PrivateTmp/ProtectSystem in LXC), P-anti-pattern 3 (never run as root) -->

Reference shape from `servers/mcow/voidnet-api.service`:
```
[Unit]
Description=VoidNet Config API
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/voidnet
EnvironmentFile=/opt/voidnet/.env
ExecStart=/opt/voidnet/voidnet-api
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

Plan 07 output hardens further: dedicated user, EnvironmentFile outside the install dir, resource limits, hardening block from probe.

`next start` binds via env:
- `HOSTNAME=127.0.0.1` (or `-H 127.0.0.1` flag)
- `PORT=3847`
- Alternative: `ExecStart=/usr/local/bin/bun run start` — package.json Plan 01 has `"start": "next start -p 3847 -H 127.0.0.1"`. Unit uses `bun run start` to respect package.json.

Bun binary path: `/usr/local/bin/bun` (verified via Plan 02 `bun_version` + Ansible install location in Plan 09).

lxc-probe.md determines hardening branch — Plan 03's output:
- If **privileged**: `ProtectSystem=strict`, `PrivateTmp=yes`, `ProtectKernelTunables=yes`, `ProtectControlGroups=yes`
- If **unprivileged**: `ProtectSystem=full`, drop `PrivateTmp`, drop `ProtectKernelTunables`

Plan 07 executor READS `servers/mcow/lxc-probe.md` (Plan 03) and picks the active block. If the probe output is missing, STOP and escalate (this is the only cross-plan hard dependency in Wave 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author servers/mcow/homelab-admin.service with probe-matched hardening</name>
  <files>servers/mcow/homelab-admin.service</files>
  <read_first>
    - `servers/mcow/lxc-probe.md` (Plan 03 output — MANDATORY. Read the "Systemd Hardening Recommendation for Plan 07" section)
    - `servers/mcow/voidnet-api.service` (shape reference)
    - `ansible/group_vars/all.yml` (Plan 02 — variable names `homelab_admin_install_dir`, `homelab_admin_env_dir`, `homelab_admin_service_user`, `homelab_admin_port`)
    - `.planning/phases/12-infra-foundation/12-CONTEXT.md` §D-12-25 through D-12-28
    - `.planning/research/PITFALLS.md` §P-15
  </read_first>
  <action>
1. Open `servers/mcow/lxc-probe.md` and identify whether mcow is privileged or unprivileged. The active `[Service]` block is already copy-paste-ready there.

2. Create `servers/mcow/homelab-admin.service`:

```
[Unit]
Description=Homelab Admin Dashboard (Next.js on Bun)
Documentation=https://github.com/makscee/homelab/tree/main/apps/admin
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=homelab-admin
Group=homelab-admin
WorkingDirectory=/opt/homelab-admin/app
EnvironmentFile=/etc/homelab-admin/env
Environment=NODE_ENV=production
Environment=PORT=3847
Environment=HOSTNAME=127.0.0.1
ExecStart=/usr/local/bin/bun run start
Restart=on-failure
RestartSec=5s
# Resource limits — tune after 24h soak (Plan 10 + Phase 17 metrics)
LimitNOFILE=65536

# ── Hardening ────────────────────────────────────────────────────────────
# Values below match the LXC privilege level discovered in Plan 03 (servers/mcow/lxc-probe.md).
# P-15 — PrivateTmp/ProtectSystem=strict break inside unprivileged Proxmox LXC (overlay fs + user namespace quirks).
NoNewPrivileges=yes
ProtectHome=yes

# BEGIN probe-conditional block — transcribe ONE of the two blocks from lxc-probe.md (NOT both)
# ─── If privileged LXC (lxc-probe.md shows unprivileged: 0 or missing): ───
ProtectSystem=strict
PrivateTmp=yes
ProtectKernelTunables=yes
ProtectControlGroups=yes
ReadWritePaths=/opt/homelab-admin/app/.next /tmp

# ─── If unprivileged LXC (lxc-probe.md shows unprivileged: 1): ───
# ProtectSystem=full
# # PrivateTmp=yes           # commented — may break in unprivileged LXC (P-15)
# # ProtectKernelTunables    # commented — requires CAP_SYS_ADMIN unavailable in unprivileged LXC
# # ProtectControlGroups     # commented — same reason
# ReadWritePaths=/opt/homelab-admin/app/.next
# END probe-conditional block

[Install]
WantedBy=multi-user.target
```

3. EXECUTOR INSTRUCTION: Delete the inactive block entirely (not just comment). Keep ONLY the block matching `lxc-probe.md`'s recommendation. The committed unit file must contain one hardening stance, not both — ambiguity here is a deploy blocker.

4. Add a top-of-file comment recording the probe decision:
```
# mcow LXC privilege: <privileged|unprivileged>   (per servers/mcow/lxc-probe.md, Plan 12-03)
# Hardening block selected: <corresponding choice>
```
  </action>
  <verify>
    <automated>test -f servers/mcow/homelab-admin.service && grep -c "^User=homelab-admin$" servers/mcow/homelab-admin.service | grep -q "^1$" && grep -c "^Group=homelab-admin$" servers/mcow/homelab-admin.service | grep -q "^1$" && grep -q "^EnvironmentFile=/etc/homelab-admin/env$" servers/mcow/homelab-admin.service && grep -q "^Environment=PORT=3847$" servers/mcow/homelab-admin.service && grep -q "^Environment=HOSTNAME=127\\.0\\.0\\.1$" servers/mcow/homelab-admin.service && grep -q "^ExecStart=/usr/local/bin/bun run start$" servers/mcow/homelab-admin.service && grep -q "^NoNewPrivileges=yes$" servers/mcow/homelab-admin.service && grep -q "^Restart=on-failure$" servers/mcow/homelab-admin.service && grep -q "^RestartSec=5s$" servers/mcow/homelab-admin.service && grep -c "^ProtectSystem=" servers/mcow/homelab-admin.service | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - File `servers/mcow/homelab-admin.service` exists
    - Contains EXACT line `User=homelab-admin` (not root — P-anti-pattern 3)
    - Contains EXACT line `Group=homelab-admin`
    - Contains `EnvironmentFile=/etc/homelab-admin/env`
    - Contains `Environment=PORT=3847` AND `Environment=HOSTNAME=127.0.0.1` (loopback bind — D-12-04)
    - Contains `ExecStart=/usr/local/bin/bun run start`
    - Contains `NoNewPrivileges=yes` AND `ProtectHome=yes` (unconditional hardening)
    - Contains EXACTLY ONE `ProtectSystem=` line (either `strict` or `full` — not both)
    - Contains `Restart=on-failure` and `RestartSec=5s` (D-12-28)
    - Top comment references `servers/mcow/lxc-probe.md` and names the privilege level
    - `systemd-analyze verify servers/mcow/homelab-admin.service` on operator laptop passes (may show warnings about the missing binary/paths — ignore those; syntax errors fail this)
  </acceptance_criteria>
  <done>Unit file committed in-repo, matched to mcow's real LXC profile. Plan 09 copies verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update servers/mcow/inventory.md — register port 3847 + homelab-admin service</name>
  <files>servers/mcow/inventory.md</files>
  <read_first>
    - `servers/mcow/inventory.md` (current content — additive only)
    - `.planning/phases/12-infra-foundation/12-CONTEXT.md` §D-12-05 (port registration)
    - `servers/mcow/README.md` if it exists (crosslink style)
  </read_first>
  <action>
1. Open `servers/mcow/inventory.md`. Identify the port-allocation table and the service list section (typical shape for this repo's `inventory.md` files — see `servers/docker-tower/inventory.md` if unfamiliar).

2. Add to the port allocation table (match existing column style):

```
| 3847 | homelab-admin | Next.js dashboard — Tailnet only via Caddy → homelab.makscee.ru (Plan 12) | 127.0.0.1 |
```

3. Add to the systemd services section (match existing style — `voidnet-api.service`, `claude-usage-exporter.service` rows):

```
- `homelab-admin.service` — Next.js admin dashboard (Bun runtime)
  - Binding: `127.0.0.1:3847`
  - User: `homelab-admin` (system user, no shell)
  - Install dir: `/opt/homelab-admin/app`
  - Env file: `/etc/homelab-admin/env` (written by Ansible from SOPS — Plan 12-08)
  - Public hostname: `https://homelab.makscee.ru` (via Caddy reverse proxy — Plan 12-09)
  - Status: scaffold deployed Phase 12; page content ships Phases 13-18
```

If `inventory.md` lacks either section, create them with minimal headings (matching the repo convention visible in sibling `servers/*/inventory.md`).
  </action>
  <verify>
    <automated>grep -q "3847" servers/mcow/inventory.md && grep -q "homelab-admin" servers/mcow/inventory.md && grep -q "127\\.0\\.0\\.1" servers/mcow/inventory.md && grep -q "/etc/homelab-admin/env" servers/mcow/inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - `servers/mcow/inventory.md` lists port `3847` tied to `homelab-admin` service bound to `127.0.0.1`
    - Contains service entry for `homelab-admin.service` with bind + user + install dir + env file + public hostname
    - Existing entries (voidnet-*, prometheus, grafana/alertmanager if present) untouched — purely additive edit
  </acceptance_criteria>
  <done>Port registry + service list reflect the new homelab-admin service. Future-phase collision checks land here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| systemd → Bun process | Unit-file-controlled execution context for long-running Node-ish runtime |
| /etc/homelab-admin/env → Bun process env | Secrets enter process memory here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Elevation of Privilege | App runs as root → full host compromise on RCE | mitigate | `User=homelab-admin`, `Group=homelab-admin`, `NoNewPrivileges=yes` — verified in acceptance criteria (D-12-25, P-anti-3) |
| T-12-07-02 | Tampering | App writes outside expected paths (log poisoning, binary overwrite) | mitigate | `ProtectSystem=strict` OR `ProtectSystem=full` (per probe); `ReadWritePaths=` scopes writable tree to `/opt/homelab-admin/app/.next [/tmp]` |
| T-12-07-03 | Information Disclosure | Other processes on mcow read /etc/homelab-admin/env | mitigate | Plan 08 sets the env file to mode 0600 owned by `homelab-admin:homelab-admin`. Unit references EnvironmentFile= which systemd reads pre-user-switch — no in-process file handle exposed. |
| T-12-07-04 | Denial of Service | App crash-loops, pins CPU on mcow | mitigate | `Restart=on-failure` (not `always`) with `RestartSec=5s` (D-12-28). Plan 09 smoke check catches first-boot failure. Phase 17 adds Prometheus alert for crashloop. |
| T-12-07-05 | Elevation of Privilege | Hardening flags break in unprivileged LXC (P-15) → deploy failure | mitigate | Plan 03 probe determined actual privilege; unit transcribes the matching block. Conservative degradation documented inline. |
| T-12-07-06 | Tampering | Unit file drift between repo and mcow | mitigate | Ansible Plan 09 copies `servers/mcow/homelab-admin.service` with `mode: 0644` and registers handler for `daemon-reload` + `restart` — drift surfaces as a `changed` task on subsequent `--check` runs |

Residual: LOW — if app RCE, blast radius limited to `homelab-admin` user + writable paths + network egress (Tailnet only).
</threat_model>

<verification>
- `systemd-analyze verify servers/mcow/homelab-admin.service` clean
- Grep assertions above all pass
- `lxc-probe.md` → unit-file hardening match is reviewable (top comment names the probe result)
</verification>

<success_criteria>
- SC #1 foundation: the process Plan 09 starts is correctly sandboxed
- SC #5 foundation: idempotent deploy depends on a stable unit file in-repo
</success_criteria>

<output>
After completion, create `.planning/phases/12-infra-foundation/12-07-SUMMARY.md` documenting:
- Privilege level decided + which hardening block was transcribed
- Any `systemd-analyze verify` output (warnings vs errors)
</output>
</content>
</invoke>