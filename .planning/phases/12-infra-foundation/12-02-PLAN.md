---
phase: 12-infra-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/group_vars/all.yml
  - ansible/requirements.yml
autonomous: true
requirements: [INFRA-06]
must_haves:
  truths:
    - "Ansible deploy (Plan 09) can reference `homelab_admin_port: 3847` from group_vars/all.yml"
    - "Ansible deploy can reference `bun_version` from group_vars/all.yml (exact pinned version)"
    - "Required Ansible collections (community.general for blockinfile) are declared in requirements.yml"
  artifacts:
    - path: "ansible/group_vars/all.yml"
      provides: "homelab_admin_port + bun_version vars"
      contains: "homelab_admin_port: 3847"
    - path: "ansible/requirements.yml"
      provides: "Ansible collections list"
      contains: "community.general"
  key_links:
    - from: "ansible/group_vars/all.yml"
      to: "ansible/playbooks/deploy-homelab-admin.yml (Plan 09)"
      via: "Ansible var lookup"
      pattern: "homelab_admin_port"
---

<objective>
Extend Ansible controller config with the two Phase-12-introduced global variables and any missing Ansible collections required by the deploy playbook (Plan 09) and Caddyfile patch (Plan 09).

Purpose: Wave-1 parallel-safe Ansible prep. Keeps Plan 09 focused on playbook logic (not collection install).
Output: `group_vars/all.yml` extended; `requirements.yml` lists collections needed by Plan 09 (`community.general` for `blockinfile`; `ansible.posix` may be needed for `sysctl`/`mount` — audit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-infra-foundation/12-CONTEXT.md
@ansible/group_vars/all.yml
@ansible/requirements.yml
@ansible/inventory/homelab.yml

<interfaces>
<!-- CONTEXT: D-12-04 (port 3847), D-12-01 (bun_version — planner picks latest stable), D-12-11 (blockinfile — community.general) -->

Existing `ansible/group_vars/all.yml`:
```yaml
node_exporter_version: "1.8.2"
node_exporter_web_listen_address: "0.0.0.0:9100"
# comments...
```

Existing `ansible/requirements.yml`:
```yaml
collections:
  - name: prometheus.prometheus
    version: "0.29.1"
```

`blockinfile` is in `community.general` — verify presence. If not present, add.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add homelab_admin_port and bun_version to group_vars/all.yml</name>
  <files>ansible/group_vars/all.yml</files>
  <read_first>
    - `ansible/group_vars/all.yml` (current content — append, don't rewrite)
    - `.planning/phases/12-infra-foundation/12-CONTEXT.md` §decisions D-12-01, D-12-04
  </read_first>
  <action>
Append to `ansible/group_vars/all.yml` (do NOT remove existing keys):

```yaml

# ── v3.0 Phase 12 — homelab-admin dashboard on mcow ─────────────────────
# Port 3847 chosen because Docker (Grafana stack) already owns 3000 on mcow.
# App binds 127.0.0.1:3847 — Caddy reverse-proxies from homelab.makscee.ru (Tailnet-only via Tailscale ACL).
homelab_admin_port: 3847
homelab_admin_domain: "homelab.makscee.ru"
homelab_admin_install_dir: "/opt/homelab-admin/app"
homelab_admin_env_dir: "/etc/homelab-admin"
homelab_admin_service_user: "homelab-admin"
homelab_admin_service_group: "homelab-admin"

# Bun runtime — pinned. Installed to /usr/local/bin/bun by Ansible task in Plan 09.
# NOTE: bump this deliberately only when testing a newer Bun on mcow confirms
# no native-module regressions (PITFALLS P-05). Latest stable at plan authoring: 1.1.38.
bun_version: "1.1.38"
```

Pin `bun_version` to the latest Bun 1.1.x stable at execution time. Executor confirms latest via `curl -fsSL https://api.github.com/repos/oven-sh/bun/releases/latest | grep tag_name` and updates the pin if a newer `1.1.x` is available. Do NOT bump to `1.2.x` without P-05 re-validation.
  </action>
  <verify>
    <automated>grep -q "^homelab_admin_port: 3847$" ansible/group_vars/all.yml && grep -q "^bun_version:" ansible/group_vars/all.yml && grep -q "^homelab_admin_service_user:" ansible/group_vars/all.yml</automated>
  </verify>
  <acceptance_criteria>
    - `ansible/group_vars/all.yml` contains the line `homelab_admin_port: 3847` (exact)
    - `ansible/group_vars/all.yml` contains `bun_version:` key with a `1.1.x` version string
    - `ansible/group_vars/all.yml` contains `homelab_admin_install_dir: "/opt/homelab-admin/app"`
    - `ansible/group_vars/all.yml` contains `homelab_admin_env_dir: "/etc/homelab-admin"`
    - `ansible/group_vars/all.yml` contains `homelab_admin_service_user: "homelab-admin"`
    - Existing `node_exporter_*` keys still present (no regression)
  </acceptance_criteria>
  <done>Plan 09 playbook can reference `{{ homelab_admin_port }}`, `{{ bun_version }}`, etc. without inline defaults.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Declare required Ansible collections in requirements.yml</name>
  <files>ansible/requirements.yml</files>
  <read_first>
    - `ansible/requirements.yml` (current content)
    - Context7 `mcp__context7__get-library-docs` for `/ansible/ansible` topic "collection community.general blockinfile" to confirm module path
  </read_first>
  <action>
Append to `ansible/requirements.yml` the collections required by Plan 09's deploy playbook:

```yaml
  # Phase 12 additions (v3.0 homelab-admin deploy)
  - name: community.general
    version: ">=9.0.0"   # provides blockinfile (Plan 09 Caddyfile patch — D-12-11)
  - name: ansible.posix
    version: ">=1.5.0"   # provides synchronize (Plan 09 rsync) + file ACL helpers
```

Keep existing `prometheus.prometheus` collection entry intact.

Document install step in a comment at the top of `requirements.yml` (if not already present):
```yaml
# Install on controller: cd ansible && ansible-galaxy collection install -r requirements.yml --upgrade
```
  </action>
  <verify>
    <automated>grep -q "community.general" ansible/requirements.yml && grep -q "ansible.posix" ansible/requirements.yml && grep -q "prometheus.prometheus" ansible/requirements.yml</automated>
  </verify>
  <acceptance_criteria>
    - `ansible/requirements.yml` contains `community.general` entry (provides `blockinfile` used in Plan 09)
    - `ansible/requirements.yml` contains `ansible.posix` entry (provides `synchronize` used in Plan 09)
    - `ansible/requirements.yml` retains existing `prometheus.prometheus` entry
    - `ansible-galaxy collection install -r ansible/requirements.yml` completes without error when executed on operator laptop
  </acceptance_criteria>
  <done>Controller has all collections Plan 09's deploy playbook needs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Ansible Galaxy → controller | Upstream collection code executes at deploy time |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | Unpinned Bun version on mcow drifts between deploys (P-05 regression risk) | mitigate | `bun_version` in group_vars is pinned to exact `1.1.x` string; Plan 09 installer checks installed version matches pin and skips otherwise |
| T-12-02-02 | Tampering | Unpinned Ansible collection → CI/deploy drift or supply-chain injection | mitigate | `requirements.yml` uses `>=9.0.0` floor on community.general; operator manually runs `ansible-galaxy collection install -r requirements.yml --upgrade` — opt-in upgrades only. Tightening to exact-pin is a Phase 19 hardening item. |
| T-12-02-03 | Information Disclosure | group_vars leak internals | accept | Values (port, install dir, user name) are not secret; secrets live in `secrets/mcow.sops.yaml` (Plan 08) |

Residual: LOW — this plan only adds non-secret configuration vars + collection declarations. No runtime attack surface created.
</threat_model>

<verification>
- Ansible lint on group_vars/all.yml passes (`ansible-lint ansible/group_vars/all.yml` or equivalent syntax check via `ansible-inventory --list`)
- `ansible-galaxy collection install -r ansible/requirements.yml --upgrade` completes clean
</verification>

<success_criteria>
- Plan 09 can reference `{{ homelab_admin_port }}`, `{{ homelab_admin_install_dir }}`, `{{ bun_version }}` as Ansible vars without inline defaults
- SC #5 (idempotent deploy) is unblocked from the vars side
</success_criteria>

<output>
After completion, create `.planning/phases/12-infra-foundation/12-02-SUMMARY.md` documenting:
- Exact Bun version pinned (confirmed latest 1.1.x at execution time)
- Collections added + their versions
</output>
</content>
</invoke>