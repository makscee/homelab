---
phase: 02-service-documentation
plan: 04
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - servers/mcow/voidnet-bot.service
  - servers/mcow/voidnet-api.service
  - servers/mcow/voidnet-overseer.service
  - servers/mcow/voidnet-satellite.service
  - servers/mcow/.env.example
  - servers/mcow/README.md
autonomous: true
requirements:
  - SVC-02
tags:
  - mcow
  - systemd
  - voidnet
  - rust
must_haves:
  truths:
    - "Every VoidNet systemd unit file in servers/mcow/ is verified against the live unit on mcow — file content matches OR drift is resolved"
    - "Stale units (overseer and satellite, unless LIVE-AUDIT proves otherwise) are marked with a `# STALE:` header and flagged for deletion in README"
    - "servers/mcow/.env.example lists every env var referenced by any live voidnet-*.service unit or used by the VoidNet binaries (discovered via live inspection)"
    - "servers/mcow/README.md documents: live service list, binary locations, systemd commands, restart procedure, SQLite DB path, stale-unit cleanup steps"
    - "Pitfall 3 is respected — NO docker-compose.yml is created for mcow; SVC-02 is met by .service files + README"
  artifacts:
    - path: servers/mcow/README.md
      provides: "mcow operator notes"
      min_lines: 35
    - path: servers/mcow/.env.example
      provides: "env var template for VoidNet services"
      contains: "REDACTED"
    - path: servers/mcow/voidnet-bot.service
      provides: "Live systemd unit definition for VoidNet bot"
      contains: "ExecStart"
    - path: servers/mcow/voidnet-api.service
      provides: "Live systemd unit definition for VoidNet API"
      contains: "ExecStart"
  key_links:
    - from: servers/mcow/voidnet-bot.service
      to: "live /etc/systemd/system/voidnet-bot.service on mcow"
      via: content parity verification
      pattern: "ExecStart="
    - from: servers/mcow/README.md
      to: "stale unit cleanup on live host"
      via: documented `systemctl disable` steps
      pattern: "systemctl disable"
---

<objective>
Bring `servers/mcow/` into SVC-02 compliance. mcow runs VoidNet Rust binaries via systemd (not Docker), so the deliverable is systemd unit files + README + .env.example, NOT a docker-compose.yml (Pitfall 3).

Audit live systemd units: verify bot and api are active, flag overseer and satellite as stale (per D-07/D-08 and Research findings), document the live topology, and provide explicit cleanup commands for removing the stale units.

Purpose: VoidNet bot and API serve live user traffic; the repo must be able to reproduce their systemd configuration exactly on a rebuild.

Output: Verified systemd unit files, enriched README covering binary locations and cleanup procedure, complete .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-service-documentation/02-CONTEXT.md
@.planning/phases/02-service-documentation/02-RESEARCH.md
@.planning/phases/02-service-documentation/LIVE-AUDIT.md
@.planning/phases/02-service-documentation/02-01-SUMMARY.md
@servers/mcow/voidnet-bot.service
@servers/mcow/voidnet-api.service
@servers/mcow/voidnet-overseer.service
@servers/mcow/voidnet-satellite.service
@servers/mcow/inventory.md
@scripts/verify-phase02.sh

<interfaces>
<!-- mcow runs Rust binaries, NOT Docker. SVC-02 says "Docker Compose files for mcow services" but this is Pitfall 3: -->
<!-- the actual architecture is systemd + binaries. SVC-02 is met by .service files + README. Do NOT create compose files on mcow. -->

<!-- VoidNet services identified in repo: -->
<!--   voidnet-bot       — Telegram bot (likely live) -->
<!--   voidnet-api       — HTTP API (likely live) -->
<!--   voidnet-overseer  — flagged stale per D-07 -->
<!--   voidnet-satellite — flagged stale per D-07 -->
<!-- VoidNet SQLite DB: /opt/voidnet/voidnet.db (per research Runtime State Inventory) -->
<!-- .env file: /opt/voidnet/.env -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit systemd units and verify .service file parity with live host</name>
  <read_first>
    - .planning/phases/02-service-documentation/LIVE-AUDIT.md (mcow section — systemctl is-active/is-enabled, ps -ef, filesystem)
    - servers/mcow/voidnet-bot.service
    - servers/mcow/voidnet-api.service
    - servers/mcow/voidnet-overseer.service
    - servers/mcow/voidnet-satellite.service
  </read_first>
  <files>
    servers/mcow/voidnet-bot.service,
    servers/mcow/voidnet-api.service,
    servers/mcow/voidnet-overseer.service,
    servers/mcow/voidnet-satellite.service
  </files>
  <action>
    Implements D-02 (verify against live), D-07 (audit stale), D-08 (flag for cleanup).

    Step 1 — From LIVE-AUDIT.md's mcow section, classify each of the 4 units:
    - `active + enabled` → LIVE — must have parity with repo copy
    - `inactive` OR `failed` OR `not-found` → STALE — mark file for cleanup flagging
    - `active but disabled` (unusual) → LIVE but note in README

    Step 2 — For each LIVE unit, pull the live file content and compare to repo copy:
    ```bash
    # Example for voidnet-bot — repeat for voidnet-api (and any unit LIVE-AUDIT classified as live)
    ssh root@mcow 'cat /etc/systemd/system/voidnet-bot.service' > /tmp/mcow-bot-live.service
    diff -u servers/mcow/voidnet-bot.service /tmp/mcow-bot-live.service > /tmp/mcow-bot-diff.txt
    ```
    If diff is non-empty: overwrite `servers/mcow/voidnet-bot.service` with `/tmp/mcow-bot-live.service`. Note the drift in the commit message (what changed).
    If diff is empty: no-op.

    Before committing, scrub any secrets that systemd units may contain inline:
    - `Environment=FOO=bar` lines where `bar` looks like a secret (API tokens, DB URLs with passwords) → replace the VALUE with `REDACTED-SEE-ENV` and move the real value logic into `EnvironmentFile=/opt/voidnet/.env`. If the live unit uses inline `Environment=` with secrets, keep the repo version with `EnvironmentFile=/opt/voidnet/.env` only (matching the existing pattern that .env.example already implies) AND document the drift so operator knows the live unit must be updated too.

    Step 3 — For each STALE unit (typically overseer and satellite per Research Pitfall — but defer to LIVE-AUDIT classification):
    Prepend these exact lines as the top of the file (above `[Unit]`):
    ```
    # STALE — flagged 2026-04-13
    # Per LIVE-AUDIT.md: systemctl is-active == inactive/failed at phase execution time.
    # Binary /usr/local/bin/voidnet-<component> likely absent or from deprecated architecture.
    # Cleanup procedure: see servers/mcow/README.md `## Stale Unit Cleanup`.
    # Keep this file until operator confirms cleanup on live host.
    ```

    If LIVE-AUDIT.md shows a unit is actually LIVE (e.g., overseer turns out to be running), do NOT flag it as stale — move to Step 2 treatment instead. Follow LIVE-AUDIT, not assumptions.

    Step 4 — If LIVE-AUDIT.md reveals any NEW voidnet-* unit that is not in the repo (e.g., `voidnet-portal.service` per Research Open Question 3):
    ```bash
    ssh root@mcow 'cat /etc/systemd/system/voidnet-<new>.service' > servers/mcow/voidnet-<new>.service
    ```
    Scrub any inline secrets as in Step 2. Add to commit.
  </action>
  <verify>
    <automated>for f in servers/mcow/voidnet-*.service; do grep -q 'ExecStart=' "$f" || { echo "FAIL: $f missing ExecStart="; exit 1; }; done && ! grep -rE '^Environment=.*=[A-Za-z0-9_/+-]{20,}' servers/mcow/voidnet-*.service | grep -v REDACTED && (grep -l 'STALE' servers/mcow/voidnet-*.service | wc -l | awk '{exit ($1>=1)?0:1}')</automated>
  </verify>
  <acceptance_criteria>
    - Every `servers/mcow/voidnet-*.service` file has an `ExecStart=` line (valid systemd unit)
    - No `Environment=KEY=VALUE` line has a 20+ character value that looks like a secret (scrubbed to `REDACTED-SEE-ENV` or moved to `EnvironmentFile=`)
    - At least one unit file has a `# STALE` header (overseer and/or satellite per LIVE-AUDIT) — if LIVE-AUDIT unexpectedly shows no stale units, this check is waived and the commit message must explicitly note "LIVE-AUDIT showed all 4 units live"
    - Each LIVE unit (per LIVE-AUDIT) has content matching the live `/etc/systemd/system/...service` on mcow (diff of repo copy vs live is empty OR drift is resolved)
    - No unit file includes a literal bot token, API key, or password pattern (`grep -iE '(bot.?token|api.?key|password)[[:space:]]*=[[:space:]]*[A-Za-z0-9]{16,}' servers/mcow/voidnet-*.service` returns empty)
  </acceptance_criteria>
  <done>Systemd units in the repo are an accurate image of live state; stale units are flagged, not deleted, with cleanup guidance pending.</done>
</task>

<task type="auto">
  <name>Task 2: Update .env.example and write mcow README</name>
  <read_first>
    - .planning/phases/02-service-documentation/LIVE-AUDIT.md (mcow section — filesystem, running processes)
    - servers/mcow/voidnet-bot.service (post-Task-1)
    - servers/mcow/voidnet-api.service (post-Task-1)
    - servers/mcow/inventory.md
    - servers/mcow/.env.example (existing — note: CONTEXT.md notes this already exists with REDACTED values per Research Runtime State Inventory)
  </read_first>
  <files>servers/mcow/.env.example, servers/mcow/README.md</files>
  <action>
    Implements D-04 (README + env completeness) and D-09 (Claude's discretion on live VoidNet doc format — decided post-audit).

    Step 1 — Verify completeness of `.env.example` against live env. If file does not already exist in the repo, create it; if it exists (per Research), verify and augment:
    ```bash
    # What variables does the LIVE .env use?
    ssh root@mcow 'grep -oE "^[A-Z_][A-Z_0-9]*" /opt/voidnet/.env | sort -u' > /tmp/mcow-live-vars.txt

    # What variables does each systemd unit reference?
    for unit in servers/mcow/voidnet-{bot,api,overseer,satellite}.service; do
      grep -oE '\$\{?[A-Z_][A-Z_0-9]*\}?' "$unit" | tr -d '${}' | sort -u
    done | sort -u > /tmp/mcow-unit-vars.txt
    ```

    Ensure every variable in EITHER file appears as a line in `servers/mcow/.env.example`:
    ```
    # VoidNet .env template — deploy to /opt/voidnet/.env on mcow
    # Copy to /opt/voidnet/.env and replace REDACTED with real values.
    # Never commit the real /opt/voidnet/.env — only this template.

    # Telegram bot
    TELEGRAM_BOT_TOKEN=REDACTED

    # API service
    VOIDNET_API_PORT=REDACTED
    VOIDNET_API_HOST=REDACTED

    # Database
    DATABASE_URL=REDACTED  # sqlite:///opt/voidnet/voidnet.db typically

    # (every other var discovered in /tmp/mcow-live-vars.txt or unit references)
    ```

    If the repo file already contains vars, add any missing ones; do not remove existing ones unless LIVE-AUDIT proves they are unused.

    Step 2 — Create/overwrite `servers/mcow/README.md`:

    ```markdown
    # mcow

    **Host:** mcow (100.101.0.9) — Moscow
    **Role:** VoidNet bot + API (Telegram bot, HTTP API, SQLite-backed)
    **Architecture:** systemd + pre-compiled Rust binaries (NOT Docker — per Pitfall 3 in Phase 02 Research)
    **Access:** `ssh root@mcow` via Tailscale

    ## Live Services

    | Unit | Status | Binary | Purpose |
    |------|--------|--------|---------|
    | voidnet-bot.service | ACTIVE | /usr/local/bin/voidnet-bot | Telegram bot |
    | voidnet-api.service | ACTIVE | /usr/local/bin/voidnet-api | HTTP API |
    | voidnet-overseer.service | STALE (inactive) | /usr/local/bin/voidnet-overseer | Decommissioned |
    | voidnet-satellite.service | STALE (inactive) | /usr/local/bin/voidnet-satellite | Decommissioned |

    (Populate the Status column from LIVE-AUDIT.md classification; populate Binary from `ExecStart=` line in each .service file.)

    ## Layout on Host

    - Binaries: `/usr/local/bin/voidnet-{bot,api}` (deployed via scp or build-artifact drop from voidnet source workspace)
    - Config: `/opt/voidnet/.env` (world-readable FORBIDDEN — should be `chmod 600`, owned by voidnet user or root)
    - Database: `/opt/voidnet/voidnet.db` (SQLite, single file — Phase 3 backup scope)
    - Systemd units: `/etc/systemd/system/voidnet-*.service`

    ## Deploy / Restart Procedure

    Updating a binary:
    ```
    scp ./target/release/voidnet-bot root@mcow:/usr/local/bin/voidnet-bot.new
    ssh root@mcow 'mv /usr/local/bin/voidnet-bot{.new,} && systemctl restart voidnet-bot && systemctl status voidnet-bot --no-pager'
    ```

    Updating /opt/voidnet/.env:
    ```
    # Edit local copy, never commit real values
    scp /tmp/voidnet.env root@mcow:/opt/voidnet/.env
    ssh root@mcow 'chmod 600 /opt/voidnet/.env && systemctl restart voidnet-bot voidnet-api'
    shred -u /tmp/voidnet.env
    ```

    Reinstalling the systemd unit from repo:
    ```
    scp servers/mcow/voidnet-bot.service root@mcow:/etc/systemd/system/voidnet-bot.service
    ssh root@mcow 'systemctl daemon-reload && systemctl enable --now voidnet-bot'
    ```

    ## Stale Unit Cleanup

    `voidnet-overseer.service` and `voidnet-satellite.service` are from the old VoidNet architecture (pre-Animaya-era). They are flagged STALE in LIVE-AUDIT.md (is-active = inactive/failed).

    To clean up on the live host (when confirmed safe):
    ```
    ssh root@mcow 'systemctl disable voidnet-overseer voidnet-satellite'
    ssh root@mcow 'systemctl stop voidnet-overseer voidnet-satellite 2>/dev/null || true'
    ssh root@mcow 'rm -f /etc/systemd/system/voidnet-overseer.service /etc/systemd/system/voidnet-satellite.service'
    ssh root@mcow 'rm -f /usr/local/bin/voidnet-overseer /usr/local/bin/voidnet-satellite'
    ssh root@mcow 'systemctl daemon-reload'
    ```

    After confirming cleanup on live host, remove the `.service` files from the repo in a follow-up commit.

    ## Gotchas

    - **No Docker on mcow.** Do not introduce a docker-compose.yml here — the VoidNet stack is systemd + binaries by design (SVC-02 is met by .service files + README per Pitfall 3).
    - **SQLite single file** at `/opt/voidnet/voidnet.db` — lock contention possible if bot and api both write heavily; Phase 3 backup must account for WAL files (`voidnet.db-wal`, `voidnet.db-shm`).
    - **TELEGRAM_BOT_TOKEN** regeneration invalidates the running bot — coordinate with users before rotating.
    - **Binary updates are NOT tracked here.** VoidNet application source lives in a separate workspace repo per CONTEXT.md. This repo only tracks deployment config.

    ## Related Files

    - Inventory: `servers/mcow/inventory.md`
    - Systemd units: `servers/mcow/voidnet-*.service`
    - Env template: `servers/mcow/.env.example`
    ```
  </action>
  <verify>
    <automated>test -f servers/mcow/README.md && test -f servers/mcow/.env.example && [ "$(wc -l < servers/mcow/README.md)" -ge 35 ] && grep -q 'systemd' servers/mcow/README.md && grep -q 'voidnet.db' servers/mcow/README.md && grep -qi 'stale' servers/mcow/README.md && grep -q 'REDACTED' servers/mcow/.env.example && ! ls servers/mcow/docker-compose*.yml 2>/dev/null && bash scripts/verify-phase02.sh 2>&1 | grep -E 'SVC-02' | grep -v FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `servers/mcow/README.md` exists, >= 35 lines
    - Contains sections: `## Live Services`, `## Layout on Host`, `## Deploy / Restart Procedure`, `## Stale Unit Cleanup`, `## Gotchas`
    - Explicitly states mcow does NOT run Docker (Pitfall 3)
    - Stale Unit Cleanup section includes literal `systemctl disable` and `rm -f /etc/systemd/system/voidnet-` commands
    - References `/opt/voidnet/voidnet.db` (SQLite DB path)
    - `servers/mcow/.env.example` has at least one `REDACTED` placeholder
    - Every variable referenced in `servers/mcow/voidnet-*.service` files (via `${VAR}` or `$VAR` or `EnvironmentFile=`) appears as a line in `.env.example`
    - NO `servers/mcow/docker-compose*.yml` file exists (verify-phase02.sh SVC-02 section passes)
  </acceptance_criteria>
  <done>mcow SVC-02 is complete: systemd units verified, stale flagged with cleanup guidance, README + env template finalize the service definition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| systemd unit file → repo | Inline `Environment=` directives may carry secrets; must scrub before commit |
| /opt/voidnet/.env → operator machine during capture | Real bot token + API secrets live here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | Bot token / DB URL pasted inline in voidnet-bot.service from live copy | mitigate | Task 1 Step 2 scrubs 20+ char Environment= values; acceptance_criteria greps for bot.token|api.key|password patterns; prefer EnvironmentFile= reference |
| T-02-04-02 | Tampering | Stale units revived by accident (systemctl enable on an old overseer) | mitigate | Task 1 Step 3 adds `# STALE` header; README cleanup section provides explicit removal steps |
| T-02-04-03 | Information Disclosure | /opt/voidnet/.env contents end up in .env.example if operator copies rather than templates | mitigate | Task 2 explicitly requires VALUES = `REDACTED` only; acceptance_criteria verifies placeholder presence |
| T-02-04-04 | Repudiation | No audit trail of when stale units were flagged vs when they are eventually removed | accept | README + commit message capture flagging; actual removal is a follow-up commit |
| T-02-04-05 | Information Disclosure | SQLite DB at /opt/voidnet/voidnet.db may contain PII if readable | accept | Phase 3 backup/restore scope; Phase 2 only documents path |
</threat_model>

<verification>
- `for f in servers/mcow/voidnet-*.service; do grep -q ExecStart= "$f"; done` — all units valid
- `grep -q STALE servers/mcow/voidnet-*.service` — at least one stale flag present
- `! ls servers/mcow/docker-compose*.yml` — no compose on mcow (Pitfall 3 respected)
- `test -f servers/mcow/README.md && test -f servers/mcow/.env.example`
- `bash scripts/verify-phase02.sh` SVC-02 section all `  ok:`
</verification>

<success_criteria>
- SVC-02 complete: systemd units verified against live, stale flagged with cleanup procedure, README + env template present
- No docker-compose file created on mcow (Pitfall 3 respected)
- No plaintext secrets in any unit file
</success_criteria>

<output>
After completion, create `.planning/phases/02-service-documentation/02-04-SUMMARY.md` summarizing:
- Which units are live vs stale (from LIVE-AUDIT)
- Drift fixes applied to unit files
- .env.example variable count (before → after)
- Stale cleanup commands documented (ready for operator to run in Phase 3 or as follow-up)
</output>
