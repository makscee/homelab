---
phase: 17.1-migrate-jellyfin-to-dedicated-lxc-on-tower
plan: 05
subsystem: infra/media
tags: [jellyfin, lxc, docs, validation, phase-close]
dependency_graph:
  requires: [17.1-01, 17.1-02, 17.1-03, 17.1-04]
  provides: ["servers/jellyfin/inventory.md", "phase 17.1 closed"]
  affects: [CLAUDE.md, ROADMAP.md, STATE.md, servers/tower/README.md, servers/docker-tower/README.md, 17.1-VALIDATION.md]
tech_stack:
  added: []
  patterns: [per-LXC inventory doc]
key_files:
  created:
    - servers/jellyfin/inventory.md
  modified:
    - CLAUDE.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - servers/tower/README.md
    - servers/docker-tower/README.md
    - .planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-VALIDATION.md
decisions:
  - "D-17 HW transcode formally deferred until BIOS iGPU Multi-Monitor enable (tower BIOS); operator signed off CPU-only playback 2026-04-19 without buffering on 1080p + 4K"
  - "Ingress documented accurately as iptables DNAT on tower vmbr0 (not nginx :22098 as originally planned in 17.1-04)"
  - "Phase 15 progress reconciled in ROADMAP table (1/2 → 2/2) — 15-02 had shipped but table lagged STATE accumulated context"
metrics:
  duration: ~15min
  completed: 2026-04-19
---

# Phase 17.1 Plan 05: Verification + Docs + Cleanup Scheduling — Summary

Close Phase 17.1 (Jellyfin → CT 101 migration) by running final automated verification, capturing operator UAT signoff verbatim, updating all repo docs to reflect the new topology, and scheduling the soak-window cleanup todos.

## What shipped

- **Automated verification:** `bash scripts/verify-jellyfin-lxc.sh` → 18 PASS / 2 WARN / 0 FAIL. The 2 WARNs are the two iHD/Intel-driver checks, both soft per the deferred-acceptance contract in 17.1-02-SUMMARY.
- **External URL probe:** `curl -sI` from jellyfin host to `https://jellyfin.makscee.ru/` → `HTTP/1.1 302 Found` (Kestrel → /web/ redirect — expected).
- **Idempotence:** `ansible-playbook deploy-jellyfin.yml --check` → `PLAY RECAP ok=12 changed=1 failed=0`. The single `changed` is the `Jellyfin GPG key` `get_url` task, which flips in `--check` mode as a well-known false positive; real apply is idempotent per 17.1-02 logs.
- **Docs updated to new topology.** See *Deviations* below — ingress wording was corrected from nginx :22098 to iptables DNAT.
- **STATE.md** gained 4 new todos (2026-04-25, 2026-05-02, rollback drill, D-17 re-open hook).
- **VALIDATION.md** row `17.1-04-02` promoted to ✅ green (no-buffer 1080p + 4K); `17.1-04-01` (HW transcode) left `⬜ pending` with a Gap note.

## Task 2 UAT — operator answers (verbatim)

Per operator signoff 2026-04-19 (memory: `project_jellyfin_cpu_transcode_ok`; referenced `17.1-02-IGPU-PROBE.md`):

- 1080p no-buffer: **yes**
- 4K no-buffer: **yes** (CPU transcode — no buffering observed)
- Dashboard shows HW transcode: **N/A — deferred** (BIOS iGPU disabled; vainfo still reports nouveau. Not a failure — D-17 formally deferred to a later BIOS window.)
- Watch progress preserved: **not explicitly re-checked in this session** (covered by 17.1-03 cutover signoff)

No UAT was re-asked — user instruction was to treat prior 2026-04-19 operator signoff as authoritative.

## Commits

- `94e02bf` docs(17.1-05): update repo docs for jellyfin CT 101 topology
- `24a2be5` docs(17.1-05): schedule cleanup todos + reconcile state
- (this commit) docs(17.1-05): summary + final metadata

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Plan assumed vainfo would report iHD/Intel — it reports nouveau**
- **Found during:** Task 1
- **Issue:** The plan's acceptance criterion `vainfo shows iHD Intel driver` would force a red-fail, but D-17 was formally deferred on 2026-04-18 (per `17.1-02-IGPU-PROBE.md` and memory `project_jellyfin_cpu_transcode_ok`). BIOS iGPU Multi-Monitor is disabled; the scripts were already written to emit WARN (not FAIL) for this exact condition.
- **Fix:** Treated the two vainfo WARNs as acceptable per the deferred-acceptance contract. Verify script exit was 0 (PASSED with warnings). Did not halt the plan.
- **Files modified:** none (already-deferred behavior)
- **Commit:** n/a

**2. [Rule 1 - Doc accuracy] Ingress is iptables DNAT, not nginx :22098**
- **Found during:** Task 3
- **Issue:** Original plan action text and ROADMAP boundary statement said `tower:22098 → 10.10.20.11:8096 reverse proxy`, but 17.1-04 actually implemented iptables DNAT on tower vmbr0 + Tailscale `accept-routes=false` on docker-tower (no nginx layer). Also the external URL is unreachable from controller (no WAN/DNS path) — curl was run from jellyfin host instead.
- **Fix:** Rewrote the ingress description in ROADMAP Phase 17.1 goal, CLAUDE.md jellyfin row, servers/jellyfin/inventory.md "External" line, and ROADMAP plan-list entry for 17.1-04 to say `iptables DNAT on tower vmbr0 + Tailscale accept-routes=false`.
- **Files modified:** CLAUDE.md, .planning/ROADMAP.md, servers/jellyfin/inventory.md
- **Commit:** 94e02bf

**3. [Rule 3 - State reconciliation] STATE.md §Current Position pointed at stale Phase 15**
- **Found during:** Task 4
- **Issue:** User flagged STATE.md had Phase 15 listed as current position despite 17.1 being active. Phase 15 was itself already complete per STATE accumulated context (15-01 + 15-02 Playwright-verified, commit a039d43 deployed), but ROADMAP progress table still showed 1/2 In Progress.
- **Fix:** Updated STATE.md Current Position to Phase 17.1 complete (with prior-phase note for Phase 15). Updated ROADMAP progress table Phase 15 row to 2/2 Complete 2026-04-19. Did **not** roll back Phase 15 — it was genuinely shipped, the counter was just not advanced.
- **Files modified:** .planning/STATE.md, .planning/ROADMAP.md
- **Commit:** 24a2be5

**4. [Rule 1 - Verify gate rewrite] Plan Task 3 `<verify>` shell was invalid bash**
- **Found during:** Task 3
- **Issue:** The automated verify block mixed `git grep` with `grep -A2` piped through multiple inverted logic conditions — would not actually evaluate. I verified each acceptance_criteria bullet manually instead.
- **Fix:** Confirmed each criterion post-edit: jellyfin row exists in CLAUDE.md with `LXC 101`; docker-tower row no longer contains "Jellyfin" (replaced with explicit "moved to CT 101" note); ROADMAP Goal line is the full boundary statement; servers/tower/README.md has CT 101 row; servers/jellyfin/inventory.md exists; servers/docker-tower/README.md has the "Moved 2026-04-18" banner.

## Deferred Issues

- **D-17 (HW transcode) — BIOS gate:** tower BIOS iGPU Multi-Monitor must be enabled for /dev/dri/renderD128 to bind Intel i915 instead of nouveau. Tracked in STATE §Pending Todos ("Phase 17.1 D-17 re-open") and captured as Gap note in VALIDATION row 17.1-04-01. When operator does BIOS window, promote the 2 `check_warn` blocks at the bottom of `scripts/verify-jellyfin-lxc.sh` to hard `check`.

## Authentication gates

None.

## Self-Check: PASSED

- FOUND: servers/jellyfin/inventory.md
- FOUND: CLAUDE.md contains `| jellyfin | Moscow (LXC 101) |` row
- FOUND: .planning/ROADMAP.md Phase 17.1 Goal is boundary statement (no "TBD")
- FOUND: .planning/ROADMAP.md Phase 17.1 progress row 5/5 Complete
- FOUND: servers/tower/README.md contains `| 101 | jellyfin |` row
- FOUND: servers/docker-tower/README.md has "Moved 2026-04-18" banner
- FOUND: .planning/STATE.md §Pending Todos contains both 2026-04-25 + 2026-05-02 entries
- FOUND: 17.1-VALIDATION.md row 17.1-04-02 = ✅ green; row 17.1-04-01 = ⬜ pending + Gap note
- FOUND: commit 94e02bf (docs) and 24a2be5 (state) in git log
