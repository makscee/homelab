# 17.1-04 Ingress Swap Log

**Plan shape deviation:** Plan 04 was written assuming tower has an L7 proxy on `:22098` (nginx/caddy).
Probe proved there is no `:22098` listener at all; ingress is pure **iptables DNAT on vmbr0:8096**.
Operator chose **Option A — flip DNAT target, no nginx**. Artifact shape changed from
`servers/tower/ingress/jellyfin.conf` (nginx) to `servers/tower/ingress/jellyfin.iptables` (rule snippet)
plus the canonical persisted rule at `/etc/iptables/rules.v4`. WebSocket is application-layer; L4 DNAT
passes the Upgrade header through unchanged.

---

## ROLLBACK (paste-executable, restores pre-change state)

```bash
ssh root@tower 'iptables-restore < /root/pre-17.1-iptables.save && cp /root/pre-17.1-iptables-rules.v4.bak /etc/iptables/rules.v4 && iptables -t nat -L PREROUTING -n --line-numbers | grep 8096'
# Expected after rollback: dpt:8096 to:10.10.20.10:8096
```

Pre-change backups on tower:
- `/root/pre-17.1-iptables.save` (full `iptables-save` dump, 2886 bytes, 2026-04-19 11:37 MSK)
- `/root/pre-17.1-iptables-rules.v4.bak` (copy of `/etc/iptables/rules.v4`, same content)

---

## Pre-change state (captured 2026-04-19 11:37 MSK)

**Handler:** `iptables-DNAT` on `vmbr0:8096`. No nginx / caddy / haproxy on tower. No listener on `:22098`.

**Persistence mechanism:** `iptables-persistent` / `netfilter-persistent.service` (enabled).
Canonical file: `/etc/iptables/rules.v4`. Restored on boot via `netfilter-persistent.service`.

**Relevant NAT rules before change:**

```
Chain PREROUTING (policy ACCEPT)
num  target  prot  in      source     destination  options
1    DNAT    tcp   *       0/0        100.101.0.7  tcp dpt:2200  to:10.10.20.200:22
2    DNAT    tcp   *       0/0        100.101.0.7  tcp dpt:2204  to:10.10.20.204:22
3    DNAT    tcp   vmbr0   0/0        0/0          tcp dpt:8096  to:10.10.20.10:8096   <-- FLIP TARGET
4    DNAT    tcp   *       0/0        100.101.0.7  tcp dpt:59000 to:10.10.20.204:59000
```

**DNS/ingress path:** `jellyfin.makscee.ru -> 95.31.38.176 (home router) -> tower:8096 (vmbr0) ->
iptables PREROUTING DNAT -> 10.10.20.10:8096 (docker-tower LXC, now STOPPED) -> will become
10.10.20.11:8096 (CT 101 jellyfin)`.

---

## Applied change

_populated after apply_
