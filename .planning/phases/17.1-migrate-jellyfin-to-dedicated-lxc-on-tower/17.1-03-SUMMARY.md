---
phase: 17.1-migrate-jellyfin-to-dedicated-lxc-on-tower
plan: 03
subsystem: media/jellyfin
tags: [migration, cutover, rsync, sqlite, jellyfin]
requires:
  - 17.1-02 (CT 101 Jellyfin installed, vainfo green, /health 200)
provides:
  - migrated-jellyfin-state-on-ct101
  - rollback-block-for-soak-window
affects:
  - docker-tower LXC 100 (old jellyfin stopped, compose block retained)
  - jellyfin CT 101 (now serves migrated state)
tech-stack:
  added: [rsync 3.2.7 on CT 101, sqlite3 on CT 101]
  patterns: [rsync -aHAX PULL mode over SSH-Tailnet, surgical XML patch via sed]
key-files:
  created:
    - .planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-03-CUTOVER-LOG.md
  modified:
    - /etc/jellyfin/encoding.xml (on CT 101 — not in repo)
decisions:
  - Adapted baseline query MediaItems → BaseItems (Jellyfin 10.10+ unified jellyfin.db)
  - Inverted rsync to PULL mode (Tailscale ACL blocks docker-tower → jellyfin SSH)
  - Retained docker compose jellyfin block for 1-week soak (D-14)
metrics:
  duration: ~5min (active work; gate wait excluded)
  completed: 2026-04-19
requirements: [D-08, D-13, D-14, D-16, D-18]
---

# Phase 17.1 Plan 03: Jellyfin state cutover docker-tower → CT 101 Summary

Live Jellyfin state (2972 BaseItems, 7 Users, ~867MB) rsync-migrated from docker-tower LXC 100 to CT 101 native-deb install; service live with integrity preserved and 1-week rollback path documented.

## Tasks Completed

| Task | Name | Commit | Result |
|---|---|---|---|
| 1 | Maintenance-window gate | — | Operator confirmed `proceed with cutover` |
| 2 | Stop services + baseline capture | 2f56828 | docker=exited, systemd=inactive, BaseItems=2972, Users=7 |
| 3 | Rsync + chown + XML patch + start | 0ecdbf5 | All integrity checks passed |

## Integrity Verification

| Metric | Pre | Post | Match |
|---|---|---|---|
| BaseItems | 2972 | 2972 | ✓ |
| Users | 7 | 7 | ✓ |
| library.db owner | — | jellyfin:jellyfin | ✓ |
| /health | — | 200 | ✓ |
| systemctl is-active | inactive | active | ✓ |

All 7 original users migrated: Pierrot_le_fou, chickipibaroom, lululovesuu, makscee, mamonichka, moot9baka, snark_hunter. Plan 02 test admin wiped as expected.

## Deviations from Plan

### Auto-fixed Issues (Rule 3 - blocking)

1. **[Rule 3 — Schema] BaseItems instead of MediaItems**
   - Found during: Task 2 baseline query
   - Issue: Jellyfin 10.10+ uses unified `jellyfin.db`; `library.db` is 0-byte placeholder; `MediaItems` table does not exist
   - Fix: Adapted baseline query to `BaseItems` table in `jellyfin.db`
   - Files: CUTOVER-LOG.md
   - Commit: 2f56828

2. **[Rule 3 — Network ACL] Inverted rsync to PULL mode**
   - Found during: Task 3 SSH setup (docker-tower → jellyfin)
   - Issue: Tailscale SSH policy blocks docker-tower → jellyfin (`tailnet policy does not permit`)
   - Fix: jellyfin → docker-tower SSH works; ran all 5 rsyncs as PULL from CT 101. Same SSH-over-Tailnet encryption (T-17.1-12 mitigation preserved)
   - Files: CUTOVER-LOG.md
   - Commit: 0ecdbf5

3. **[Rule 3 — Missing deps] rsync + sqlite3 absent on CT 101**
   - Fix: `apt-get install -y rsync sqlite3` on CT 101
   - Commit: 0ecdbf5

4. **[Rule 3 — XML surgery] TranscodingTempPath tag absent (not wrong value)**
   - Fix: Injected `<TranscodingTempPath>/var/cache/jellyfin/transcodes</TranscodingTempPath>` after `</VaapiDevice>` in encoding.xml. Created `/var/cache/jellyfin/transcodes` with jellyfin ownership.
   - Commit: 0ecdbf5

## Automation-First Notes

- Full migration executed autonomously; operator gate was maintenance-window confirmation only.
- Full login smoke-test with real user password deferred to Plan 04 post-ingress flip (can't probe with operator password).

## Rollback Readiness

Rollback block in CUTOVER-LOG.md. Hot-standby window: 2026-04-19 → 2026-04-25 (D-14/D-15). Old compose block retained and verified unchanged (`grep -c 'jellyfin:'` = 3, same as pre-cutover).

## Plan 04 Readiness

- Jellyfin serving migrated state at `http://jellyfin:8096` (Tailnet)
- Old docker-tower Jellyfin stopped but not removed
- Ingress flip (tower:22098 proxy → CT 101) is Plan 04's responsibility
- No blockers

## Self-Check: PASSED

- FOUND: .planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-03-CUTOVER-LOG.md
- FOUND: commit 2f56828 (Task 2)
- FOUND: commit 0ecdbf5 (Task 3)
