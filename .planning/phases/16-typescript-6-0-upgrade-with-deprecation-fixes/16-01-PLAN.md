---
phase: 16-typescript-6-0-upgrade-with-deprecation-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/package.json
  - apps/admin/bun.lock
autonomous: false
requirements:
  - D-1
  - D-2
  - D-3
  - D-4
  - D-5
user_setup: []

must_haves:
  truths:
    - "apps/admin typechecks clean on TypeScript 6.0.3 (bun run typecheck exits 0)"
    - "apps/admin production build succeeds on TypeScript 6.0.3 (bun run build exits 0)"
    - "bun run lint exits 0 with no new deprecation warnings on TS 6"
    - "typescript-eslint 8.58.2 continues to operate against TS 6 without peer-range conflicts"
    - "Deployed admin instance on mcow renders /, /audit, /alerts without console errors on production build"
    - "No sibling dep bumps shipped (typescript-eslint stays 8.58.2, @types/node stays ^22) unless typecheck forced them"
    - "tsconfig.json unchanged unless TS 6 surfaced a hard error requiring a minimal delta"
  artifacts:
    - path: "apps/admin/package.json"
      provides: "typescript dep bumped to ^6.0.3"
      contains: "\"typescript\": \"^6"
    - path: "apps/admin/bun.lock"
      provides: "Lockfile resolved against TS 6.0.3"
      contains: "typescript"
  key_links:
    - from: "apps/admin/package.json"
      to: "typescript 6.0.3 in node_modules"
      via: "bun install resolution"
      pattern: "\"typescript\": \"\\^6"
    - from: "apps/admin tsc (node_modules/.bin/tsc)"
      to: "apps/admin/tsconfig.json"
      via: "bun run typecheck script"
      pattern: "tsc --noEmit"
---

<objective>
Upgrade `apps/admin` from TypeScript `^5.6.0` to `^6.0.3` per 16-CONTEXT.md + 16-RESEARCH.md. Research confirms baseline `bun x tsc --noEmit` is clean on TS 5.6, the tsconfig uses no TS-6-deprecated options, and `typescript-eslint@8.58.2` already declares `typescript: ">=4.8.4 <6.1.0"` — so this is a single mechanical bump with verification.

Purpose: Keep the frontend stack current, unblock Phase 17 (ESLint 10 + @types/node 24), pay zero migration tax. Scope is strictly upgrade-only per D-5 — no refactors, no sibling dep bumps unless typecheck forces them.

Output: apps/admin running on TS 6.0.3, typecheck + build + lint all green, deployed to mcow, Playwright smoke clean on the three live pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-typescript-6-0-upgrade-with-deprecation-fixes/16-CONTEXT.md
@.planning/phases/16-typescript-6-0-upgrade-with-deprecation-fixes/16-RESEARCH.md

@apps/admin/package.json
@apps/admin/tsconfig.json
@ansible/playbooks/deploy-homelab-admin.yml

<interfaces>
<!-- TS 6 escape hatch — only use if a legitimate deprecation surfaces on an option we cannot trivially replace. -->
<!-- Per 16-RESEARCH.md: tsconfig already targets ES2017 + module=esnext + moduleResolution=Bundler, so no deprecated options are in use. -->

```jsonc
// tsconfig.json escape hatch (DO NOT add reflexively — only if TS5101/TS5107 surfaces on an option we cannot replace)
{
  "compilerOptions": {
    "ignoreDeprecations": "6.0"
  }
}
```

<!-- Expected TS-6-relevant tsconfig options per 16-RESEARCH.md compat audit: -->
<!--   target: ES2017    (safe; ES5 deprecated, ES2017 is not) -->
<!--   module: esnext    (safe; None/AMD deprecated, esnext is not) -->
<!--   moduleResolution: Bundler (safe; classic deprecated, Bundler is not) -->
<!--   downlevelIteration: not set (safe) -->
<!--   outFile: not set (safe) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump typescript to ^6.0.3, install, typecheck, build, lint</name>
  <files>apps/admin/package.json, apps/admin/bun.lock</files>
  <action>
Per D-1 (fix-all-that-surface) and D-5 (upgrade-only). Research (16-RESEARCH.md) confirmed baseline typecheck is clean and no deprecated tsconfig options are in use, so this is expected to be a single-edit bump.

1. **Edit `apps/admin/package.json`** — change `"typescript": "^5.6.0"` to `"typescript": "^6.0.3"` in `devDependencies`. Do NOT touch `typescript-eslint` (stays `^8.58.2` per 16-RESEARCH.md — peer range `>=4.8.4 <6.1.0` covers TS 6.0.x; D-2 becomes a no-op). Do NOT touch `@types/node` (stays `^22` per D-4; Phase 17 owns the bump to 24).

2. **Install:**
   ```bash
   cd apps/admin && bun install
   ```
   Expect: lockfile updates, `node_modules/typescript/package.json` resolves to `6.0.3`. Verify with `bun x tsc --version` → `Version 6.0.3` (or latest ^6.0.3 patch).

3. **Typecheck:**
   ```bash
   cd apps/admin && bun run typecheck
   ```
   MUST exit 0. If errors surface:
   - Apply D-1 mechanical fixes on the reported lines only (narrow casts, explicit types) — do NOT refactor surrounding code.
   - If a deprecated tsconfig option is flagged (TS5101/TS5107) on an option we cannot trivially replace, add `"ignoreDeprecations": "6.0"` to `tsconfig.json` `compilerOptions` (minimal-delta per D-3) and document the reason in the commit message.
   - Do NOT silence errors with `@ts-ignore`; do NOT bump sibling deps unless an error explicitly demands it (record which dep + why in commit).

4. **Build:**
   ```bash
   cd apps/admin && bun run build
   ```
   MUST exit 0. Watch for Next.js plugin warnings about TS version — Next 15.5 treats TS as a peer, so unlikely to block, but log any warnings to surface in the summary.

5. **Lint:**
   ```bash
   cd apps/admin && bun run lint
   ```
   MUST exit 0. typescript-eslint 8.58.2 should operate cleanly on TS 6.0.x. If new deprecation warnings surface on source files (not config warnings about options), apply mechanical fixes on the reported lines only per D-1. If warnings are in generated files (e.g. `.next/types/*`), ignore — not in scope.

6. **Commit atomically:**
   ```
   chore(admin): upgrade typescript 5.6 → 6.0.3 (16-01)
   ```
   One commit covers package.json + bun.lock + any forced tsconfig/source fixes. Do NOT split into multiple commits unless a sibling dep bump was truly forced (then commit the forced bump separately with rationale).
  </action>
  <verify>
    <automated>cd apps/admin &amp;&amp; grep -q '"typescript": "\^6' package.json &amp;&amp; bun x tsc --version | grep -q 'Version 6' &amp;&amp; bun run typecheck &amp;&amp; bun run build &amp;&amp; bun run lint</automated>
  </verify>
  <done>package.json has `"typescript": "^6.0.3"`; `bun x tsc --version` reports 6.x; typecheck, build, and lint all exit 0; no sibling dep bumps beyond what typecheck forced; tsconfig unchanged (or minimally delta'd per D-3 with rationale); atomic commit on main.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to mcow via Ansible</name>
  <files>(no repo files modified — deploy only)</files>
  <action>
Deploy the TS-6-built admin bundle to mcow using the existing playbook:

```bash
ansible-playbook -i ansible/inventory.yml ansible/playbooks/deploy-homelab-admin.yml
```

Expect: playbook rsyncs `apps/admin/` build artifacts to mcow, restarts `homelab-admin.service`. Watch for:
- Build-on-target steps if the playbook rebuilds remotely — confirm remote `bun install` picks up TS 6.0.3
- systemd restart success: `ssh root@mcow 'systemctl status homelab-admin'` → active (running)
- Journal clean: `ssh root@mcow 'journalctl -u homelab-admin -n 50 --no-pager'` → no TS/runtime errors on startup

If the playbook fails, surface the error — do NOT attempt manual remote fixes outside the playbook (per repo convention: Ansible is the deployment source of truth).
  </action>
  <verify>
    <automated>ssh root@mcow 'systemctl is-active homelab-admin'</automated>
  </verify>
  <done>Playbook run completes without errors; `homelab-admin.service` is active on mcow; journal shows no new runtime errors post-restart.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playwright smoke — /, /audit, /alerts render without console errors</name>
  <what-built>
apps/admin now runs on TypeScript 6.0.3 in production, deployed to mcow. TS is a compile-time-only change — runtime behavior should be identical, but the smoke check catches any edge case where TS 6's lib `.d.ts` reshape changed runtime-adjacent code paths (narrowing in SWR/Recharts/Radix call sites that affected emit).
  </what-built>
  <how-to-verify>
Claude drives Playwright MCP (per user memory: `Test UI with Playwright`) against `https://homelab.makscee.ru` over Tailnet from the operator machine.

**Pages to verify (Claude runs via Playwright, captures console logs):**
1. `/` (Overview) — host tiles, AlertsCard, ClaudeSummary render; no console errors
2. `/audit` — audit table renders with rows; pagination works; no console errors
3. `/alerts` — alerts list renders (may be empty); no console errors

**Checks per page:**
- `page.on('console')` captures ZERO `error`-level messages (warnings acceptable if pre-existing)
- `page.on('pageerror')` captures ZERO uncaught exceptions
- Visible render: at least one data-bearing element per page (host tile, audit row, alert row or empty-state message)

**Red flags (block approval):**
- Any console `error` that didn't exist in Phase 15-01 smoke baseline
- Digest errors (Next.js runtime exception hashes) on any page
- Blank page / hydration mismatch

If any red flag appears, Claude checks the journal (`ssh root@mcow 'journalctl -u homelab-admin -n 100 --no-pager'`), diagnoses, and applies the minimum in-scope fix (typically a narrowing cast on the failing call site per D-1). Re-deploy via Task 2 playbook and re-verify.

User only needs to weigh in if Claude's Playwright output is ambiguous or if aesthetic regression is suspected (not expected — TS-only change).
  </how-to-verify>
  <resume-signal>Type "approved" if Playwright smoke is clean on all three pages. Describe the failing page + error if regression visible.</resume-signal>
  <files>(verification only — no file modifications unless red-flag fix required in apps/admin source)</files>
  <action>Run Playwright MCP against /, /audit, /alerts on homelab.makscee.ru over Tailnet. Capture console + pageerror streams. Report pass/fail per page. On red flag: diagnose via mcow journal, apply minimum D-1 fix, re-deploy via Task 2, re-verify. Request approval once all three pages are clean.</action>
  <verify><automated>MISSING — Playwright MCP smoke is the automated substitute for manual browser check; no pure-CLI replacement for rendered-page console capture</automated></verify>
  <done>User types "approved"; Playwright smoke reports zero console errors and zero pageerrors on /, /audit, /alerts; any red-flag regression resolved by minimum in-scope fix committed atomically on top of Task 1 commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Phase 16 is a compile-time toolchain bump; no new request/response paths, no new trust boundaries, no runtime behavior change intended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | typescript 6.0.3 npm package | accept | typescript is a first-party Microsoft package; bun install verifies integrity against registry hashes in bun.lock |
| T-16-02 | Denial of Service | production build pipeline | mitigate | `bun run build` must pass in Task 1 before deploy; broken build is caught pre-merge; deploy playbook fails fast on remote build errors |
| T-16-03 | Denial of Service | homelab-admin.service on mcow | mitigate | Playwright smoke (Task 3) confirms runtime render on /, /audit, /alerts before approval; journal checked post-deploy |
</threat_model>

<verification>
- `grep -q '"typescript": "\^6' apps/admin/package.json`
- `cd apps/admin && bun x tsc --version` reports `Version 6.x`
- `cd apps/admin && bun run typecheck` exits 0
- `cd apps/admin && bun run build` exits 0
- `cd apps/admin && bun run lint` exits 0
- `ssh root@mcow 'systemctl is-active homelab-admin'` returns `active`
- Playwright smoke on /, /audit, /alerts: zero console errors, zero pageerrors
- No sibling dep bumps committed unless typecheck forced them (rationale in commit message)
- tsconfig.json unchanged, or minimally delta'd per D-3 with commit rationale
</verification>

<success_criteria>
- TypeScript 5.6 → 6.0.3 upgrade complete per D-1 (fix-all-that-surface), D-2 (typescript-eslint no-op confirmed), D-3 (tsconfig minimal delta — likely zero change), D-4 (@types/node stays ^22), D-5 (upgrade-only, no refactors)
- Production build passes on TS 6
- Deployed instance on mcow renders three live pages without regression (Playwright-verified)
- No new runtime errors in homelab-admin journal
- Atomic commit on main; Phase 17 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/16-typescript-6-0-upgrade-with-deprecation-fixes/16-01-SUMMARY.md`
</output>
