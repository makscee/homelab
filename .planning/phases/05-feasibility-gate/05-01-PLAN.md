---
phase: 05-feasibility-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/nether/tailscale/README.md
  - servers/nether/tailscale/app-connector-domains.md
autonomous: false
requirements: [MON-02]
user_setup:
  - service: tailscale
    why: "Extend existing nether App Connector to advertise api.anthropic.com — admin-console action cannot be done by Claude"
    dashboard_config:
      - task: "Add api.anthropic.com to existing App Connector domain list on nether"
        location: "Tailscale admin console → DNS → App Connectors → nether-app-connector → Domains"
    env_vars: []

must_haves:
  truths:
    - "Operator can `curl api.anthropic.com` from mcow successfully — nether App Connector advertises api.anthropic.com and mcow routes through it"
  artifacts:
    - path: "servers/nether/tailscale/app-connector-domains.md"
      provides: "Documented list of domains advertised by the nether App Connector (Telegram CIDRs + api.anthropic.com)"
      contains: "api.anthropic.com"
  key_links:
    - from: "mcow (100.101.0.9)"
      to: "api.anthropic.com"
      via: "nether App Connector (tag:connector, MagicDNS)"
      pattern: "curl -m 8 -sSIL https://api.anthropic.com/"
---

<objective>
Extend the existing nether Tailscale App Connector (already advertising Telegram CIDRs) to also advertise `api.anthropic.com`. Prove from mcow that `curl https://api.anthropic.com/` completes a TLS handshake via the App Connector — not via direct Moscow ISP egress. Document the final domain list in repo so the configuration is reproducible.

Purpose: MON-02 requires mcow to reach api.anthropic.com. Direct egress from Moscow is assumed hostile (H-01 PITFALLS, v1.0 Telegram replay). This plan pre-wires egress before any soak is attempted.

Output: `servers/nether/tailscale/app-connector-domains.md` documenting the current advertised-domains state, plus verified `tailscale status` + curl output from mcow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-feasibility-gate/05-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- Existing App Connector state (from project memory `project_mcow_egress_lesson.md` + PROJECT.md Key Decisions). -->
<!-- nether runs Tailscale with `tag:connector`. ACL `nodeAttrs` grants `"attr:app-connector"`. -->
<!-- Currently advertises: 149.154.166.110/32, 149.154.167.99/32 (Telegram IPv4 CIDRs). -->
<!-- mcow has `--accept-routes` set; MagicDNS is enabled Tailnet-wide. -->
<!-- Adding a DOMAIN (not a CIDR) uses Tailscale App Connector's domain-advertisement feature: admin console → DNS → App Connectors → add `api.anthropic.com`. -->
<!-- mcow's MagicDNS will resolve api.anthropic.com to a Tailscale-internal 100.x address that routes through nether. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-flight baseline — capture current connector state</name>
  <read_first>
    - CLAUDE.md (mcow/nether Tailscale IPs)
    - .planning/phases/05-feasibility-gate/05-CONTEXT.md §Decisions D-05-13 (extend existing connector, don't create new one)
    - .planning/research/ARCHITECTURE.md §Egress Path Decision (confirms App Connector is preferred path)
  </read_first>
  <action>
    Before touching anything, capture baseline evidence that (a) the existing Telegram connector path still works and (b) api.anthropic.com is currently BLOCKED from mcow. This baseline goes into the GATE artifact later.

    Run from operator workstation:
    ```
    ssh root@nether  'tailscale status | grep -E "connector|exit" ; echo --- ; tailscale serve status 2>/dev/null || true'
    ssh root@mcow    'tailscale status | head -20 ; echo --- ; curl -4 -m 8 -sSI https://149.154.167.99/ -k -o /dev/null -w "telegram-ipv4=%{http_code}\n" ; curl -4 -m 8 -sSI https://api.anthropic.com/ -o /dev/null -w "anthropic-baseline=%{http_code}\n" || echo "anthropic-baseline=BLOCKED"'
    ```

    Save the full output into `.planning/phases/05-feasibility-gate/evidence/baseline-pre-connector.txt` (create the `evidence/` directory).

    Document any surprise: if Telegram 149.154.167.99 already returns non-zero, flag it. If anthropic baseline unexpectedly returns 200 already, STOP and flag — the whole feasibility question shifts.
  </action>
  <verify>
    <automated>test -s /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/baseline-pre-connector.txt &amp;&amp; grep -qE "telegram-ipv4=|anthropic-baseline=" /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/baseline-pre-connector.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-feasibility-gate/evidence/baseline-pre-connector.txt` exists and is non-empty.
    - File contains the strings `telegram-ipv4=` and `anthropic-baseline=`.
    - File contains at least one line from `tailscale status` for both nether and mcow.
  </acceptance_criteria>
  <done>Baseline state recorded so post-change comparison is meaningful. Existing Telegram path confirmed intact.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator adds api.anthropic.com to the existing nether App Connector</name>
  <read_first>
    - Project memory `project_mcow_egress_lesson.md` (App Connector recipe already proven in v1.0)
    - .planning/phases/05-feasibility-gate/05-CONTEXT.md §Decisions D-05-13 (extend existing connector, don't create new)
    - .planning/research/ARCHITECTURE.md §Egress Path Decision (App Connector preferred over HTTPS_PROXY)
  </read_first>
  <action>
    Claude cannot operate the Tailscale admin console. Operator follows numbered steps:

    1. Open https://login.tailscale.com/admin/dns → section "App connectors".
    2. Click the existing app connector (the one with `tag:connector` advertising Telegram CIDRs on `nether`) → "Edit".
    3. In the "Domains" list, add a new entry: `api.anthropic.com` (domain, not CIDR — App Connector resolves and routes automatically).
    4. Do NOT remove any existing Telegram CIDRs or domains.
    5. Save. Wait ~30s for propagation.
    6. On nether, confirm connector sees the new domain:
       ```
       ssh root@nether 'journalctl -u tailscaled --since "2 minutes ago" | grep -i "app.*connect\|api.anthropic" | tail -20'
       ```
    7. Commit a record of what you did — Claude writes `servers/nether/tailscale/app-connector-domains.md` with the current advertised-domains list (see Task 3). Paste any error messages from the admin console into that file if present.

    If the admin console shows an error on save (e.g. "domain must be resolvable", "conflicts with existing subnet route"), STOP and record it in the evidence directory — feasibility path may need re-planning.
  </action>
  <verify>
    <automated>ssh root@nether 'journalctl -u tailscaled --since "10 minutes ago" | grep -ciE "api\.anthropic\.com|appc"' 2>/dev/null | awk '$1 &gt; 0 {print "OK"; exit 0} {print "NO-LOG-EVIDENCE"; exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Operator confirms in chat: "added api.anthropic.com to App Connector, save succeeded".
    - `ssh root@nether 'journalctl -u tailscaled --since "10 minutes ago"'` contains at least one line referencing `api.anthropic.com` OR an `appc` (app-connector) event after the save timestamp.
    - No Tailscale admin-console error screenshot captured in `evidence/`.
  </acceptance_criteria>
  <resume-signal>Type "connector-updated" (or "connector-error: &lt;reason&gt;" to abort)</resume-signal>
  <done>api.anthropic.com is advertised through the existing nether App Connector without disturbing Telegram routes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify egress from mcow + document the state in repo</name>
  <read_first>
    - servers/nether/tailscale/README.md (if exists — otherwise create)
    - .planning/phases/05-feasibility-gate/05-CONTEXT.md §Claude's Discretion
  </read_first>
  <action>
    Verify the new route is live from mcow and document the connector's domain inventory in-repo so it is reproducible.

    1. From operator workstation, run:
       ```
       ssh root@mcow 'tailscale status | head -20 ; echo --- ; tailscale whois api.anthropic.com 2>&amp;1 || dig +short api.anthropic.com @100.100.100.100 ; echo --- ; curl -4 -m 8 -sSIL https://api.anthropic.com/ -o /dev/null -w "anthropic-via-connector=%{http_code} remote_ip=%{remote_ip}\n" ; curl -4 -m 8 -sSIL https://api.anthropic.com/api/oauth/usage -o /dev/null -w "usage-endpoint-no-auth=%{http_code}\n"'
       ```
       Expected: `anthropic-via-connector=400|401|403|200` (NOT 000/timeout). `usage-endpoint-no-auth=401` (endpoint exists, rejects unauthenticated). `remote_ip` should be a 100.x Tailscale address, NOT a public 160.x/162.x IP.

    2. Append the full stdout/stderr to `.planning/phases/05-feasibility-gate/evidence/post-connector-smoke.txt`.

    3. Create `servers/nether/tailscale/app-connector-domains.md` with EXACT contents:
       ```markdown
       # nether App Connector — Advertised Domains &amp; CIDRs

       **Last updated:** 2026-04-16 (Phase 05 feasibility gate)
       **Tailscale host:** nether (100.101.0.3), tag:connector, attr:app-connector

       ## CIDRs (explicit IPv4 routes)

       | CIDR | Purpose | Added |
       |------|---------|-------|
       | 149.154.166.110/32 | Telegram Bot API (IPv4 egress for voidnet bot) | v1.0 |
       | 149.154.167.99/32  | Telegram Bot API (IPv4 egress for voidnet bot) | v1.0 |

       ## Domains (App Connector auto-resolved)

       | Domain | Purpose | Added |
       |--------|---------|-------|
       | api.anthropic.com | Claude Code `/api/oauth/usage` feasibility soak (Phase 05) — may become long-lived if v2.0 milestone passes the gate | 2026-04-16 |

       ## How to modify

       1. Tailscale admin console → DNS → App Connectors → edit existing connector on nether.
       2. Add/remove domain or CIDR. Save.
       3. Update this file in the same commit.
       4. Verify from a consuming host: `curl -m 8 -sSIL https://&lt;domain&gt;/ -o /dev/null -w "%{http_code} %{remote_ip}\n"`. `remote_ip` MUST be a 100.x Tailscale IP.

       ## Verification evidence

       - Pre-change baseline: `.planning/phases/05-feasibility-gate/evidence/baseline-pre-connector.txt`
       - Post-change smoke: `.planning/phases/05-feasibility-gate/evidence/post-connector-smoke.txt`
       ```

    4. Create or append to `servers/nether/tailscale/README.md` a one-line pointer to `app-connector-domains.md` under a "Routing" section.

    5. If `remote_ip` in step 1 is a PUBLIC IP (not 100.x), the App Connector is NOT intercepting the request — STOP, record in evidence/, and flag in Task 4 checkpoint. Do not advance to Wave 2.
  </action>
  <verify>
    <automated>test -f /Users/admin/hub/workspace/homelab/servers/nether/tailscale/app-connector-domains.md &amp;&amp; grep -q "api.anthropic.com" /Users/admin/hub/workspace/homelab/servers/nether/tailscale/app-connector-domains.md &amp;&amp; grep -qE "anthropic-via-connector=(200|400|401|403)" /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/post-connector-smoke.txt</automated>
  </verify>
  <acceptance_criteria>
    - `servers/nether/tailscale/app-connector-domains.md` exists and contains `api.anthropic.com` under "Domains".
    - `.planning/phases/05-feasibility-gate/evidence/post-connector-smoke.txt` exists, contains `anthropic-via-connector=` with a 2xx/4xx status (NOT `000`/timeout/`BLOCKED`), and contains `usage-endpoint-no-auth=401` (or `403`).
    - `remote_ip` recorded in smoke output is a `100.` Tailscale address.
    - `servers/nether/tailscale/README.md` exists and references `app-connector-domains.md`.
  </acceptance_criteria>
  <done>MON-02 egress path proven: mcow TCP-reaches api.anthropic.com via the nether App Connector; repo documents the state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator sign-off on egress path</name>
  <what-built>
    - api.anthropic.com added to existing nether App Connector (admin-console action by operator).
    - mcow now TCP-reaches api.anthropic.com via Tailscale 100.x address (proven by curl `remote_ip`).
    - `servers/nether/tailscale/app-connector-domains.md` + `README.md` commit this config shape into repo.
    - Baseline + post-smoke evidence saved under `.planning/phases/05-feasibility-gate/evidence/`.
  </what-built>
  <how-to-verify>
    1. `cat .planning/phases/05-feasibility-gate/evidence/post-connector-smoke.txt` — confirm `anthropic-via-connector=` is 4xx or 200 AND `remote_ip=100.x.x.x`.
    2. `cat .planning/phases/05-feasibility-gate/evidence/baseline-pre-connector.txt` — confirm Telegram path was healthy before the change.
    3. `cat servers/nether/tailscale/app-connector-domains.md` — confirm domain list is complete + accurate.
    4. Spot-check Telegram egress still works (no regression): `ssh root@mcow 'curl -4 -m 8 -sSI https://149.154.167.99/ -k -o /dev/null -w "%{http_code}\n"'` — should match baseline.
  </how-to-verify>
  <action>Operator reads each of the four verification artifacts listed in how-to-verify, confirms values match expectations, and types the resume-signal. No automation runs in this task -- it is a pure gate confirming Plan 01 Tasks 1-3 produced correct, consistent evidence before Wave 2 spends mcow time on a 24h soak.</action>
  <resume-signal>Type "egress-verified" OR "egress-broken: &lt;reason&gt;" (latter halts Wave 2).</resume-signal>
  <done>Operator confirms (a) api.anthropic.com reachable via connector, (b) Telegram path unregressed, (c) repo accurately documents the change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcow → nether → api.anthropic.com | mcow egresses through the nether Tailscale node; nether acts as exit for anthropic traffic |
| Operator ↔ Tailscale admin console | Admin-console change is unauthenticated-to-Claude; human performs it manually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | T (Tampering) | Tailscale ACL / App Connector config | mitigate | Task 1 baselines the pre-change state; Task 3 diffs post-change; any unexpected domain/CIDR change is caught before Wave 2. Repo file `app-connector-domains.md` is single source of truth — drift is grep-detectable. |
| T-05-01-02 | I (Information disclosure) | api.anthropic.com traffic via nether | accept | nether is Tailnet-internal; no third-party sees the traffic. TLS is end-to-end from mcow's perspective (connector forwards TCP, not TLS-terminates). Low residual risk; documented in ADR D-07 (Plan 05). |
| T-05-01-03 | D (DoS) | nether node / connector | mitigate | App Connector adds 1 domain to an already-in-service box; Plan 03 uses 300s polling cadence so throughput added is negligible (~1 req / 5min). No capacity concern. |
| T-05-01-04 | E (Elevation of privilege) | Tailscale admin console session | accept | Operator-controlled; outside Claude's automation surface. Documented as `checkpoint:human-action`. |

**No high-severity threats.** Plan is not blocked.
</threat_model>

<verification>
- `servers/nether/tailscale/app-connector-domains.md` committed and contains `api.anthropic.com`.
- `.planning/phases/05-feasibility-gate/evidence/post-connector-smoke.txt` shows mcow TCP-reaches api.anthropic.com via `remote_ip=100.*`.
- Baseline file confirms Telegram path healthy pre-change; post-change spot-check shows no regression.
</verification>

<success_criteria>
MON-02 satisfied: mcow's DNS resolves api.anthropic.com to a Tailscale 100.x address, TCP connect succeeds, TLS handshake completes, HTTPS GET to /api/oauth/usage without auth returns 401 (endpoint present, auth required).
</success_criteria>

<output>
After completion, create `.planning/phases/05-feasibility-gate/05-01-SUMMARY.md` capturing: baseline state, admin-console change description, post-change smoke evidence, any surprises, go/no-go signal for Wave 2.
</output>
