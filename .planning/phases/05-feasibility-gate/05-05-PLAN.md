---
phase: 05-feasibility-gate
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - .planning/phases/05-feasibility-gate/GATE-PASSED.md
  - .planning/phases/05-feasibility-gate/GATE-FAILED.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - servers/mcow/soak/teardown.sh
autonomous: false
requirements: [DEPLOY-03, MON-02]

must_haves:
  truths:
    - "A single verdict file (GATE-PASSED.md XOR GATE-FAILED.md) exists in the phase directory with soak summary + evidence references"
    - "ADR D-07 (Claude Code quota access strategy) lives in PROJECT.md Key Decisions table with ToS reasoning + operator sign-off"
    - "STATE.md reflects the verdict: status=planning (v2.0 continues to Phase 06) OR status=blocked (milestone halted)"
    - "Soak spike fully removed from mcow: no claude-usage-soak.* units, no /etc/claude-usage-soak/env, no /root/.claude-usage-token"
  artifacts:
    - path: ".planning/phases/05-feasibility-gate/GATE-PASSED.md OR GATE-FAILED.md"
      provides: "Feasibility verdict with evidence links + reviewable soak summary"
      contains: "## Soak Summary"
    - path: ".planning/PROJECT.md"
      provides: "ADR D-07 row in Key Decisions table"
      contains: "D-07 | Claude Code quota access strategy"
    - path: "servers/mcow/soak/teardown.sh"
      provides: "Idempotent teardown script that removes the spike from mcow"
      contains: "systemctl disable --now claude-usage-soak.timer"
  key_links:
    - from: ".planning/phases/05-feasibility-gate/GATE-PASSED.md"
      to: ".planning/PROJECT.md (D-07 row)"
      via: "explicit anchor link from verdict to ADR"
      pattern: "PROJECT.md#key-decisions"
    - from: "teardown.sh"
      to: "mcow state"
      via: "systemctl disable + rm of token/unit files"
      pattern: "rm -f /etc/claude-usage-soak/env"
---

<objective>
Decide the feasibility verdict based on Plan 04's evidence, write the verdict file, write ADR D-07 into PROJECT.md, update STATE.md, and tear down the spike from mcow. This is the last plan in Phase 05 -- the phase transitions to Phase 06 on PASS or to `status: blocked` on FAIL.

Decision rules (from CONTEXT.md + ROADMAP.md):

- **PASS** (write `GATE-PASSED.md`) iff ALL of:
  - rows >= 200
  - http_429_ratio < 0.05
  - schema_match = true for >= 3 distinct hash samples AND all 4 required fields present in each
  - No persistent 401/403 halt event (auth counter never reached 3)
  - Plan 01 post-connector smoke confirmed `remote_ip=100.*`
- **FAIL** (write `GATE-FAILED.md`) iff ANY:
  - egress-blocked: Plan 01 smoke failed OR timer halted on network errors
  - persistent-401/403: auth counter reached 3 (halt fired)
  - schema-mismatch: fewer than 3 samples contain all required fields (soft-halt but for gate purposes, still FAIL with rework recommendation)
  - http_429_ratio >= 0.05 (soft-halt -> re-tune cadence + re-soak; still FAIL for this gate cycle)

D-05-07 classifies schema-mismatch and 429>=5% as "rework signals, not milestone kills." The verdict file MUST distinguish between **HARD FAIL** (milestone blocked, re-scope needed) and **SOFT FAIL** (rework + retry this phase).

Output: one verdict file, ADR row, STATE.md bump, teardown.sh committed AND executed on mcow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-feasibility-gate/05-01-PLAN.md
@.planning/phases/05-feasibility-gate/05-03-PLAN.md
@.planning/phases/05-feasibility-gate/05-04-PLAN.md
@.planning/phases/05-feasibility-gate/evidence/soak-analysis.txt
@.planning/phases/05-feasibility-gate/evidence/soak-mcow-state.txt
@.planning/phases/05-feasibility-gate/evidence/soak-t0.txt
@.planning/phases/05-feasibility-gate/evidence/post-connector-smoke.txt
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create teardown.sh (commit to repo; not yet executed)</name>
  <files>servers/mcow/soak/teardown.sh</files>
  <read_first>
    - servers/mcow/soak/install.sh (mirror of what needs reversing)
    - .planning/phases/05-feasibility-gate/05-CONTEXT.md §D-05-10 (disposability)
  </read_first>
  <action>
    Create `servers/mcow/soak/teardown.sh` (mode 0755). EXACT contents:

    ```bash
    #!/usr/bin/env bash
    # teardown.sh --- remove the Phase 05 feasibility soak from mcow.
    # Idempotent: safe to rerun. Archives /var/log/claude-usage-soak.jsonl if it exists
    # and has not yet been pulled to the evidence directory.
    set -euo pipefail
    MCOW=${MCOW_HOST:-root@mcow}
    HERE="$(cd "$(dirname "$0")" && pwd)"
    EVIDENCE_DIR="${HERE}/../../../.planning/phases/05-feasibility-gate/evidence"
    mkdir -p "$EVIDENCE_DIR"

    echo "==> Stopping and disabling timer + service"
    ssh "$MCOW" 'systemctl disable --now claude-usage-soak.timer 2>/dev/null || true; systemctl stop claude-usage-soak.service 2>/dev/null || true'

    echo "==> Archiving JSONL (if present and larger than existing archive)"
    REMOTE_SIZE=$(ssh "$MCOW" 'stat -c %s /var/log/claude-usage-soak.jsonl 2>/dev/null || echo 0')
    LOCAL_ARCHIVE="$EVIDENCE_DIR/soak-jsonl.archive"
    LOCAL_SIZE=0; [[ -f "$LOCAL_ARCHIVE" ]] && LOCAL_SIZE=$(wc -c < "$LOCAL_ARCHIVE")
    if [[ "$REMOTE_SIZE" -gt "$LOCAL_SIZE" ]]; then
      echo "  remote=$REMOTE_SIZE local=$LOCAL_SIZE -- pulling updated archive"
      scp -q "$MCOW":/var/log/claude-usage-soak.jsonl "$LOCAL_ARCHIVE"
    else
      echo "  remote=$REMOTE_SIZE local=$LOCAL_SIZE -- keeping existing archive"
    fi

    echo "==> Removing unit files, binaries, state, and token"
    ssh "$MCOW" 'set -e
      rm -f /etc/systemd/system/claude-usage-soak.service /etc/systemd/system/claude-usage-soak.timer
      rm -rf /usr/local/sbin/claude-usage-soak /etc/claude-usage-soak /var/lib/claude-usage-soak
      rm -f /root/.claude-usage-token
      rm -f /var/log/claude-usage-soak.jsonl
      systemctl daemon-reload
      systemctl reset-failed claude-usage-soak.service 2>/dev/null || true'

    echo "==> Verifying"
    ssh "$MCOW" 'systemctl list-unit-files | grep -c claude-usage-soak || true'
    # Expect 0.
    ssh "$MCOW" 'ls /etc/claude-usage-soak /var/lib/claude-usage-soak /root/.claude-usage-token 2>&1 | grep -v "No such" || true'
    echo "==> Teardown complete."
    ```

    Do NOT execute yet. Task 2/3 write the verdict first.
  </action>
  <verify>
    <automated>test -x /Users/admin/hub/workspace/homelab/servers/mcow/soak/teardown.sh && bash -n /Users/admin/hub/workspace/homelab/servers/mcow/soak/teardown.sh && grep -q 'systemctl disable --now claude-usage-soak.timer' /Users/admin/hub/workspace/homelab/servers/mcow/soak/teardown.sh && grep -q 'rm -f /etc/systemd/system/claude-usage-soak' /Users/admin/hub/workspace/homelab/servers/mcow/soak/teardown.sh && grep -q 'rm -f /root/.claude-usage-token' /Users/admin/hub/workspace/homelab/servers/mcow/soak/teardown.sh && grep -q 'rm -rf /usr/local/sbin/claude-usage-soak' /Users/admin/hub/workspace/homelab/servers/mcow/soak/teardown.sh</automated>
  </verify>
  <acceptance_criteria>
    - `teardown.sh` exists, executable, `bash -n` passes.
    - Grep-verifiable: `systemctl disable --now claude-usage-soak.timer`, `rm -f /etc/systemd/system/claude-usage-soak.service`, `rm -f /etc/systemd/system/claude-usage-soak.timer`, `rm -rf /usr/local/sbin/claude-usage-soak`, `rm -rf /etc/claude-usage-soak`, `rm -rf /var/lib/claude-usage-soak`, `rm -f /root/.claude-usage-token`.
    - Script is idempotent: second run after first produces no non-zero exits (every `rm` + `systemctl` uses `|| true` or `-f`).
  </acceptance_criteria>
  <done>teardown.sh ready; not yet executed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Classify verdict + write GATE-PASSED.md OR GATE-FAILED.md</name>
  <read_first>
    - .planning/phases/05-feasibility-gate/evidence/soak-analysis.txt
    - .planning/phases/05-feasibility-gate/evidence/soak-mcow-state.txt
    - .planning/phases/05-feasibility-gate/evidence/post-connector-smoke.txt
    - .planning/phases/05-feasibility-gate/05-CONTEXT.md §D-05-06, D-05-07, D-05-08, D-05-09
    - .planning/ROADMAP.md §Phase 05 Success Criteria
  </read_first>
  <decision>
    Based on Plan 04 evidence, classify the outcome into one of:
    - **PASS** (all success criteria met) -> write `GATE-PASSED.md`.
    - **HARD FAIL -- egress-blocked OR persistent-401/403** -> write `GATE-FAILED.md` with `verdict: hard-fail`, flip STATE.md to `blocked`.
    - **SOFT FAIL -- schema-mismatch OR 429>=5%** -> write `GATE-FAILED.md` with `verdict: soft-fail`, flip STATE.md to `planning` (rework), recommend next action (re-tune cadence / re-scope research).
  </decision>
  <context>
    This is the go/no-go moment for the v2.0 milestone. PASS unlocks Phase 06. HARD FAIL kills the endpoint-scrape approach (D-05-01) with no fallback (local-log was explicitly ruled out). SOFT FAIL recommends staying in Phase 05 for a second soak cycle with tuned parameters.

    Operator must read `evidence/soak-analysis.txt` headline numbers and compare against the thresholds in the objective block above.
  </context>
  <options>
    <option id="pass">
      <name>PASS (all 4 ROADMAP criteria satisfied)</name>
      <pros>v2.0 unblocked; ADR D-07 locks in approach; Phase 06 ready.</pros>
      <cons>Accepts A-01 ToS residual risk in exchange for utility.</cons>
    </option>
    <option id="hard-fail">
      <name>HARD FAIL (egress or persistent auth rejection)</name>
      <pros>Clean milestone halt; saves downstream build cost; surfaces Moscow/ToS block loudly.</pros>
      <cons>v2.0 re-scope required; no fallback path available per D-05-01.</cons>
    </option>
    <option id="soft-fail">
      <name>SOFT FAIL (schema drift OR 429 >= 5%)</name>
      <pros>Actionable rework signal; re-soak with tuned cadence likely converges.</pros>
      <cons>Adds another 24h soak cycle; delays Phase 06 by at least 1 day.</cons>
    </option>
  </options>
  <action>
    Based on decision, write ONE of the following files. Use the exact templates below. Values in `{{...}}` are taken from `evidence/soak-analysis.txt`.

    ### If PASS: `.planning/phases/05-feasibility-gate/GATE-PASSED.md`

    ```markdown
    # Phase 05 Feasibility Gate -- PASSED

    **Verdict:** PASS
    **Decided:** {{ISO8601 now}}
    **Operator sign-off:** {{name/email}}

    ## Summary

    All four ROADMAP §Phase 05 success criteria were satisfied over a 24h soak on mcow. Phase 06 (Exporter Skeleton) is unblocked. ADR D-07 (below) records the ToS interpretation and the locked-in endpoint-scrape approach.

    ## Soak Summary

    | Metric | Value | Criterion | Pass? |
    |--------|-------|-----------|-------|
    | Duration | {{T0 -> T+24h01m or similar}} | >= 24h | yes |
    | Total ticks | {{rows}} | >= 200 | yes |
    | HTTP 200 | {{http_200}} ({{%}}) | -- | -- |
    | HTTP 429 ratio | {{http_429_ratio}} | < 0.05 | yes |
    | HTTP 401/403 | {{count}} (max consecutive: {{n}}) | < 3 consecutive | yes |
    | Network errors | {{count}} | -- | -- |
    | Distinct schema hashes | {{distinct}} | >= 1 per consistent schema | yes |
    | Schema-match rate | {{pct}} | >= 3 full-schema samples | yes |
    | Latency p50 / p95 / p99 | {{p50}} / {{p95}} / {{p99}} ms | -- | informational |

    ## Evidence Pack

    - Pre-change baseline: `evidence/baseline-pre-connector.txt`
    - Post-connector smoke: `evidence/post-connector-smoke.txt`
    - Install transcript: `evidence/install-on-mcow.txt`
    - t0 timestamp: `evidence/soak-t0.txt`
    - mcow state at T+24h: `evidence/soak-mcow-state.txt`
    - Raw JSONL archive ({{rows}} rows): `evidence/soak-jsonl.archive`
    - Analyzer output: `evidence/soak-analysis.txt`

    ## Three Sample Responses

    {{paste three full 200-OK bodies from soak-analysis.txt sample section, ts + body each}}

    ## ADR Reference

    See `.planning/PROJECT.md` §Key Decisions row `D-07 | Claude Code quota access strategy`.

    ## Next Action

    Proceed to Phase 06 planning: `/gsd-plan-phase 06`.
    ```

    ### If HARD FAIL or SOFT FAIL: `.planning/phases/05-feasibility-gate/GATE-FAILED.md`

    ```markdown
    # Phase 05 Feasibility Gate -- FAILED

    **Verdict:** {{HARD FAIL | SOFT FAIL}}
    **Reason code:** {{egress-blocked | persistent-401-403 | schema-mismatch | rate-limit-exceeded}}
    **Decided:** {{ISO8601 now}}
    **Operator sign-off:** {{name/email}}

    ## Summary

    {{1-2 sentences on what broke.}}

    ## Soak Summary

    {{same table as PASS template, with the failing row highlighted}}

    ## Failure Evidence

    - Specific line(s) from `evidence/soak-jsonl.archive`: {{ts, http_status, retry_after, request_id}}
    - Specific journal line(s) from `evidence/soak-mcow-state.txt`: {{paste}}
    - If Plan 01 baseline was wrong: quote from `evidence/baseline-pre-connector.txt`.

    ## Next Action

    {{for hard-fail:}} v2.0 milestone blocked. Re-scope: either re-evaluate local-log path (reverses D-05-01), or abandon milestone. Update ROADMAP.md accordingly.

    {{for soft-fail:}} Stay in Phase 05. Recommended re-tune:
    - `rate-limit-exceeded` -> increase `OnUnitActiveSec` to 600s and `RandomizedDelaySec` to 120s; re-arm timer; re-soak 24h.
    - `schema-mismatch` -> re-enter research phase; `/api/oauth/usage` response shape drifted from STACK.md. Do not proceed to Phase 06 without updated schema.

    ## ADR Reference

    See `.planning/PROJECT.md` §Key Decisions row `D-07 | Claude Code quota access strategy` (records the approach attempted + failure disposition).
    ```

    Substitute ALL `{{...}}` placeholders with actual values from the evidence files before committing.
  </action>
  <verify>
    <automated>{ test -f /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/GATE-PASSED.md || test -f /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/GATE-FAILED.md; } && VERDICT_FILE=$(ls /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/GATE-*.md 2>/dev/null | head -1) && grep -q '## Soak Summary' "$VERDICT_FILE" && grep -q '## Evidence Pack\|## Failure Evidence' "$VERDICT_FILE" && ! grep -q '{{' "$VERDICT_FILE"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE of `GATE-PASSED.md` / `GATE-FAILED.md` exists in `.planning/phases/05-feasibility-gate/`.
    - Verdict file contains `## Soak Summary` with filled-in numbers (no `{{...}}` placeholders remain).
    - Verdict file contains an `## Evidence Pack` or `## Failure Evidence` section referencing the files under `evidence/`.
    - For PASS: table row `HTTP 429 ratio | < 0.05 | yes`.
    - For FAIL: `**Verdict:**` line reads `HARD FAIL` or `SOFT FAIL` AND `**Reason code:**` line present.
  </acceptance_criteria>
  <resume-signal>Type "verdict: pass", "verdict: hard-fail: &lt;reason&gt;", or "verdict: soft-fail: &lt;reason&gt;".</resume-signal>
  <done>Verdict file exists with full evidence references and no unresolved placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Write ADR D-07 into PROJECT.md Key Decisions + update STATE.md</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/PROJECT.md §Key Decisions (table at line ~95)
    - .planning/STATE.md (status + decisions sections)
    - .planning/phases/05-feasibility-gate/GATE-PASSED.md OR GATE-FAILED.md (verdict written in Task 2)
    - .planning/research/PITFALLS.md §A-01 (ToS language to quote)
  </read_first>
  <action>
    **Step A -- Append D-07 row to PROJECT.md Key Decisions table.**

    Locate the table at PROJECT.md §Key Decisions. Append a new row. EXACT text template (fill fields from verdict):

    ```
    | D-07: Claude Code quota access strategy | v2.0 milestone requires per-token weekly/5h quota data; Anthropic publishes no supported API; OAuth endpoint `/api/oauth/usage` is the only reverse-engineered path. User operates their own tokens on their own homelab, reads only own-quota data, runs poll at 300s jitter (soft floor per Anthropic community rate-limit evidence), halts on persistent 401/403. Residual risks accepted: A-01 ToS ambiguity (Feb 2026 auth-policy restricts OAuth tokens to Claude Code + Claude.ai; homelab monitor is policy-gray not policy-red per PITFALLS.md), A-02 token TTL via 1-year `claude setup-token` in Phase 08, A-04 undocumented endpoint drift via defensive parsing in Phase 06. | {{one of: Validated (Phase 05, 24h soak PASS {{date}}) | Rejected (Phase 05 FAIL, {{reason}}, milestone blocked) | Rejected (Phase 05 SOFT FAIL, {{reason}}, re-soak pending)}} |
    ```

    Insert the row in the Key Decisions table after the existing final row (`Token file perms ...`). Preserve existing table structure.

    Then append under the table a short ADR block:

    ```markdown
    ### ADR D-07 detail (2026-04-16, Phase 05 feasibility gate)

    **Context.** v2.0 milestone ("Claude Code Usage Monitor") requires per-token weekly + 5-hour session utilization data for 2-5 OAuth tokens. No official API exposes this. `/api/oauth/usage` is the endpoint Claude Code itself consumes; community reverse-engineering has documented the schema (`five_hour.utilization`, `seven_day.utilization`, `seven_day_opus.utilization`, `resets_at`) and stability since April 2025.

    **Decision.** Proceed with endpoint-scrape from mcow via nether App Connector. Cadence 300s +/-60s jitter, one poll per enabled token, exponential back-off on 429, halt-on-persistent-401/403.

    **ToS reasoning.** Anthropic's February 2026 policy update (The Register 2026-02-20) restricts OAuth tokens to "Claude Code and Claude.ai" usage. A homelab monitor of one's own tokens is arguably within-spirit (observability of own-account state; tokens are still primarily consumed by Claude Code on the same hosts; data never leaves operator's Tailnet). Residual enforcement risk accepted: Anthropic may silently revoke tokens detected as non-CLI clients. Mitigation: fingerprint-minimal (UA `claude-usage-soak/0.*`, not Claude Code's UA; no request fingerprints beyond plain curl); halt-on-401/403 caps blast radius to one revocation per token.

    **Alternatives rejected.**
    - Local-log tail (ccusage-style) -- explicitly ruled out by D-05-01 user constraint: "find a way to get same data with only a auth token."
    - Claude Console Admin API -- research phase resolved this is only for API-key-auth customers, not Max subscribers.
    - No monitoring -- unacceptable; v1.0 operating model explicitly requires observability.

    **Sign-off conditions that flip this decision.**
    - Anthropic publishes an explicit prohibition on `/api/oauth/usage` use by non-CLI clients, OR
    - Anthropic silently revokes a homelab-operated token with evidence of non-CLI detection as the trigger, OR
    - Official quota API (per GitHub issues #19880, #32796) ships; migrate to it.

    **Evidence.**
    - `.planning/phases/05-feasibility-gate/GATE-PASSED.md` (or `GATE-FAILED.md`) with 24h soak summary.
    - `.planning/research/STACK.md` §Critical: Anthropic Usage API Endpoint.
    - `.planning/research/PITFALLS.md` §A-01, A-03, H-01.

    **Operator sign-off:** {{same signatory as GATE-PASSED.md}}, {{ISO8601 date}}.
    ```

    **Step B -- Update STATE.md.**

    Open `.planning/STATE.md`. Modify frontmatter:
    - If PASS: `status: planning`, `stopped_at: Phase 05 PASSED -- ready for Phase 06 planning`, `last_activity: "{{date}} -- Phase 05 feasibility gate PASSED; ADR D-07 recorded"`.
    - If HARD FAIL: `status: blocked`, `stopped_at: Phase 05 HARD FAIL ({{reason}}) -- milestone blocked`, `last_activity: same`.
    - If SOFT FAIL: `status: planning`, `stopped_at: Phase 05 SOFT FAIL ({{reason}}) -- rework + re-soak`, `last_activity: same`.

    In the body, under `## Accumulated Context` > `### Decisions`, REPLACE the `**v2.0 pending ADR:** D-07 (Phase 05 output)...` bullet with:
    - If PASS: `**v2.0 validated ADRs:** D-07 (Claude Code quota access strategy) -- endpoint-scrape approach validated by 24h mcow soak, see `.planning/phases/05-feasibility-gate/GATE-PASSED.md`.`
    - If FAIL: `**v2.0 rejected ADRs:** D-07 (Claude Code quota access strategy) -- {{reason}}, see `.planning/phases/05-feasibility-gate/GATE-FAILED.md`.`

    Update progress if applicable (completed_phases increments on PASS).
  </action>
  <verify>
    <automated>grep -q 'D-07' /Users/admin/hub/workspace/homelab/.planning/PROJECT.md && grep -q 'Claude Code quota access strategy' /Users/admin/hub/workspace/homelab/.planning/PROJECT.md && grep -q 'ADR D-07 detail' /Users/admin/hub/workspace/homelab/.planning/PROJECT.md && ! grep -q 'v2.0 pending ADR' /Users/admin/hub/workspace/homelab/.planning/STATE.md && grep -qE 'Phase 05 (PASSED|HARD FAIL|SOFT FAIL)' /Users/admin/hub/workspace/homelab/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `PROJECT.md` contains a row `| D-07: Claude Code quota access strategy |` in the Key Decisions table.
    - `PROJECT.md` contains a section `### ADR D-07 detail` with Context / Decision / ToS reasoning / Alternatives rejected / Sign-off conditions / Evidence / Operator sign-off.
    - `STATE.md` status field reflects verdict (`planning` on PASS/SOFT-FAIL, `blocked` on HARD-FAIL).
    - `STATE.md` no longer contains `v2.0 pending ADR` text.
    - `STATE.md` `stopped_at` contains one of: `Phase 05 PASSED`, `Phase 05 HARD FAIL`, `Phase 05 SOFT FAIL`.
  </acceptance_criteria>
  <done>ADR D-07 is in PROJECT.md; STATE.md reflects verdict; project is in a consistent committed state.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Execute teardown.sh on mcow + verify + archive final evidence</name>
  <read_first>
    - servers/mcow/soak/teardown.sh
    - .planning/phases/05-feasibility-gate/GATE-*.md (the verdict file)
  </read_first>
  <action>
    Regardless of PASS or FAIL, the spike must be torn down (D-05-10 disposability). The token-in-EnvironmentFile exposure ends at this step.

    Operator runs from workstation:

    ```
    bash servers/mcow/soak/teardown.sh | tee .planning/phases/05-feasibility-gate/evidence/teardown-on-mcow.txt
    ```

    Then verify:

    ```
    ssh root@mcow 'systemctl list-unit-files | grep -c claude-usage-soak'
    # expect: 0

    ssh root@mcow 'ls /etc/claude-usage-soak /var/lib/claude-usage-soak /root/.claude-usage-token /var/log/claude-usage-soak.jsonl /usr/local/sbin/claude-usage-soak 2>&1'
    # expect: every path "No such file or directory"

    ssh root@mcow 'journalctl -u claude-usage-soak.service -n 5 --no-pager || echo "unit unknown"'
    # acceptable: "unit unknown" or empty (unit removed, daemon-reloaded)
    ```

    Append verification output to `evidence/teardown-on-mcow.txt` under `## Verification`.

    If any path still exists on mcow, STOP and re-run teardown.sh (it is idempotent). If it repeatedly fails to clean up, halt and document in evidence -- operator manual cleanup needed.

    Leave the nether App Connector `api.anthropic.com` domain IN PLACE:
    - On PASS: Phase 06 will consume it.
    - On HARD FAIL: it is harmless; remove during milestone-wrap or leave for future re-attempt (operator discretion; document in GATE-FAILED.md "Next Action").
    - On SOFT FAIL: re-soak will reuse it.
  </action>
  <verify>
    <automated>test -s /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/teardown-on-mcow.txt && grep -q 'Teardown complete' /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/teardown-on-mcow.txt && grep -q 'Verification' /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/teardown-on-mcow.txt</automated>
  </verify>
  <acceptance_criteria>
    - `evidence/teardown-on-mcow.txt` exists and contains `Teardown complete` + a `## Verification` section.
    - `ssh root@mcow 'systemctl list-unit-files | grep -c claude-usage-soak'` returns `0`.
    - None of `/etc/claude-usage-soak`, `/var/lib/claude-usage-soak`, `/root/.claude-usage-token`, `/var/log/claude-usage-soak.jsonl`, `/usr/local/sbin/claude-usage-soak` exist on mcow.
    - nether App Connector still advertises `api.anthropic.com` (disposition documented in verdict file's Next Action).
  </acceptance_criteria>
  <resume-signal>Type "teardown-clean" OR "teardown-residue: &lt;path&gt;" (latter requires manual cleanup before phase exit).</resume-signal>
  <done>Spike fully removed from mcow; token file gone; verdict file, ADR D-07, and STATE.md committed; Phase 05 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Token file `/etc/claude-usage-soak/env` | Must be deleted by teardown -- residual token on disk violates D-05-10 disposability |
| JSONL archive in git | Low-sensitivity content (utilization floats + reset timestamps); must not include raw token (enforced in Plan 04 Task 1 step 3) |
| ADR D-07 text | Contains operator's ToS reasoning + residual-risk acceptance; audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | I (Information disclosure) | Token file lingering on mcow | mitigate | Task 4 `teardown.sh` explicitly `rm -f /root/.claude-usage-token` AND `rm -rf /etc/claude-usage-soak`. Verification step greps `ls ... 2>&1` for "No such file or directory". Acceptance criteria block phase exit if any token-related path remains. |
| T-05-05-02 | R (Repudiation) | Decision audit trail for ToS-risk acceptance | mitigate | ADR D-07 in PROJECT.md includes explicit `Operator sign-off` line with name + ISO8601 date. Verdict file also includes operator sign-off. Git commit history provides immutable record. |
| T-05-05-03 | T (Tampering) | Verdict file template with unsubstituted placeholders | mitigate | Task 2 `<automated>` check greps for `{{` and fails if any remain. Prevents committing a template-shaped verdict with missing values. |
| T-05-05-04 | D | Orphaned nether App Connector entry | accept | On HARD FAIL, the `api.anthropic.com` domain advertisement is harmless -- costs nothing, exposes nothing to anyone off Tailnet. Operator discretion in Next Action whether to remove. Low residual; accepted. |
| T-05-05-05 | I | ADR D-07 leak of soak-evidence file content | accept | Evidence files live in private homelab repo; contents are operator's own account data. No external distribution surface. Documented in ADR D-07 "Residual risks accepted". |

**No high-severity threats** once teardown verifies clean. Plan NOT blocked.
</threat_model>

<verification>
- Verdict file exists (PASSED XOR FAILED), no `{{...}}` placeholders, contains `## Soak Summary`.
- `PROJECT.md` Key Decisions table has `D-07 | Claude Code quota access strategy` row AND `### ADR D-07 detail` section.
- `STATE.md` status bumped; `stopped_at` reflects verdict.
- `servers/mcow/soak/teardown.sh` exists, committed, and executed (evidence file documents the run).
- mcow has zero residue: no units, no directories, no token file, no JSONL log.
- nether App Connector domain entry remains (disposition documented).
</verification>

<success_criteria>
Phase 05 final state (all must be true):

1. `.planning/phases/05-feasibility-gate/GATE-PASSED.md` OR `.planning/phases/05-feasibility-gate/GATE-FAILED.md` exists with complete soak summary + evidence links (DEPLOY-03 criterion 4).
2. `.planning/PROJECT.md` Key Decisions table contains row `D-07 | Claude Code quota access strategy |` with outcome + ADR detail block (DEPLOY-03 criterion 4).
3. `.planning/STATE.md` status matches verdict; `v2.0 pending ADR` text is gone.
4. `servers/mcow/soak/teardown.sh` committed to repo AND executed on mcow with verified clean state.
5. Evidence directory complete: baseline, post-connector, install, t0, mcow-state, JSONL archive, analysis, teardown.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feasibility-gate/05-05-SUMMARY.md`: verdict, ADR D-07 row quote, STATE.md before/after, teardown transcript summary, next-phase pointer (`/gsd-plan-phase 06` on PASS, `/gsd-plan-phase 05 --gaps` on SOFT FAIL, milestone-halt note on HARD FAIL).
</output>
