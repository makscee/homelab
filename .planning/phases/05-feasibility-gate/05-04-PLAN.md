---
phase: 05-feasibility-gate
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - .planning/phases/05-feasibility-gate/evidence/soak-analysis.txt
  - .planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive
autonomous: false
requirements: [DEPLOY-03]

must_haves:
  truths:
    - ">=24h elapsed since Plan 03 armed the timer (t0 recorded in evidence/soak-t0.txt)"
    - "JSONL log contains >=240 rows (24h at 300s = 288 nominal; >=240 = ~20% tolerance)"
    - "Analyzer report quantifies 429 rate, latency percentiles, distinct schema hashes"
  artifacts:
    - path: ".planning/phases/05-feasibility-gate/evidence/soak-analysis.txt"
      provides: "Post-soak analyzer output -- counts, 429 rate, latency p50/p95/p99, distinct schema hashes"
      contains: "http_429_ratio"
    - path: ".planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive"
      provides: "Verbatim copy of /var/log/claude-usage-soak.jsonl at T+24h"
      min_lines: 240
  key_links:
    - from: "mcow:/var/log/claude-usage-soak.jsonl"
      to: ".planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive"
      via: "scp pull, verbatim, no filter"
      pattern: "scp.*claude-usage-soak.jsonl"
    - from: "evidence/soak-jsonl.archive"
      to: "evidence/soak-analysis.txt"
      via: "bash servers/mcow/soak/analyze.sh archive analysis.txt"
      pattern: "analyze.sh"
---

<objective>
Wait 24h from the t0 recorded in Plan 03, then pull the JSONL log off mcow, run `analyze.sh`, and archive both raw JSONL + analysis into the phase evidence directory. This plan does NOT decide pass/fail; that decision is Plan 05's job based on this evidence.

Purpose: Phase 05 success criterion #2 (soak pass) and #3 (schema consistency) can only be evaluated against the accumulated JSONL. This plan produces the self-contained evidence pack so Plan 05 can write the verdict without re-running anything.

Output:
- `.planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive` (verbatim copy)
- `.planning/phases/05-feasibility-gate/evidence/soak-analysis.txt` (analyzer output)
- `.planning/phases/05-feasibility-gate/evidence/soak-mcow-state.txt` (timer + journal snapshot)

This plan is operator-paced: `autonomous: false`. Pre-condition: at least 24h elapsed since t0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-feasibility-gate/05-CONTEXT.md
@.planning/phases/05-feasibility-gate/05-03-PLAN.md
@servers/mcow/soak/analyze.sh
@.planning/phases/05-feasibility-gate/evidence/soak-t0.txt
@.planning/phases/05-feasibility-gate/evidence/install-on-mcow.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wait for T+24h then pull JSONL + run analyzer</name>
  <read_first>
    - .planning/phases/05-feasibility-gate/evidence/soak-t0.txt (the T+0 timestamp)
    - servers/mcow/soak/analyze.sh (input/output contract)
    - .planning/phases/05-feasibility-gate/05-03-PLAN.md §Verification (halt conditions to recognize)
  </read_first>
  <action>
    **Precondition**: At least 24h has elapsed since the timestamp in `evidence/soak-t0.txt`. Confirm:
    ```
    T0=$(cat .planning/phases/05-feasibility-gate/evidence/soak-t0.txt)
    NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ)
    python3 -c "import datetime as d; t0=d.datetime.fromisoformat('${T0}'.replace('Z','+00:00')); now=d.datetime.fromisoformat('${NOW}'.replace('Z','+00:00')); print((now-t0).total_seconds())"
    ```
    Must print `>= 86400`. If less, STOP and wait. If `soak-t0.txt` missing, halt -- Plan 03 Task 2 did not sign off.

    Then execute, in order:

    1. **Snapshot mcow state** (before anything else, in case timer halted itself):
       ```
       ssh root@mcow 'systemctl status claude-usage-soak.timer --no-pager; echo ---; systemctl status claude-usage-soak.service --no-pager; echo ---; journalctl -u claude-usage-soak.service --since "25 hours ago" --no-pager | tail -200; echo ---; wc -l /var/log/claude-usage-soak.jsonl; echo ---; cat /var/lib/claude-usage-soak/consecutive_fail /var/lib/claude-usage-soak/consecutive_authfail' \
         > .planning/phases/05-feasibility-gate/evidence/soak-mcow-state.txt
       ```

    2. **Pull JSONL verbatim** (do NOT filter on the wire):
       ```
       scp root@mcow:/var/log/claude-usage-soak.jsonl \
         .planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive
       ```

    3. **Sanity checks on the archive**:
       ```
       ROWS=$(wc -l < .planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive)
       echo "rows=$ROWS"   # expect >= 240 ideally, hard floor 200
       # Token-leak check:
       grep -cE 'sk-ant|Bearer' .planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive   # must be 0
       # Valid JSON per line:
       awk 'NF' .planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive | while IFS= read -r line; do echo "$line" | jq . >/dev/null || echo "BAD_LINE: $line"; done | head
       ```
       If `rows < 200` OR token-leak count > 0 OR any BAD_LINE output: STOP and escalate to Plan 05 as a data-integrity problem rather than a feasibility verdict.

    4. **Run analyzer locally against the archive**:
       ```
       bash servers/mcow/soak/analyze.sh \
         .planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive \
         .planning/phases/05-feasibility-gate/evidence/soak-analysis.txt
       ```
       This prints to stdout AND writes the analysis file.

    5. **Extract the four headline numbers** Plan 05 needs:
       ```
       grep -E '^(total|http_200|http_429|http_401_403|network_errors|http_429_ratio|distinct_hashes|p50|p95|p99)=' \
         .planning/phases/05-feasibility-gate/evidence/soak-analysis.txt
       ```
       Append the captured stdout as a final section `## Headline numbers` in `soak-analysis.txt`.

    6. Do NOT delete mcow state yet -- teardown happens in Plan 05 after the verdict is written.
  </action>
  <what-built>
    - `evidence/soak-mcow-state.txt` -- timer/service/journal snapshot at T+24h.
    - `evidence/soak-jsonl.archive` -- verbatim JSONL log copy.
    - `evidence/soak-analysis.txt` -- analyzer output with `total`, `http_200`, `http_429`, `http_401_403`, `network_errors`, `http_429_ratio`, `distinct_hashes`, latency percentiles, and a `## Headline numbers` summary.
  </what-built>
  <how-to-verify>
    1. `wc -l .planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive` -- expect 240..360 rows (24h at 240..288 nominal, +tolerance).
    2. `grep -cE 'sk-ant|Bearer' .planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive` -- must return 0.
    3. `grep -E '^(http_429_ratio|distinct_hashes|p95)=' .planning/phases/05-feasibility-gate/evidence/soak-analysis.txt` -- shows the key numbers.
    4. `cat .planning/phases/05-feasibility-gate/evidence/soak-mcow-state.txt` -- confirm timer is either still `active (waiting)` OR `inactive (dead)` due to documented halt (D-05-06). If `failed`, this is an unexpected state -- flag in resume-signal.
  </how-to-verify>
  <verify>
    <automated>test -s /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive && test -s /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/soak-analysis.txt && test -s /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/soak-mcow-state.txt && ROWS=$(wc -l < /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive) && [ "$ROWS" -ge 200 ] && [ "$(grep -cE 'sk-ant|Bearer' /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/soak-jsonl.archive)" = "0" ] && grep -q 'http_429_ratio=' /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/soak-analysis.txt && grep -q 'distinct_hashes=' /Users/admin/hub/workspace/homelab/.planning/phases/05-feasibility-gate/evidence/soak-analysis.txt</automated>
  </verify>
  <acceptance_criteria>
    - Precondition proven: `(now - t0) >= 86400` seconds.
    - `evidence/soak-jsonl.archive` exists with >=200 lines; each line parses as JSON.
    - `evidence/soak-jsonl.archive` contains zero matches for `sk-ant` or `Bearer`.
    - `evidence/soak-analysis.txt` contains `http_429_ratio=`, `distinct_hashes=`, `p50=`, `p95=`, and a `## Headline numbers` section.
    - `evidence/soak-mcow-state.txt` contains `systemctl status claude-usage-soak.timer` output.
  </acceptance_criteria>
  <resume-signal>Type "soak-complete" (include rows + 429_ratio + distinct_hashes) OR "soak-halted-early: &lt;reason&gt;" (Plan 05 must still write a verdict using partial data).</resume-signal>
  <done>Post-soak evidence pack is complete in the phase evidence directory. Analyzer numbers ready for Plan 05 verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcow -> operator workstation (scp pull) | JSONL archive traverses Tailnet |
| Archive file -> repo | Low-sensitivity content (utilization + resets_at + hashes); explicitly NOT the token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | I (Information disclosure) | JSONL archive committed to git | mitigate | Task 1 step 3 greps for `sk-ant|Bearer` before archive is accepted; zero matches required. Content that DOES land in git: response bodies (utilization floats + reset timestamps), hashes. These are operator's own account data, homelab-private repo -- documented in ADR D-07 as accepted residual. |
| T-05-04-02 | T (Tampering) | Analyzer output integrity | mitigate | `analyze.sh` is deterministic and version-controlled. Re-running it on the archive produces the same numbers. No hand-edited analysis values accepted by Plan 05. |
| T-05-04-03 | D | Timer auto-halt misread as feasibility pass | mitigate | Task 1 explicitly reads `/var/lib/claude-usage-soak/consecutive_fail` + `consecutive_authfail`. If either reached 3, timer is `inactive` -- distinct from "completed 24h cleanly." Plan 05 verdict logic handles both. |

**No high-severity threats.** Plan not blocked.
</threat_model>

<verification>
- `soak-jsonl.archive`, `soak-analysis.txt`, `soak-mcow-state.txt` all exist in evidence directory and pass grep-based acceptance checks.
- Timer state (still-running OR self-halted) is explicitly recorded -- not inferred.
</verification>

<success_criteria>
DEPLOY-03 evidence pack complete: Plan 05 can now write GATE-PASSED.md or GATE-FAILED.md based solely on files in `.planning/phases/05-feasibility-gate/evidence/` without needing mcow access.
</success_criteria>

<output>
After completion, create `.planning/phases/05-feasibility-gate/05-04-SUMMARY.md`: elapsed time, row count, 429 ratio, auth-fail count, distinct schema hashes, p50/p95 latency, timer final state, pointer to soak-analysis.txt.
</output>
