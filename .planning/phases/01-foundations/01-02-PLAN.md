---
phase: 01-foundations
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - servers/tower/inventory.md
  - servers/docker-tower/inventory.md
  - servers/tower-sat/inventory.md
  - servers/mcow/inventory.md
  - servers/nether/inventory.md
  - servers/cc-vk/inventory.md
autonomous: true
requirements: [INV-01]

must_haves:
  truths:
    - "Every one of the 6 servers has an inventory.md with hostname, Tailscale IP, role, hardware, services, storage, and access info"
    - "Inventory files are in servers/{hostname}/inventory.md (per D-07)"
    - "Each inventory contains all required fields from D-08"
  artifacts:
    - path: "servers/tower/inventory.md"
      provides: "Proxmox host inventory"
      contains: "100.101.0.7"
    - path: "servers/docker-tower/inventory.md"
      provides: "Media stack inventory"
      contains: "100.101.0.8"
    - path: "servers/tower-sat/inventory.md"
      provides: "Satellite services inventory"
      contains: "100.101.0.10"
    - path: "servers/mcow/inventory.md"
      provides: "VoidNet services inventory"
      contains: "100.101.0.9"
    - path: "servers/nether/inventory.md"
      provides: "VPN/proxy inventory"
      contains: "100.101.0.3"
    - path: "servers/cc-vk/inventory.md"
      provides: "Claude Code runner inventory"
      contains: "100.91.54.83"
  key_links:
    - from: "servers/docker-tower/inventory.md"
      to: "servers/docker-tower/running-containers.txt"
      via: "Service list derived from running containers"
      pattern: "Jellyfin.*8096"
    - from: "servers/mcow/inventory.md"
      to: "servers/mcow/.env.example"
      via: "Service config and cross-server deps"
      pattern: "voidnet"
---

<objective>
Create inventory.md documents for all 6 servers with hardware specs, roles, Tailscale IPs, hosted services, storage layout, and access info.

Purpose: INV-01 requires every server to be fully documented. These inventories become the single source of truth for what runs where and are prerequisites for the dependency map and network topology in Plan 03.
Output: 6 inventory.md files, one per server in servers/{hostname}/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.planning/phases/01-foundations/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Inventory template from RESEARCH.md D-08 required fields -->

```markdown
# {hostname}

| Field | Value |
|-------|-------|
| Hostname | {hostname} |
| Tailscale IP | {ip} |
| Role | {description} |
| Hardware | {CPU, RAM, disk} |
| Access | `ssh root@{hostname}` via Tailnet |

## Hosted Services

| Service | Port | Image/Binary | Notes |
|---------|------|-------------|-------|
| {name} | {port} | {image} | {notes} |

## Storage Layout

| Path | Purpose | Stateful Data |
|------|---------|--------------|
| {path} | {purpose} | {what lives here} |

## Notes

{anything non-obvious about this server}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create inventory.md for tower, docker-tower, and tower-sat (Proxmox cluster)</name>
  <files>servers/tower/inventory.md, servers/docker-tower/inventory.md, servers/tower-sat/inventory.md</files>
  <read_first>
    - CLAUDE.md (server table with Tailscale IPs, roles, hardware for tower)
    - servers/tower/lxc-100-docker-tower.conf (LXC config details)
    - servers/tower/lxc-101-tower-sat.conf (LXC config details)
    - servers/docker-tower/running-containers.txt (complete container list with ports)
    - servers/docker-tower/docker-compose.homestack.yml (service definitions)
    - servers/docker-tower/docker-compose.services.yml (additional services)
    - servers/docker-tower/docker-compose.monitoring.yml (monitoring services)
  </read_first>
  <action>
Create 3 inventory.md files following the template in the interfaces block above. Use D-08 required fields for all. Per D-09, add extra fields where relevant.

**servers/tower/inventory.md:**
- Hostname: tower
- Tailscale IP: 100.101.0.7
- Role: Proxmox host — runs all Moscow LXC containers
- Hardware: Intel i7-8700 (12 threads), 16GB RAM (get disk info via `ssh root@tower "df -h"` if reachable, otherwise note "disk info pending SSH query")
- Access: `ssh root@tower` via Tailnet
- Hosted Services: Proxmox VE (management interface). List LXC containers as services:
  - LXC 100 — docker-tower (100.101.0.8)
  - LXC 101 — tower-sat (100.101.0.10)
  - LXC 204 — cc-vk (100.91.54.83)
- Storage Layout: Extract mount points from LXC configs (mp0, mp1, etc.). Note Proxmox storage paths.
- Notes: This is the physical machine. LXC containers are the actual service hosts. Tower itself runs no application services directly.

**servers/docker-tower/inventory.md:**
- Hostname: docker-tower
- Tailscale IP: 100.101.0.8
- Role: Media stack — Jellyfin, Navidrome, *arr suite, downloads
- Hardware: LXC 100 on tower (get allocated resources from LXC config: `lxc.cgroup2.memory.max`, cores). Run `ssh root@docker-tower "nproc && free -h && df -h"` if reachable.
- Access: `ssh root@docker-tower` via Tailnet
- Hosted Services (from running-containers.txt, with ports):
  - Jellyfin :8096 (media streaming)
  - Navidrome :4533 (music streaming)
  - qBittorrent :8080 (downloads)
  - Radarr :7878 (movie automation)
  - Sonarr :8989 (TV automation)
  - Lidarr :8686 (music automation)
  - Prowlarr :9696 (indexer management)
  - Grafana :3000 (monitoring dashboards)
  - Prometheus (metrics collection)
  - node-exporter (host metrics)
  - FileBrowser :8081
  - slskd (Soulseek client)
  - FlareSolverr (captcha solving for *arr)
  - personal-page (personal website)
  - docker-tower-api (custom API)
  - Portainer Agent (container management agent)
- Storage Layout: Extract volume mounts from docker-compose files. Key data paths for media, downloads, configs.
- Notes: This is the most service-dense server. All media automation flows through here.

**servers/tower-sat/inventory.md:**
- Hostname: tower-sat
- Tailscale IP: 100.101.0.10
- Role: Satellite services (secondary LXC on tower)
- Hardware: LXC 101 on tower (get allocated resources from LXC config). Run `ssh root@tower-sat "nproc && free -h && df -h"` if reachable.
- Access: `ssh root@tower-sat` via Tailnet
- Hosted Services: Run `ssh root@tower-sat "docker ps 2>/dev/null; systemctl list-units --type=service --state=running 2>/dev/null | head -30"` to discover. If SSH fails, note "services pending SSH query".
- Storage Layout: Query via SSH or note as pending.
- Notes: Per research, this server may have minimal or no services currently. Document what is found; sparse inventory is acceptable per D-09.

For SSH queries: attempt each query. If SSH fails (timeout, connection refused), document what is known from existing files and mark unknown fields as "Pending SSH query" rather than guessing.
  </action>
  <verify>
    <automated>cd /Users/admin/hub/workspace/homelab && for h in tower docker-tower tower-sat; do test -f "servers/$h/inventory.md" && grep -q "Tailscale IP" "servers/$h/inventory.md" && grep -q "Hosted Services" "servers/$h/inventory.md" && echo "$h OK" || echo "$h FAIL"; done</automated>
  </verify>
  <acceptance_criteria>
    - `servers/tower/inventory.md` exists and contains "100.101.0.7", "Proxmox", "i7-8700", "LXC 100", "LXC 101"
    - `servers/docker-tower/inventory.md` exists and contains "100.101.0.8", "Jellyfin", "8096", "Navidrome", "4533", "Radarr", "Sonarr"
    - `servers/tower-sat/inventory.md` exists and contains "100.101.0.10", "LXC 101"
    - All three files have sections: "Hosted Services", "Storage Layout", "Notes"
    - All three files have the required field table (Hostname, Tailscale IP, Role, Hardware, Access)
  </acceptance_criteria>
  <done>Tower cluster (tower, docker-tower, tower-sat) fully inventoried with all D-08 required fields populated from existing repo files and SSH queries where possible.</done>
</task>

<task type="auto">
  <name>Task 2: Create inventory.md for mcow, nether, and cc-vk (standalone servers)</name>
  <files>servers/mcow/inventory.md, servers/nether/inventory.md, servers/cc-vk/inventory.md</files>
  <read_first>
    - CLAUDE.md (server table)
    - servers/mcow/.env.example (VoidNet config, cross-server deps, DB path)
    - servers/mcow/voidnet-api.service (systemd unit for API)
    - servers/mcow/voidnet-bot.service (systemd unit for bot)
    - servers/nether/Caddyfile (reverse proxy config)
    - servers/nether/docker-compose.void.yml (VoidNet compose on nether)
    - servers/nether/docker-compose.monitoring.yml (monitoring compose on nether)
  </read_first>
  <action>
Create 3 inventory.md files following the same template. Use D-08 required fields for all.

**servers/mcow/inventory.md:**
- Hostname: mcow
- Tailscale IP: 100.101.0.9
- Role: VoidNet bot, API, portal, SQLite database
- Hardware: Run `ssh root@mcow "lscpu | head -15 && free -h && df -h"` if reachable. Otherwise note "hardware pending SSH query".
- Access: `ssh root@mcow` via Tailnet
- Hosted Services (from systemd units and .env.example):
  - voidnet-api (VoidNet REST API)
  - voidnet-bot (Telegram bot)
  - voidnet-overseer (monitoring/management)
  - voidnet-satellite (satellite service)
  - voidnet-portal (web portal — check .env.example for port)
- Cross-server dependencies: Calls docker-tower services via DOCKER_TOWER_IP=100.101.0.8 (Jellyfin, Radarr, Sonarr API keys)
- Storage Layout:
  - `/opt/voidnet/voidnet.db` — SQLite database (CRITICAL stateful data)
  - Extract other paths from .env.example and systemd units
- Notes: This server has the most cross-server dependencies. VoidNet services depend on docker-tower being operational.

**servers/nether/inventory.md:**
- Hostname: nether
- Tailscale IP: 100.101.0.3
- Role: VPN entry/exit point (Netherlands), reverse proxy
- Hardware: Run `ssh root@nether "lscpu | head -15 && free -h && df -h"` if reachable. Otherwise note "VPS — hardware pending SSH query".
- Access: `ssh root@nether` via Tailnet
- Public IP: 77.239.110.57 (from .env.example NETHER_PUBLIC_IP)
- Hosted Services (from docker-compose files and Caddyfile):
  - AmneziaWG (VPN, port 46476/UDP)
  - Caddy (reverse proxy — extract upstream targets from Caddyfile)
  - VoidNet-related containers (from docker-compose.void.yml)
  - Monitoring containers (from docker-compose.monitoring.yml)
- Storage Layout: Extract volume mounts from docker-compose files.
- Notes: Only server outside Moscow. Netherlands location provides VPN exit point. AmneziaWG is the active VPN protocol (XRay/VLESS is deprecated/out of scope). SPOF for Netherlands VPN access.

**servers/cc-vk/inventory.md:**
- Hostname: cc-vk
- Tailscale IP: 100.91.54.83
- Role: Vibe Kanban host, Claude Code runner (operator machine)
- Hardware: LXC 204 on tower. Run `ssh root@cc-vk "nproc && free -h && df -h"` if reachable (or query locally since this IS the operator machine — try `nproc && free -h && df -h` directly).
- Access: `ssh root@cc-vk` via Tailnet (or local shell)
- Hosted Services:
  - Vibe Kanban (web app — check for running processes/containers)
  - Claude Code (AI operator — this is where plans are executed from)
  - SOPS + age (encryption tools for secrets management)
- Storage Layout: Check for significant data paths. Age key at `~/.config/sops/age/keys.txt`.
- Notes: This is the operator machine. Age private key lives here (per D-06). All Claude Code plan execution happens from this server.

Same SSH approach as Task 1: attempt queries, fall back to documenting known facts with "pending SSH query" markers for unknowns.
  </action>
  <verify>
    <automated>cd /Users/admin/hub/workspace/homelab && for h in mcow nether cc-vk; do test -f "servers/$h/inventory.md" && grep -q "Tailscale IP" "servers/$h/inventory.md" && grep -q "Hosted Services" "servers/$h/inventory.md" && echo "$h OK" || echo "$h FAIL"; done</automated>
  </verify>
  <acceptance_criteria>
    - `servers/mcow/inventory.md` exists and contains "100.101.0.9", "voidnet", "SQLite", "/opt/voidnet/voidnet.db"
    - `servers/nether/inventory.md` exists and contains "100.101.0.3", "AmneziaWG", "77.239.110.57", "Caddy"
    - `servers/cc-vk/inventory.md` exists and contains "100.91.54.83", "LXC 204", "Claude Code"
    - All three files have sections: "Hosted Services", "Storage Layout", "Notes"
    - All three files have the required field table (Hostname, Tailscale IP, Role, Hardware, Access)
  </acceptance_criteria>
  <done>All 6 servers inventoried. Every server has an inventory.md with D-08 required fields. Cross-server dependencies documented for mcow. VPN details documented for nether.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH queries | Executor runs SSH commands on remote servers to gather facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | inventory.md files | accept | Inventory contains IPs and service names but no secrets; repo is private; Tailscale IPs are internal-only |
| T-02-02 | Information Disclosure | SSH query outputs | accept | SSH queries gather only hardware specs and service lists; no secrets extracted; output goes into markdown docs |
</threat_model>

<verification>
1. All 6 `servers/{hostname}/inventory.md` files exist
2. Each contains: Hostname, Tailscale IP, Role, Hardware, Access fields
3. Each contains: Hosted Services table, Storage Layout table, Notes section
4. docker-tower inventory lists all containers from running-containers.txt
5. mcow inventory documents cross-server VoidNet dependencies
6. nether inventory documents AmneziaWG VPN and public IP
</verification>

<success_criteria>
- 6 inventory.md files exist, one per server
- Every file has all D-08 required fields (hostname, Tailscale IP, role, hardware, services, storage, access)
- Hardware specs are populated from SSH queries where reachable, marked "pending" where not
- Cross-server dependencies are documented (mcow -> docker-tower)
- Inventory data is sufficient to build dependency map and network topology in Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-02-SUMMARY.md`
</output>
