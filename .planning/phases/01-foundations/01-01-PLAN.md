---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - secrets/.gitkeep
  - docs/.gitkeep
  - shared/.gitkeep
  - servers/tower-sat/.gitkeep
  - servers/cc-vk/.gitkeep
autonomous: true
requirements: [SEC-01, SEC-02]
user_setup:
  - service: age
    why: "SOPS encryption backend — age keypair must exist on operator machine"
    env_vars: []
    dashboard_config:
      - task: "Verify age key exists at ~/.config/sops/age/keys.txt or generate with age-keygen"
        location: "Operator machine terminal"

must_haves:
  truths:
    - "No plaintext secret file can be committed to git"
    - ".sops.yaml at hub root enables SOPS encryption for all sub-repos"
    - "secrets/ directory exists for encrypted YAML files"
    - "Repo structure has servers/, docs/, shared/ directories"
    - "Root-level docker-tower/ duplicate is removed"
  artifacts:
    - path: ".gitignore"
      provides: "Secret file blocking patterns"
      contains: "!secrets/*.sops.yaml"
    - path: "secrets/.gitkeep"
      provides: "Placeholder for future encrypted secret files"
    - path: "docs/.gitkeep"
      provides: "Cross-server documentation directory"
    - path: "shared/.gitkeep"
      provides: "Cross-cutting concerns directory"
    - path: "servers/tower-sat/.gitkeep"
      provides: "tower-sat server directory"
    - path: "servers/cc-vk/.gitkeep"
      provides: "cc-vk server directory"
  key_links:
    - from: "/Users/admin/hub/.sops.yaml"
      to: "secrets/*.sops.yaml"
      via: "SOPS creation_rules path_regex"
      pattern: "path_regex.*secrets.*sops\\.yaml"
    - from: ".gitignore"
      to: "secrets/"
      via: "gitignore rules block plaintext, allow .sops.yaml"
      pattern: "!secrets/\\*\\.sops\\.yaml"
---

<objective>
Establish the secrets management pattern (SOPS + age) and scaffold the repo directory structure before any configuration content is committed.

Purpose: SEC-01 and SEC-02 are blocking requirements -- no server configs should be committed until secrets are safe and the canonical directory layout exists.
Output: Hub-level .sops.yaml, hardened .gitignore, secrets/ directory, docs/ and shared/ directories, removal of root docker-tower/ duplicate, missing server directories created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold repo directory structure and harden .gitignore</name>
  <files>.gitignore, secrets/.gitkeep, docs/.gitkeep, shared/.gitkeep, servers/tower-sat/.gitkeep, servers/cc-vk/.gitkeep, docker-tower/.gitkeep</files>
  <read_first>
    - .gitignore (current state to augment, not replace)
    - servers/ (ls to confirm existing subdirectories)
    - docker-tower/.gitkeep (confirm root-level duplicate exists before removal)
  </read_first>
  <action>
1. Create missing directories with .gitkeep files:
   - `secrets/.gitkeep`
   - `docs/.gitkeep`
   - `shared/.gitkeep`
   - `servers/tower-sat/.gitkeep`
   - `servers/cc-vk/.gitkeep`

2. Remove the root-level `docker-tower/` directory (per D-03). This contains only a `.gitkeep` -- the canonical location is `servers/docker-tower/`. Run: `git rm -r docker-tower/`

3. Augment `.gitignore` (do NOT replace existing content). Append these blocks after the existing content:

```
# SOPS decrypted outputs (never commit plaintext)
secrets/*.yaml
!secrets/*.sops.yaml

# age key material
*.age
keys.txt

# SOPS temp files
*.sops.yaml.tmp
```

The pattern `secrets/*.yaml` blocks all YAML in secrets/. The exception `!secrets/*.sops.yaml` re-allows only the encrypted form. This implements the naming convention: `*.sops.yaml` = encrypted (committed), `*.yaml` = plaintext (blocked).
  </action>
  <verify>
    <automated>cd /Users/admin/hub/workspace/homelab && test -d secrets && test -d docs && test -d shared && test -d servers/tower-sat && test -d servers/cc-vk && test ! -d docker-tower && grep -q '!secrets/\*.sops.yaml' .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `secrets/.gitkeep` exists
    - `docs/.gitkeep` exists
    - `shared/.gitkeep` exists
    - `servers/tower-sat/.gitkeep` exists
    - `servers/cc-vk/.gitkeep` exists
    - `docker-tower/` directory does NOT exist at repo root
    - `.gitignore` contains `secrets/*.yaml`
    - `.gitignore` contains `!secrets/*.sops.yaml`
    - `.gitignore` contains `*.age`
    - `.gitignore` contains `keys.txt`
    - `.gitignore` still contains original entries (`.env`, `*.key`, `*.pem`, `.DS_Store`)
  </acceptance_criteria>
  <done>Repo has canonical directory layout (servers/, docs/, shared/, secrets/) with root docker-tower/ removed. .gitignore blocks all plaintext secrets and age key material.</done>
</task>

<task type="auto">
  <name>Task 2: Create hub-level .sops.yaml and verify SOPS+age integration</name>
  <files>/Users/admin/hub/.sops.yaml</files>
  <read_first>
    - /Users/admin/hub/.sops.yaml (confirm it does NOT exist yet)
    - ~/.config/sops/age/keys.txt (check if age key already exists; if not, generate one)
  </read_first>
  <action>
1. Check if age key exists: `test -f ~/.config/sops/age/keys.txt`. 
   - If it exists: extract the public key with `grep "public key:" ~/.config/sops/age/keys.txt | awk '{print $NF}'`
   - If it does NOT exist: generate with `mkdir -p ~/.config/sops/age && age-keygen -o ~/.config/sops/age/keys.txt 2>&1 | grep "Public key:"` and capture the public key from stderr output

2. Create `/Users/admin/hub/.sops.yaml` with the following content (substitute the actual age public key for `AGE_PUBLIC_KEY`):

```yaml
creation_rules:
  - path_regex: .*/secrets/.*\.sops\.yaml$
    age: >-
      AGE_PUBLIC_KEY
```

Per D-04, this file lives at hub root (`/Users/admin/hub/`) so all sibling projects (homelab, animaya, voidnet) share one SOPS config. Per D-06, the private key lives only on operator machines.

3. Verify the integration works end-to-end:
   - Create a test encrypted file: `cd /Users/admin/hub/workspace/homelab && echo "test_key: test_value" | sops encrypt --input-type yaml --output-type yaml /dev/stdin > secrets/test.sops.yaml`
   - Verify it was encrypted: `grep -q "ENC\[AES256_GCM" secrets/test.sops.yaml || grep -q "age" secrets/test.sops.yaml`
   - Verify it can be decrypted: `sops decrypt secrets/test.sops.yaml | grep -q "test_key: test_value"`
   - Clean up: `rm secrets/test.sops.yaml`

4. Verify .gitignore blocks plaintext but allows encrypted:
   - `echo "test: val" > secrets/plaintext-test.yaml`
   - `git status` should NOT show `secrets/plaintext-test.yaml` as untracked (blocked by .gitignore)
   - `rm secrets/plaintext-test.yaml`

Note: The hub `.sops.yaml` file is outside the homelab repo. It will need to be committed in the hub repo separately, or noted for the user. The executor should note this in the summary.
  </action>
  <verify>
    <automated>test -f /Users/admin/hub/.sops.yaml && grep -q "age1" /Users/admin/hub/.sops.yaml && grep -q "path_regex.*secrets.*sops" /Users/admin/hub/.sops.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/admin/hub/.sops.yaml` exists
    - `/Users/admin/hub/.sops.yaml` contains `path_regex: .*/secrets/.*\.sops\.yaml$`
    - `/Users/admin/hub/.sops.yaml` contains an age public key starting with `age1`
    - `~/.config/sops/age/keys.txt` exists (either pre-existing or newly generated)
    - SOPS encrypt/decrypt round-trip succeeds with the configured key
    - A plaintext `.yaml` file in `secrets/` is blocked by .gitignore (does not appear in `git status`)
  </acceptance_criteria>
  <done>Hub-level .sops.yaml is configured with age recipient. SOPS can encrypt and decrypt files in secrets/. Plaintext secrets are blocked from git tracking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git commit boundary | Unencrypted secrets must never cross into git history |
| operator machine boundary | age private key must stay on cc-vk and local dev only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | secrets/*.yaml | mitigate | .gitignore blocks `secrets/*.yaml`, allows only `secrets/*.sops.yaml`; Task 1 implements this pattern |
| T-01-02 | Information Disclosure | age private key | mitigate | .gitignore blocks `*.age` and `keys.txt`; D-06 mandates key stays on operator machines only |
| T-01-03 | Denial of Service | .sops.yaml missing/wrong key | mitigate | Task 2 verifies encrypt/decrypt round-trip; age public key documented in .sops.yaml (readable) |
| T-01-04 | Tampering | .sops.yaml creation_rules | accept | Hub repo is private; .sops.yaml is version-controlled; low risk at this scale |
</threat_model>

<verification>
1. `test -d secrets && test -d docs && test -d shared` -- directories exist
2. `test ! -d docker-tower` -- root duplicate removed
3. `test -f /Users/admin/hub/.sops.yaml` -- SOPS config exists
4. `grep -q 'age1' /Users/admin/hub/.sops.yaml` -- has age recipient
5. `grep -q '!secrets/\*.sops.yaml' .gitignore` -- gitignore pattern correct
6. Round-trip encrypt/decrypt test passes
</verification>

<success_criteria>
- SOPS + age pattern is fully operational: encrypt and decrypt work end-to-end
- No plaintext secret file can be staged in git (gitignore blocks it)
- Repo has canonical directory structure: servers/, docs/, shared/, secrets/
- Root-level docker-tower/ duplicate is gone
- Missing server directories (tower-sat, cc-vk) exist
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-01-SUMMARY.md`
</output>
