# 04-03 Resume — Checkpoint 1 State

## What just happened (FIRING cycle)

- **17:26:15Z** — Silenced DiskUsageCritical for 100.101.0.7:9100 (silence `d8a4f221-1526-41e5-9c03-b0d7485511b1`) and 100.101.0.8:9100 (silence `0813f485-a3d0-41ce-9746-38607644992e`), 20-min window (ends 17:46:15Z).
- **17:26:20Z** — `systemctl stop node_exporter` on cc-worker (100.99.133.9).
- **~17:32:20Z** — Prometheus HostDown alert transitioned pending → **firing** (for: 5m + 1 scrape interval = 6 min).
- **~17:32:30Z** — Alertmanager confirmed HostDown active with labels `{alertname=HostDown, instance=100.99.133.9:9100, job=node, severity=critical}`.
- **~17:33:15Z** — Counter delta captured:
  - `alertmanager_notifications_total{integration="telegram"} 1 → 2`
  - **+1 firing notification dispatched.**

## Expected Telegram message body

Delivered to @void_homelab_bot (chat per configured `chat_id`):
- Subject line mentions **HostDown** / critical
- Host: **cc-worker** (or instance `100.99.133.9:9100`)
- Severity: **critical**
- Fresh `startsAt` within the last ~60s from operator-receive time

## Awaiting operator

Operator: confirm the FIRING message arrived in Telegram chat fresh (post-17:33Z).

## Next steps on resume

1. `systemctl start node_exporter` on cc-worker (capture ISO ts).
2. Wait ~30s for `up{instance="100.99.133.9:9100"}` to return to 1 (Prometheus resolves HostDown).
3. Poll `alertmanager_notifications_total{integration="telegram",state="resolved"}` — expect increment within ~5 min (AM resolve_timeout + dispatch).
4. Checkpoint 2 for operator RESOLVED confirmation.
5. Clean up: delete two silences (ids above).
6. Write `04-03-SUMMARY.md`, flip ROADMAP, commit.

## Safety state

- node_exporter on cc-worker: **STOPPED** (must restart after Checkpoint 1 confirm).
- Silences expire 17:46:15Z — if resume is delayed past that, DiskUsageCritical will redispatch and pollute deltas; re-silence or accept pollution.
- Bot token: SOPS-encrypted, not exposed.
