---
phase: 04-operator-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json
  - servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-summary.json
  - servers/mcow/monitoring/grafana/grafana.ini
  - servers/mcow/docker-compose.monitoring.yml
  - servers/mcow/monitoring/README.md
autonomous: false
requirements:
  - MON-01
  - MON-02
must_haves:
  truths:
    - "Operator Overview dashboard loaded as Grafana home dashboard (UID homelab-overview)"
    - "KPI strip shows 6 host status pills (tower, docker-tower, cc-worker, mcow, nether, animaya-dev), total active-alerts count, total running-container count"
    - "Per-host detail rows render CPU%, RAM%, disk% gauges + time-series, container count, uptime, network I/O for all 6 hosts"
    - "Active-alerts panel uses Alertmanager datasource (uid alertmanager-homelab), NOT Prometheus ALERTS metric"
    - "Container-count panels show N/A for tower, nether, animaya-dev (no cAdvisor); show live count for docker-tower and mcow"
    - "Homelab Summary dashboard remains available as secondary detail view"
    - "User approved the dashboard in UAT (checkpoint)"
  artifacts:
    - path: "servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json"
      provides: "Operator Overview dashboard"
      contains: '"uid": "homelab-overview"'
    - path: "servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-summary.json"
      provides: "Secondary detail dashboard (ported from docker-tower)"
    - path: "servers/mcow/monitoring/grafana/grafana.ini"
      provides: "Home dashboard pin"
      contains: "default_home_dashboard_path"
  key_links:
    - from: "homelab-overview.json panels"
      to: "Prometheus datasource"
      via: "datasource.uid = prometheus-homelab"
      pattern: '"uid": "prometheus-homelab"'
    - from: "active-alerts panel"
      to: "Alertmanager datasource"
      via: "datasource.uid = alertmanager-homelab"
      pattern: '"uid": "alertmanager-homelab"'
    - from: "Grafana grafana.ini"
      to: "home dashboard JSON"
      via: "default_home_dashboard_path"
      pattern: "homelab-overview.json"
---

<objective>
Build the "Operator Overview" dashboard as provisioned JSON, provision it as the Grafana home dashboard on mcow, and port the existing Homelab Summary dashboard as a secondary view. Every panel visible at a glance: 6 host status pills + alert count + container count in a KPI strip; per-host detail rows with CPU/RAM/disk/containers/uptime/net I/O. Active-alerts panel uses the Alertmanager datasource (respects silences/inhibits — matches what fires Telegram).

Purpose: Deliver the one-page operator view that is the user-facing half of Phase 04 success criteria 2 and 4.
Output: `homelab-overview.json` + `homelab-summary.json` provisioned on mcow Grafana; home dashboard pinned; user-approved via UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-operator-dashboard/04-CONTEXT.md
@.planning/phases/04-operator-dashboard/04-01-SUMMARY.md
@.planning/phases/03-health-monitoring/03-04-SUMMARY.md
@servers/docker-tower/monitoring/grafana/provisioning/dashboards/json/homelab-summary.json
@servers/mcow/docker-compose.monitoring.yml
@servers/mcow/monitoring/grafana/provisioning/datasources/prometheus.yml
@servers/mcow/monitoring/grafana/provisioning/datasources/alertmanager.yml

<interfaces>
Datasource UIDs (pinned in 04-01, do not change):
- Prometheus:   uid = "prometheus-homelab"
- Alertmanager: uid = "alertmanager-homelab"

Host Tailnet IPs used in PromQL `instance` labels (format `IP:9100`):
- tower:        100.101.0.7:9100
- docker-tower: 100.101.0.8:9100
- cc-worker:    100.99.133.9:9100
- mcow:         100.101.0.9:9100
- nether:       100.101.0.3:9100
- animaya-dev:  100.119.15.122:9100

cAdvisor target instances (from docker-tower + mcow only):
- docker-tower: 100.101.0.8:8080
- mcow:         100.101.0.9:18080

Gauge thresholds (per CONTEXT.md Claude's discretion):
- CPU%:  0-80 green,  80-95 amber, 95-100 red
- RAM%:  0-85 green,  85-95 amber, 95-100 red
- Disk%: 0-80 green,  80-90 amber, 90-100 red

Key PromQL queries (canonical — paste verbatim into panels):
- up status:   up{job="node",instance="$HOST:9100"}
- CPU%:        100 - (avg by (instance) (irate(node_cpu_seconds_total{mode="idle",instance="$HOST:9100"}[5m])) * 100)
- RAM%:        100 * (1 - (node_memory_MemAvailable_bytes{instance="$HOST:9100"} / node_memory_MemTotal_bytes{instance="$HOST:9100"}))
- Disk% (/):   100 - (node_filesystem_avail_bytes{instance="$HOST:9100",mountpoint="/",fstype!="tmpfs"} / node_filesystem_size_bytes{instance="$HOST:9100",mountpoint="/",fstype!="tmpfs"} * 100)
- Uptime:      node_time_seconds{instance="$HOST:9100"} - node_boot_time_seconds{instance="$HOST:9100"}
- Net RX:      irate(node_network_receive_bytes_total{instance="$HOST:9100",device!~"lo|veth.*|docker.*|br-.*|tailscale.*"}[5m])
- Net TX:      irate(node_network_transmit_bytes_total{instance="$HOST:9100",device!~"lo|veth.*|docker.*|br-.*|tailscale.*"}[5m])
- Container count (per host):  count(container_last_seen{name!=""}) by (instance)  -- returns entries for 100.101.0.8:8080 and 100.101.0.9:18080
- Total containers (fleet):    count(container_last_seen{name!=""})
- Total firing alerts: (Alertmanager datasource) count of alerts where status.state = "active"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write homelab-overview.json (KPI strip + per-host detail rows)</name>
  <read_first>
    - servers/docker-tower/monitoring/grafana/provisioning/dashboards/json/homelab-summary.json (reference for query patterns + panel structure)
    - servers/mcow/monitoring/grafana/provisioning/datasources/prometheus.yml (UID to reference)
    - servers/mcow/monitoring/grafana/provisioning/datasources/alertmanager.yml (UID to reference)
  </read_first>
  <action>
    Create `servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json`.

    Dashboard shape (Grafana schemaVersion 39, Grafana 11.x compatible):

    - Top-level fields:
      - `"uid": "homelab-overview"`
      - `"title": "Operator Overview"`
      - `"tags": ["homelab","overview"]`
      - `"timezone": "browser"`
      - `"refresh": "30s"`
      - `"time": {"from": "now-1h", "to": "now"}`
      - `"schemaVersion": 39`
      - `"editable": false` (provisioned)

    - **Row 1 — STATUS KPI strip (y=0, h=4):**
      - 6× `stat` panels (one per host), width 3 each (cols 0,3,6,9,12,15):
        - Display name = short hostname (tower, docker-tower, cc-worker, mcow, nether, animaya-dev)
        - Query: `up{job="node",instance="<IP>:9100"}` (Prometheus, uid prometheus-homelab)
        - Value mappings: `1 -> UP (green)`, `0 -> DOWN (red)`, `null -> NO-DATA (grey)`
        - `"colorMode": "background"`, `"textMode": "value_and_name"`
      - 1× `stat` panel "Alerts firing" at x=18, w=3, h=4:
        - Datasource: Alertmanager (uid `alertmanager-homelab`)
        - Query: leave expr empty and use `filter` = `state="active"` OR use Grafana's Alertmanager query type — set `"queryType": "alerts"` and filter in transformation with `count`. Alternatively fall back to `ALERTS{alertstate="firing"}` on Prometheus with a count transformation — but per D-11 **prefer the Alertmanager datasource**. Expected result: a single numeric count.
        - Thresholds: 0 green, ≥1 red.
      - 1× `stat` panel "Containers running" at x=21, w=3, h=4:
        - Datasource: Prometheus
        - Query (instant): `count(container_last_seen{name!=""})`
        - Thresholds: 0 red, ≥1 green.

    - **Rows 2-7 — Per-host detail (one row per host, 6 rows total):**
      For each host (tower, docker-tower, cc-worker, mcow, nether, animaya-dev), emit a collapsible Grafana `row` panel titled by hostname, with 6 child panels inside (y auto-layout, h=6 each):

      | Panel | Type | Width | Query |
      |-------|------|-------|-------|
      | CPU % | gauge + timeseries overlay (use `gauge` type w/ thresholds) | 4 | `100 - (avg by (instance) (irate(node_cpu_seconds_total{mode="idle",instance="<IP>:9100"}[5m])) * 100)` |
      | RAM % | gauge | 4 | `100 * (1 - (node_memory_MemAvailable_bytes{instance="<IP>:9100"} / node_memory_MemTotal_bytes{instance="<IP>:9100"}))` |
      | Disk % (/) | gauge | 4 | `100 - (node_filesystem_avail_bytes{instance="<IP>:9100",mountpoint="/",fstype!="tmpfs"} / node_filesystem_size_bytes{instance="<IP>:9100",mountpoint="/",fstype!="tmpfs"} * 100)` |
      | Containers | stat | 4 | For docker-tower and mcow: `count(container_last_seen{name!="",instance=~"<IP>:(8080|18080)"})`. For tower/nether/animaya-dev: same query — returns empty — use `noValue: "N/A"` in fieldConfig to render "N/A". |
      | Uptime | stat (unit = `s`, display = `duration`) | 4 | `node_time_seconds{instance="<IP>:9100"} - node_boot_time_seconds{instance="<IP>:9100"}` |
      | Net I/O (rx/tx) | timeseries (2 series) | 4 | A: `irate(node_network_receive_bytes_total{instance="<IP>:9100",device!~"lo\|veth.*\|docker.*\|br-.*\|tailscale.*"}[5m])` legend "rx", B: `irate(node_network_transmit_bytes_total{...}[5m])` legend "tx", unit = `Bps` |

      Apply gauge thresholds:
      - CPU gauge: `"thresholds": {"mode":"absolute","steps":[{"value":null,"color":"green"},{"value":80,"color":"orange"},{"value":95,"color":"red"}]}`
      - RAM gauge: steps at 0/85/95.
      - Disk gauge: steps at 0/80/90.

      For ALL per-host panels targeting prometheus: set `"datasource": {"type":"prometheus","uid":"prometheus-homelab"}`.

    Expand the first 3 rows (tower/docker-tower/cc-worker) by default; collapse mcow/nether/animaya-dev to keep the initial view focused on the currently-most-loaded hosts — `row.collapsed: false/true` per row.

    Panel IDs must be unique integers (1..100+). Panel `id` for panels in the JSON must be assigned sequentially.

    Validate JSON syntactically with `jq` before saving:
    ```bash
    jq -e '.uid == "homelab-overview" and .title == "Operator Overview" and (.panels | length) >= 7' \
      servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json
    ```
  </action>
  <verify>
    <automated>
    cd /Users/admin/hub/workspace/homelab &amp;&amp; \
    jq -e '.uid == "homelab-overview"' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json &amp;&amp; \
    jq -e '.title == "Operator Overview"' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json &amp;&amp; \
    jq -e '[.. | objects | select(.datasource?.uid == "prometheus-homelab")] | length >= 20' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json &amp;&amp; \
    jq -e '[.. | objects | select(.datasource?.uid == "alertmanager-homelab")] | length >= 1' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json &amp;&amp; \
    jq -e '[.. | strings | select(test("100\\.101\\.0\\.7:9100"))] | length &gt; 0' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json &amp;&amp; \
    jq -e '[.. | strings | select(test("100\\.119\\.15\\.122:9100"))] | length &gt; 0' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json &amp;&amp; \
    jq -e '[.. | strings | select(test("100\\.99\\.133\\.9:9100"))] | length &gt; 0' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json &amp;&amp; \
    ! grep -q 'ALERTS{alertstate' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json
    </automated>
  </verify>
  <done>
    JSON valid. 6 host pills + alerts stat (Alertmanager datasource) + containers stat in KPI strip. 6 per-host rows with CPU/RAM/disk gauges + containers + uptime + net I/O. All 6 Tailnet IPs present. No Prometheus ALERTS metric used (Alertmanager datasource instead).
  </done>
</task>

<task type="auto">
  <name>Task 2: Port Homelab Summary, pin home dashboard, deploy, verify</name>
  <read_first>
    - servers/docker-tower/monitoring/grafana/provisioning/dashboards/json/homelab-summary.json (copy source)
    - servers/mcow/docker-compose.monitoring.yml (add grafana.ini mount if using INI; else env var override)
    - servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json (just-written)
  </read_first>
  <action>
    **Step A — Port Homelab Summary.** Copy
    `servers/docker-tower/monitoring/grafana/provisioning/dashboards/json/homelab-summary.json`
    to `servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-summary.json`
    verbatim. The dashboard already references `uid: prometheus-homelab` (pinned in 04-01), so no changes needed.

    **Step B — Pin home dashboard.** Create `servers/mcow/monitoring/grafana/grafana.ini`:
    ```ini
    [users]
    default_home_dashboard_path = /etc/grafana/provisioning/dashboards/json/homelab-overview.json
    ```
    Update `servers/mcow/docker-compose.monitoring.yml` — add volume mount for grafana:
    ```yaml
          volumes:
            - grafana-data:/var/lib/grafana
            - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
            - ./monitoring/grafana/grafana.ini:/etc/grafana/grafana.ini:ro
    ```

    **Step C — Deploy.**
    ```bash
    rsync -aP /Users/admin/hub/workspace/homelab/servers/mcow/ root@mcow:/root/homelab/servers/mcow/
    ssh root@mcow 'cd /root/homelab/servers/mcow &amp;&amp; docker compose -f docker-compose.monitoring.yml up -d --force-recreate grafana'
    # Wait for Grafana
    for i in 1 2 3 4 5; do
      sleep 3
      curl -sf http://100.101.0.9:3000/api/health >/dev/null &amp;&amp; break
    done
    ```

    **Step D — Verify provisioning and home dashboard.**
    ```bash
    source /run/secrets/grafana.env   # on mcow, or adapt to extract locally via sops
    GF_PW=$(sops -d --extract '["GF_SECURITY_ADMIN_PASSWORD"]' /Users/admin/hub/workspace/homelab/secrets/mcow.sops.yaml)
    # 1. Overview dashboard loaded
    curl -s -u admin:$GF_PW "http://100.101.0.9:3000/api/dashboards/uid/homelab-overview" | jq -re '.dashboard.uid == "homelab-overview"'
    # 2. Summary dashboard loaded
    curl -s -u admin:$GF_PW "http://100.101.0.9:3000/api/dashboards/uid/homelab-summary" | jq -re '.dashboard.uid == "homelab-summary"'
    # 3. Home dashboard pinned
    curl -s -u admin:$GF_PW "http://100.101.0.9:3000/api/dashboards/home" | jq -re '.dashboard.uid == "homelab-overview"'
    # 4. Sample query returns data (CPU for docker-tower)
    curl -s "http://100.101.0.8:9090/api/v1/query" --data-urlencode 'query=100 - (avg by (instance) (irate(node_cpu_seconds_total{mode="idle",instance="100.101.0.8:9100"}[5m])) * 100)' | jq -re '.data.result | length &gt; 0'
    ```
  </action>
  <verify>
    <automated>
    cd /Users/admin/hub/workspace/homelab &amp;&amp; \
    test -f servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-summary.json &amp;&amp; \
    test -f servers/mcow/monitoring/grafana/grafana.ini &amp;&amp; \
    grep -q 'homelab-overview.json' servers/mcow/monitoring/grafana/grafana.ini &amp;&amp; \
    grep -q 'grafana.ini:/etc/grafana/grafana.ini:ro' servers/mcow/docker-compose.monitoring.yml &amp;&amp; \
    GF_PW=$(sops -d --extract '["GF_SECURITY_ADMIN_PASSWORD"]' secrets/mcow.sops.yaml) &amp;&amp; \
    [ "$(curl -s -u admin:$GF_PW http://100.101.0.9:3000/api/dashboards/home | jq -r .dashboard.uid)" = "homelab-overview" ] &amp;&amp; \
    [ "$(curl -s -u admin:$GF_PW http://100.101.0.9:3000/api/dashboards/uid/homelab-summary | jq -r .dashboard.uid)" = "homelab-summary" ]
    </automated>
  </verify>
  <done>
    Both dashboards provisioned. Home dashboard = Operator Overview. Summary remains as secondary.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — User approves Operator Overview dashboard</name>
  <what-built>
    The Operator Overview dashboard on mcow Grafana at http://100.101.0.9:3000, pinned as the home dashboard. KPI strip across the top (6 host pills, active-alerts count, fleet container count). Six per-host detail rows below with CPU/RAM/disk gauges, container count, uptime, net I/O.
  </what-built>
  <how-to-verify>
    **Claude Code runs beforehand (automated):** Fetch Grafana admin password (`sops -d --extract '["GF_SECURITY_ADMIN_PASSWORD"]' secrets/mcow.sops.yaml`) and paste it into the checkpoint prompt for the user.

    1. Open browser (on a machine connected to Tailnet): `http://100.101.0.9:3000`
    2. Log in as `admin` / `<password from SOPS>`.
    3. You should land directly on "Operator Overview" (it's the home dashboard).
    4. Verify KPI strip shows all 6 host pills:
       - tower — UP green (instance 100.101.0.7)
       - docker-tower — UP green
       - cc-worker — UP green
       - mcow — UP green
       - nether — UP green
       - animaya-dev — depends on 04-04 order: UP green if 04-04 ran first, otherwise NO-DATA grey (acceptable — will go green after 04-04)
    5. Verify "Alerts firing" shows a number (0 if steady state; non-zero if animaya-dev HostDown still pending and 04-04 hasn't run).
    6. Verify "Containers running" shows a positive number (≥ ~20 on docker-tower + mcow combined).
    7. Scroll through all 6 per-host rows. For each host with node-exporter live, gauges should show actual values (CPU ~5-40%, RAM 30-80%, disk varies). Time-series should render data.
    8. For docker-tower and mcow, Containers panel shows a positive count. For tower, nether, animaya-dev, Containers panel shows "N/A".
    9. Hover a gauge — tooltip shows current value + timestamp.
    10. Navigate to Dashboards → Homelab folder — confirm "Homelab Summary" dashboard also exists and loads.

    **Stop conditions:** any KPI pill missing; alert panel not using Alertmanager datasource (check panel datasource in edit mode — should say "Alertmanager", not "Prometheus"); gauges not rendering data for live hosts; no-data for hosts other than animaya-dev.
  </how-to-verify>
  <acceptance_criteria>
    User responds "approved" and confirms:
    - [ ] Home dashboard = Operator Overview (landed automatically)
    - [ ] 6 host pills visible in KPI strip
    - [ ] Alert count panel pulls from Alertmanager (verified in panel edit pane)
    - [ ] Container count panel shows positive integer
    - [ ] Per-host gauges render for all hosts with node-exporter live
    - [ ] Homelab Summary accessible as secondary dashboard
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Grafana | Admin-only auth, Tailnet-gated |
| Grafana → Prometheus | Internal Tailnet, no auth required (firewall-equivalent via Tailscale) |
| Grafana → Alertmanager | Internal docker bridge network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Active-alerts panel exposing secrets via annotation labels | mitigate | Alertmanager datasource respects silences/inhibits; no alert annotations contain tokens — confirmed by `alertmanager.yml` template (only uses `{{ .Labels.* }}` and `{{ .Annotations.summary/description }}`). |
| T-04-02-02 | Tampering | Ad-hoc dashboard edits reverting on restart | accept | `allowUiUpdates: false` is intentional — dashboards-as-code. Any UI edit reverts in 30s (D-09 locked). |
| T-04-02-03 | Repudiation | No audit of dashboard changes | accept | Git log on provisioning JSON is the audit trail. |
| T-04-02-04 | Denial of Service | Heavy PromQL (20+ panels) overloading Prometheus | mitigate | Queries are lightweight rate/avg ops on indexed series; `refresh: 30s`; scrape_interval 15s — confirmed sufficient in Phase 03 load testing. |
| T-04-02-05 | Information Disclosure | Home dashboard pin writing to grafana-data volume | accept | Volume is local to mcow (Tailnet-only host); no external exfil path. |

</threat_model>

<verification>
1. `jq -e '.uid == "homelab-overview"' servers/mcow/monitoring/grafana/provisioning/dashboards/json/homelab-overview.json` → exit 0
2. `curl -s -u admin:$GF_PW http://100.101.0.9:3000/api/dashboards/home | jq -r .dashboard.uid` → `homelab-overview`
3. `curl -s -u admin:$GF_PW http://100.101.0.9:3000/api/dashboards/uid/homelab-summary` → 200 with dashboard body
4. `curl -s -u admin:$GF_PW "http://100.101.0.9:3000/api/datasources/uid/alertmanager-homelab/resources/api/v2/alerts"` returns a JSON array (even if empty)
5. No occurrences of `ALERTS{alertstate=` in homelab-overview.json
6. Human UAT approval recorded in SUMMARY
</verification>

<success_criteria>
- Operator Overview dashboard loaded as home
- KPI strip + 6 per-host detail rows rendering data
- Active-alerts panel uses Alertmanager datasource (not Prometheus `ALERTS`)
- Container count panels show N/A for hosts without cAdvisor (tower/nether/animaya-dev)
- User approves in UAT
</success_criteria>

<output>
After completion, create `.planning/phases/04-operator-dashboard/04-02-SUMMARY.md` including a screenshot (PNG at `.planning/phases/04-operator-dashboard/assets/operator-overview.png`) and the UAT approval statement.
</output>
