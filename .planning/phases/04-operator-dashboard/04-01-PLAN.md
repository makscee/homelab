---
phase: 04-operator-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/mcow/docker-compose.monitoring.yml
  - servers/mcow/monitoring/grafana/provisioning/datasources/prometheus.yml
  - servers/mcow/monitoring/grafana/provisioning/datasources/alertmanager.yml
  - servers/mcow/monitoring/grafana/provisioning/dashboards/dashboards.yml
  - servers/mcow/monitoring/alertmanager/alertmanager.yml
  - servers/mcow/monitoring/alertmanager/README.md
  - servers/mcow/monitoring/README.md
  - secrets/mcow.sops.yaml
  - servers/docker-tower/docker-compose.monitoring.yml
  - servers/docker-tower/monitoring/prometheus/prometheus.yml
  - servers/docker-tower/monitoring/alertmanager/README.md
autonomous: true
requirements:
  - MON-01
  - MON-02
must_haves:
  truths:
    - "Grafana reachable at http://100.101.0.9:3000 and authenticates with SOPS-sourced admin creds"
    - "Grafana Prometheus datasource reaches docker-tower Prometheus over Tailnet at http://100.101.0.8:9090"
    - "Alertmanager on mcow reachable at http://100.101.0.9:9093 with Telegram bot_token_file loaded"
    - "Prometheus on docker-tower sends alerts to mcow Alertmanager (alertmanagers: target = 100.101.0.9:9093)"
    - "docker-tower Grafana container stopped (volume preserved for 1 week rollback)"
  artifacts:
    - path: "servers/mcow/docker-compose.monitoring.yml"
      provides: "mcow Grafana + Alertmanager stack"
      contains: "grafana:"
    - path: "servers/mcow/monitoring/alertmanager/alertmanager.yml"
      provides: "Alertmanager Telegram receiver on mcow"
      contains: "bot_token_file: /etc/alertmanager/telegram_token"
    - path: "servers/mcow/monitoring/grafana/provisioning/datasources/prometheus.yml"
      provides: "Prometheus datasource with pinned UID"
      contains: "uid: prometheus-homelab"
    - path: "servers/mcow/monitoring/grafana/provisioning/datasources/alertmanager.yml"
      provides: "Alertmanager datasource for active-alerts panel"
      contains: "type: alertmanager"
  key_links:
    - from: "mcow Grafana"
      to: "docker-tower Prometheus"
      via: "http://100.101.0.8:9090 over Tailnet"
      pattern: "url: http://100\\.101\\.0\\.8:9090"
    - from: "docker-tower Prometheus"
      to: "mcow Alertmanager"
      via: "alertmanagers static_configs"
      pattern: "100\\.101\\.0\\.9:9093"
    - from: "/run/secrets/telegram_token on mcow"
      to: "Alertmanager container /etc/alertmanager/telegram_token"
      via: "bind mount"
      pattern: "0440 root:65534"
---

<objective>
Migrate Grafana from docker-tower to mcow, co-locate Alertmanager on mcow, and rewire Prometheus on docker-tower to point at the new Alertmanager. Decommission (stop) docker-tower Grafana container. Preserve docker-tower Grafana volume for one rollback week. Enforce Telegram token perms `0440 root:65534` (load-bearing — do not regress to 0600).

Purpose: Move operator UI and alert routing to mcow (Tailnet-only) while Prometheus stays on docker-tower. Establishes the mcow monitoring surface that plans 04-02/04-03/04-04 build on.
Output: Running Grafana + Alertmanager on mcow; Prometheus rewired; docker-tower Grafana stopped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-operator-dashboard/04-CONTEXT.md
@.planning/phases/03-health-monitoring/03-05-SUMMARY.md
@servers/docker-tower/docker-compose.monitoring.yml
@servers/docker-tower/monitoring/alertmanager/alertmanager.yml
@servers/docker-tower/monitoring/alertmanager/README.md
@servers/docker-tower/monitoring/grafana/provisioning/datasources/prometheus.yml
@servers/docker-tower/monitoring/grafana/provisioning/dashboards/dashboards.yml
@servers/docker-tower/monitoring/prometheus/prometheus.yml

<interfaces>
Host IPs (Tailnet, exact):
- tower: 100.101.0.7
- docker-tower: 100.101.0.8
- mcow: 100.101.0.9
- cc-worker: 100.99.133.9
- nether: 100.101.0.3
- animaya-dev: 100.119.15.122

Port allocations on mcow (100.101.0.9):
- 3000  — Grafana (NEW, this plan)
- 9093  — Alertmanager (NEW, this plan)
- 18080 — cAdvisor (existing, do not touch)
- 9100  — node-exporter (existing, systemd, do not touch)
- 8080  — voidnet-api (existing, native binary — DO NOT bind anything else here)

SOPS secret keys expected in secrets/mcow.sops.yaml:
- GF_SECURITY_ADMIN_USER     (string, e.g. "admin")
- GF_SECURITY_ADMIN_PASSWORD (string, generated, high-entropy)
- TELEGRAM_BOT_TOKEN         (string, same value currently in secrets/docker-tower.sops.yaml)

Telegram chat_id: 193835258 (NOT a secret — plain in alertmanager.yml).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mcow monitoring scaffold + SOPS secrets + compose</name>
  <read_first>
    - servers/docker-tower/docker-compose.monitoring.yml (lift grafana + alertmanager stanzas verbatim; adapt IPs)
    - servers/docker-tower/monitoring/alertmanager/alertmanager.yml (copy verbatim)
    - servers/docker-tower/monitoring/alertmanager/README.md (perms recipe — load-bearing)
    - servers/docker-tower/monitoring/grafana/provisioning/datasources/prometheus.yml (UID pattern)
    - servers/docker-tower/monitoring/grafana/provisioning/dashboards/dashboards.yml (provider pattern)
    - servers/mcow/docker-compose.cadvisor.yml (port allocation reference — do not collide)
    - secrets/docker-tower.sops.yaml (reference for TELEGRAM_BOT_TOKEN value to copy into mcow.sops.yaml)
  </read_first>
  <action>
    Create the mcow monitoring directory tree and SOPS secrets file.

    1. Create directories:
       ```
       mkdir -p servers/mcow/monitoring/grafana/provisioning/datasources
       mkdir -p servers/mcow/monitoring/grafana/provisioning/dashboards/json
       mkdir -p servers/mcow/monitoring/alertmanager
       ```

    2. Create `secrets/mcow.sops.yaml` encrypted with age (use repo's existing age recipients from `.sops.yaml`):
       ```bash
       # Build plaintext
       cat > /tmp/mcow.env <<'EOF'
       GF_SECURITY_ADMIN_USER=admin
       GF_SECURITY_ADMIN_PASSWORD=<generate with: openssl rand -base64 24>
       TELEGRAM_BOT_TOKEN=<copy from: sops -d --extract '["TELEGRAM_BOT_TOKEN"]' secrets/docker-tower.sops.yaml>
       EOF
       sops --encrypt --input-type dotenv --output-type yaml /tmp/mcow.env > secrets/mcow.sops.yaml
       shred -u /tmp/mcow.env
       ```

    3. Write `servers/mcow/docker-compose.monitoring.yml` with this exact content (Grafana + Alertmanager only — cAdvisor stays in its own compose):
       ```yaml
       # Source: derived from servers/docker-tower/docker-compose.monitoring.yml
       # Grafana + Alertmanager on mcow (Tailnet 100.101.0.9 only).
       # Prometheus stays on docker-tower — this stack points at it over Tailnet.
       #
       # Secrets deploy flow (run on mcow before `docker compose up`):
       #   mkdir -p /run/secrets
       #   sops --decrypt --output-type dotenv /root/homelab/secrets/mcow.sops.yaml > /run/secrets/mcow.env
       #   chmod 600 /run/secrets/mcow.env
       #   source /run/secrets/mcow.env
       #   install -m 0600 -o root -g root /dev/stdin /run/secrets/grafana.env <<EOF
       #   GF_SECURITY_ADMIN_USER=$GF_SECURITY_ADMIN_USER
       #   GF_SECURITY_ADMIN_PASSWORD=$GF_SECURITY_ADMIN_PASSWORD
       #   EOF
       #   install -m 0440 -o root -g 65534 /dev/stdin /run/secrets/telegram_token <<<"$TELEGRAM_BOT_TOKEN"
       #
       # PERMS ARE LOAD-BEARING: telegram_token MUST be mode 0440 owner root group 65534
       # (prom/alertmanager runs as nobody:nogroup uid/gid 65534). 0600 will break with EACCES.
       # See servers/mcow/monitoring/alertmanager/README.md.

       services:

         grafana:
           image: grafana/grafana@sha256:2175aaa91c96733d86d31cf270d5310b278654b03f5718c59de12a865380a31f  # grafana/grafana:latest pinned 2026-04-14 (matches docker-tower)
           container_name: grafana
           restart: unless-stopped
           ports:
             - "100.101.0.9:3000:3000"
           env_file:
             - path: /run/secrets/grafana.env
               required: false
           environment:
             - GF_USERS_ALLOW_SIGN_UP=false
             - GF_SERVER_ROOT_URL=http://100.101.0.9:3000
             - GF_INSTALL_PLUGINS=grafana-piechart-panel
           volumes:
             - grafana-data:/var/lib/grafana
             - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
           networks:
             - monitoring

         alertmanager:
           image: prom/alertmanager@sha256:e13b6ed5cb929eeaee733479dce55e10eb3bc2e9c4586c705a4e8da41e5eacf5  # prom/alertmanager:v0.27.0 pinned 2026-04-14
           container_name: alertmanager
           restart: unless-stopped
           ports:
             - "100.101.0.9:9093:9093"
           volumes:
             - ./monitoring/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
             - /run/secrets/telegram_token:/etc/alertmanager/telegram_token:ro
             - alertmanager-data:/alertmanager
           command:
             - '--config.file=/etc/alertmanager/alertmanager.yml'
             - '--storage.path=/alertmanager'
             - '--web.listen-address=:9093'
           networks:
             - monitoring

       networks:
         monitoring:
           name: monitoring
           driver: bridge

       volumes:
         grafana-data:
         alertmanager-data:
       ```

       Note: Grafana uses bridge net (ports published on 100.101.0.9:3000) rather than host net, because voidnet-api occupies :8080 host-net; bridge avoids any future collisions. Alertmanager uses bridge net and publishes :9093 on the Tailnet IP so docker-tower Prometheus can reach it.

    4. Copy `servers/docker-tower/monitoring/alertmanager/alertmanager.yml` verbatim to `servers/mcow/monitoring/alertmanager/alertmanager.yml` (no changes — bot_token_file path `/etc/alertmanager/telegram_token`, chat_id 193835258, routes, HTML template all preserved).

    5. Write `servers/mcow/monitoring/grafana/provisioning/datasources/prometheus.yml`:
       ```yaml
       apiVersion: 1
       datasources:
         - name: Prometheus
           type: prometheus
           access: proxy
           url: http://100.101.0.8:9090
           isDefault: true
           uid: prometheus-homelab
           jsonData:
             timeInterval: '15s'
       ```
       UID pinned per D-02 — dashboard JSON in 04-02 will reference `prometheus-homelab`.

    6. Write `servers/mcow/monitoring/grafana/provisioning/datasources/alertmanager.yml`:
       ```yaml
       apiVersion: 1
       datasources:
         - name: Alertmanager
           type: alertmanager
           access: proxy
           url: http://alertmanager:9093
           uid: alertmanager-homelab
           jsonData:
             implementation: prometheus
             handleGrafanaManagedAlerts: false
       ```
       UID pinned as `alertmanager-homelab` for the active-alerts panel in 04-02. URL uses the docker network service name `alertmanager` because both containers share the `monitoring` bridge network.

    7. Write `servers/mcow/monitoring/grafana/provisioning/dashboards/dashboards.yml`:
       ```yaml
       apiVersion: 1
       providers:
         - name: homelab
           orgId: 1
           folder: 'Homelab'
           type: file
           disableDeletion: false
           updateIntervalSeconds: 30
           allowUiUpdates: false
           options:
             path: /etc/grafana/provisioning/dashboards/json
       ```
       (Empty `json/` dir for now — populated by 04-02.)

    8. Write `servers/mcow/monitoring/alertmanager/README.md` with the exact deploy flow (copy from `servers/docker-tower/monitoring/alertmanager/README.md` but change paths: `secrets/mcow.sops.yaml`, mcow compose path). Preserve the `install -m 0440 -o root -g 65534` line verbatim.

    9. Write `servers/mcow/monitoring/README.md` as an index explaining: Grafana + Alertmanager live here on mcow (migrated from docker-tower 2026-04-15); Prometheus remains on docker-tower; secrets flow summary.
  </action>
  <verify>
    <automated>
    cd /Users/admin/hub/workspace/homelab &amp;&amp; \
    test -f servers/mcow/docker-compose.monitoring.yml &amp;&amp; \
    test -f servers/mcow/monitoring/alertmanager/alertmanager.yml &amp;&amp; \
    test -f servers/mcow/monitoring/grafana/provisioning/datasources/prometheus.yml &amp;&amp; \
    test -f servers/mcow/monitoring/grafana/provisioning/datasources/alertmanager.yml &amp;&amp; \
    test -f secrets/mcow.sops.yaml &amp;&amp; \
    docker compose -f servers/mcow/docker-compose.monitoring.yml config --quiet &amp;&amp; \
    grep -q '0440 -o root -g 65534' servers/mcow/monitoring/alertmanager/README.md &amp;&amp; \
    grep -q 'bot_token_file: /etc/alertmanager/telegram_token' servers/mcow/monitoring/alertmanager/alertmanager.yml &amp;&amp; \
    grep -q 'chat_id: 193835258' servers/mcow/monitoring/alertmanager/alertmanager.yml &amp;&amp; \
    grep -q 'uid: prometheus-homelab' servers/mcow/monitoring/grafana/provisioning/datasources/prometheus.yml &amp;&amp; \
    grep -q 'uid: alertmanager-homelab' servers/mcow/monitoring/grafana/provisioning/datasources/alertmanager.yml &amp;&amp; \
    grep -q '100.101.0.9:3000:3000' servers/mcow/docker-compose.monitoring.yml &amp;&amp; \
    grep -q '100.101.0.9:9093:9093' servers/mcow/docker-compose.monitoring.yml &amp;&amp; \
    sops -d --extract '["TELEGRAM_BOT_TOKEN"]' secrets/mcow.sops.yaml | grep -qE '^[0-9]+:[A-Za-z0-9_-]{30,}$'
    </automated>
  </verify>
  <done>
    All 7 files + SOPS secret exist. `docker compose config` validates. Telegram token perms recipe preserved verbatim. Both datasource UIDs pinned. No 0.0.0.0 binds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy Grafana+Alertmanager on mcow; rewire Prometheus on docker-tower</name>
  <read_first>
    - servers/mcow/docker-compose.monitoring.yml (just-written)
    - servers/mcow/monitoring/alertmanager/README.md (perms recipe)
    - servers/docker-tower/monitoring/prometheus/prometheus.yml (change alertmanagers: target)
  </read_first>
  <action>
    Deploy mcow stack, rewire Prometheus, then stop (do not remove) docker-tower Grafana container.

    **Step A — Deploy mcow secrets + stack.** Run from operator:
    ```bash
    # 1. Sync repo to mcow
    rsync -aP --delete /Users/admin/hub/workspace/homelab/servers/mcow/ root@mcow:/root/homelab/servers/mcow/
    rsync -aP /Users/admin/hub/workspace/homelab/secrets/mcow.sops.yaml root@mcow:/root/homelab/secrets/mcow.sops.yaml

    # 2. On mcow, decrypt + install secrets + bring up stack
    ssh root@mcow bash -s <<'REMOTE'
    set -euo pipefail
    cd /root/homelab/servers/mcow
    mkdir -p /run/secrets

    sops --decrypt --output-type dotenv /root/homelab/secrets/mcow.sops.yaml > /run/secrets/mcow.env
    chmod 600 /run/secrets/mcow.env
    source /run/secrets/mcow.env

    install -m 0600 -o root -g root /dev/stdin /run/secrets/grafana.env <<EOF
    GF_SECURITY_ADMIN_USER=$GF_SECURITY_ADMIN_USER
    GF_SECURITY_ADMIN_PASSWORD=$GF_SECURITY_ADMIN_PASSWORD
    EOF

    # LOAD-BEARING: 0440 root:65534 — prom/alertmanager runs as uid/gid 65534
    install -m 0440 -o root -g 65534 /dev/stdin /run/secrets/telegram_token <<<"$TELEGRAM_BOT_TOKEN"

    docker compose -f docker-compose.monitoring.yml up -d
    REMOTE
    ```
    Expected output: `Container grafana Started`, `Container alertmanager Started`. Stop condition: any non-zero exit on compose up.

    **Step B — Verify both services healthy.**
    ```bash
    # Give containers 20s to boot
    for i in 1 2 3 4 5; do
      sleep 4
      if curl -sf http://100.101.0.9:3000/api/health >/dev/null && curl -sf http://100.101.0.9:9093/-/healthy >/dev/null; then
        echo OK; break
      fi
    done
    curl -s http://100.101.0.9:3000/api/health | jq .   # expect: {"database":"ok","version":"..."}
    curl -s http://100.101.0.9:9093/api/v2/status | jq -r .config.original | head -5   # expect config with bot_token_file
    ssh root@mcow 'stat -c "%a %U:%G" /run/secrets/telegram_token'   # expect: 440 root:nogroup  (gid 65534 = nogroup)
    ```
    Stop condition: either endpoint not healthy after 20s, or telegram_token perms != 440.

    **Step C — Rewire Prometheus on docker-tower.** Edit `servers/docker-tower/monitoring/prometheus/prometheus.yml` — change the `alerting:` block:
    ```yaml
    alerting:
      alertmanagers:
        - static_configs:
            - targets: ['100.101.0.9:9093']   # Alertmanager on mcow (moved 2026-04-15; was localhost:9093)
    ```
    Then:
    ```bash
    rsync -aP /Users/admin/hub/workspace/homelab/servers/docker-tower/monitoring/prometheus/prometheus.yml \
      root@docker-tower:/opt/homestack/prometheus/prometheus.yml
    # Hot-reload Prometheus (no container restart needed)
    ssh root@docker-tower 'curl -sX POST http://localhost:9090/-/reload'
    sleep 3
    # Verify new AM discovered
    curl -s http://100.101.0.8:9090/api/v1/alertmanagers | \
      jq -r '.data.activeAlertmanagers[0].url'
    # expect: http://100.101.0.9:9093/api/v2/alerts
    ```
    Stop condition: activeAlertmanagers[0].url does not contain `100.101.0.9:9093`.

    **Step D — Decommission docker-tower Grafana.** Edit `servers/docker-tower/docker-compose.monitoring.yml` — wrap the entire `grafana:` service stanza (lines 39-58 in current file) in a comment block like:
    ```yaml
      # ── Grafana — DECOMMISSIONED 2026-04-15 ─────────────────────────────────────
      # Migrated to mcow (100.101.0.9:3000). See servers/mcow/docker-compose.monitoring.yml.
      # Volume `grafana-data` preserved for 1 week rollback window.
      # If rollback needed within that window: uncomment + `docker compose up -d grafana`.
      # After 2026-04-22: remove this block and run `docker volume rm homestack_grafana-data`.
      # grafana:
      #   image: grafana/grafana@sha256:2175aaa...
      #   ...
    ```
    Also comment out the same-named `alertmanager:` service stanza with the same decommission banner (migrated to mcow; volume preserved).

    Deploy the edit:
    ```bash
    rsync -aP /Users/admin/hub/workspace/homelab/servers/docker-tower/docker-compose.monitoring.yml \
      root@docker-tower:/opt/homestack/docker-compose.monitoring.yml
    ssh root@docker-tower 'cd /opt/homestack &amp;&amp; docker compose -f docker-compose.monitoring.yml up -d --remove-orphans'
    # This stops+removes the now-absent grafana and alertmanager containers; prometheus + cadvisor remain.
    ssh root@docker-tower 'docker ps --format "{{.Names}}"' | sort
    # expect ONLY: cadvisor, prometheus  (NOT grafana, NOT alertmanager)
    ssh root@docker-tower 'docker volume ls --format "{{.Name}}"' | grep -E 'grafana-data|alertmanager-data'
    # expect both volumes still present (rollback window)
    ```
    Stop condition: `grafana` or `alertmanager` still in `docker ps`, OR volumes missing.

    **Step E — Update docker-tower alertmanager README** `servers/docker-tower/monitoring/alertmanager/README.md` — prepend a banner:
    ```markdown
    > **DECOMMISSIONED 2026-04-15** — Alertmanager moved to mcow.
    > See `servers/mcow/monitoring/alertmanager/README.md` for the live deploy flow.
    > Volume `alertmanager-data` on docker-tower preserved until 2026-04-22 for rollback.
    ```
  </action>
  <verify>
    <automated>
    cd /Users/admin/hub/workspace/homelab &amp;&amp; \
    curl -sf http://100.101.0.9:3000/api/health | jq -re '.database == "ok"' &amp;&amp; \
    curl -sf http://100.101.0.9:9093/-/healthy &amp;&amp; \
    [ "$(ssh root@mcow 'stat -c %a /run/secrets/telegram_token')" = "440" ] &amp;&amp; \
    [ "$(ssh root@mcow 'stat -c %g /run/secrets/telegram_token')" = "65534" ] &amp;&amp; \
    curl -s http://100.101.0.8:9090/api/v1/alertmanagers | jq -re '.data.activeAlertmanagers[0].url | contains("100.101.0.9:9093")' &amp;&amp; \
    ! ssh root@docker-tower 'docker ps --format "{{.Names}}"' | grep -qE '^(grafana|alertmanager)$' &amp;&amp; \
    ssh root@docker-tower 'docker volume ls --format "{{.Name}}"' | grep -q 'grafana-data' &amp;&amp; \
    grep -q 'DECOMMISSIONED 2026-04-15' servers/docker-tower/monitoring/alertmanager/README.md &amp;&amp; \
    grep -qE "targets:\s*\['100\.101\.0\.9:9093'\]" servers/docker-tower/monitoring/prometheus/prometheus.yml
    </automated>
  </verify>
  <done>
    Grafana + Alertmanager running on mcow. Telegram token perms 440/nogroup verified. Prometheus on docker-tower shows mcow as active alertmanager. docker-tower grafana + alertmanager containers stopped, volumes retained for rollback. Both docker-tower README and compose file show decommission banner.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → mcow | Grafana and Alertmanager MUST NOT be reachable from outside Tailnet |
| git repo → SOPS | Plaintext creds must never land in a committed file |
| SOPS → runtime | `/run/secrets/` is the only place plaintext lives; perms gate access |
| Alertmanager container → Telegram API | Outbound HTTPS only; bot token read from file, never env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | Grafana port binding | mitigate | Bind `100.101.0.9:3000:3000` (Tailnet IP only) — compose string MUST start with `100.101.0.9:`. Verified by grep in acceptance criteria. |
| T-04-01-02 | Information Disclosure | Alertmanager port binding | mitigate | Bind `100.101.0.9:9093:9093` (Tailnet IP only). Verified by grep. |
| T-04-01-03 | Information Disclosure | Telegram bot token in compose/config | mitigate | `bot_token_file` (not inline); token sourced from SOPS; `/run/secrets/telegram_token` mode `0440 root:65534`. Regression to 0600 is a known container-EACCES failure (see 03-05 SUMMARY) — acceptance criteria enforces 440. |
| T-04-01-04 | Information Disclosure | Grafana admin creds | mitigate | `GF_SECURITY_ADMIN_*` sourced from `/run/secrets/grafana.env` (mode 0600), NEVER in compose literals. `env_file:` path is `/run/secrets/grafana.env`. No `GF_SECURITY_ADMIN_PASSWORD=...` string anywhere in repo. |
| T-04-01-05 | Elevation of Privilege | Grafana sign-up | mitigate | `GF_USERS_ALLOW_SIGN_UP=false` (literal in compose). |
| T-04-01-06 | Tampering | Prometheus datasource URL | mitigate | Fixed to `http://100.101.0.8:9090` (internal Tailnet). UID pinned `prometheus-homelab` to survive migration. |
| T-04-01-07 | Denial of Service | Prometheus alertmanagers rewire window | accept | 1-2s hot-reload window where alerts silently drop if firing. Acceptable: no active firings expected during planned migration; smoke test in 04-03 confirms delivery post-rewire. |
| T-04-01-08 | Repudiation | Volume deletion of old Grafana state | mitigate | Volumes PRESERVED for 7 days; banner in compose + README records decommission date + deletion date. |

</threat_model>

<verification>
1. `docker compose -f servers/mcow/docker-compose.monitoring.yml config --quiet` → exit 0
2. `curl -sf http://100.101.0.9:3000/api/health | jq -r .database` → `ok`
3. `curl -sf http://100.101.0.9:9093/-/healthy` → 200
4. `ssh root@mcow 'stat -c %a /run/secrets/telegram_token'` → `440`
5. `ssh root@mcow 'stat -c %g /run/secrets/telegram_token'` → `65534`
6. `curl -s http://100.101.0.8:9090/api/v1/alertmanagers | jq -r '.data.activeAlertmanagers[0].url'` → contains `100.101.0.9:9093`
7. `ssh root@docker-tower 'docker ps --format "{{.Names}}"'` → does NOT contain `grafana` or `alertmanager`
8. `ssh root@docker-tower 'docker volume ls'` → contains `grafana-data` and `alertmanager-data`
9. Grafana datasource test: `curl -s -u admin:$GF_PW http://100.101.0.9:3000/api/datasources/uid/prometheus-homelab/health | jq -r .status` → `OK`
10. Grafana datasource test: `curl -s -u admin:$GF_PW http://100.101.0.9:3000/api/datasources/uid/alertmanager-homelab/health | jq -r .status` → `OK`
</verification>

<success_criteria>
- Grafana and Alertmanager running on mcow, Tailnet-bound, SOPS-sourced creds
- Prometheus on docker-tower sends alerts to mcow Alertmanager
- docker-tower Grafana + Alertmanager containers stopped; volumes preserved 1 week
- Telegram bot token installed at `/run/secrets/telegram_token` mode `0440 root:65534` (NOT 0600)
- Both Grafana datasources (prometheus-homelab, alertmanager-homelab) health=OK
</success_criteria>

<output>
After completion, create `.planning/phases/04-operator-dashboard/04-01-SUMMARY.md`.
</output>
