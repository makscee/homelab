---
phase: 19-proxmox-ops-readonly
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - apps/admin/app/(auth)/proxmox/page.tsx
  - apps/admin/app/(auth)/proxmox/proxmox-list.client.tsx
  - apps/admin/app/(auth)/proxmox/[vmid]/page.tsx
  - apps/admin/app/(auth)/proxmox/[vmid]/proxmox-detail.client.tsx
  - apps/admin/app/(auth)/layout.tsx
  - apps/admin/e2e/proxmox-list.spec.ts
  - apps/admin/e2e/proxmox-detail.spec.ts
autonomous: false
requirements: [PROXMOX-01, PROXMOX-05]
user_setup: []
must_haves:
  truths:
    - "/proxmox renders a table with ≥3 LXC rows (CT 100 docker-tower, CT 101 jellyfin, CT 204 cc-worker minimum)"
    - "Each row shows: vmid, hostname, status badge (running/stopped), cpu config, mem config, uptime"
    - "List polls every 10s; manual refresh button present"
    - "/proxmox/[vmid] renders config dump (collapsible), recent tasks table (click-to-expand log), network info block (parsed net0)"
    - "Detail polls every 30s; manual refresh button present"
    - "Tower-down state shows red error banner with actionable message; layout remains usable"
    - "Nav link to /proxmox added to main auth layout"
    - "No console errors and no 5xx responses in normal operation"
    - "Visual layout matches existing /audit and /alerts pages (shadcn components, same spacing/typography)"
  artifacts:
    - path: "apps/admin/app/(auth)/proxmox/page.tsx"
      provides: "Server component — initial fetch of /api/proxmox/lxcs + render shell"
    - path: "apps/admin/app/(auth)/proxmox/proxmox-list.client.tsx"
      provides: "Client component — 10s setInterval poll + error banner + refresh button"
    - path: "apps/admin/app/(auth)/proxmox/[vmid]/page.tsx"
      provides: "Server component — initial detail fetch + recent tasks fetch"
    - path: "apps/admin/app/(auth)/proxmox/[vmid]/proxmox-detail.client.tsx"
      provides: "Client component — 30s poll + click-to-expand task log"
    - path: "apps/admin/e2e/proxmox-list.spec.ts"
      provides: "Playwright smoke: page loads, ≥3 rows, no console errors"
    - path: "apps/admin/e2e/proxmox-detail.spec.ts"
      provides: "Playwright smoke: detail panel opens for CT 101"
  key_links:
    - from: "proxmox/page.tsx"
      to: "/api/proxmox/lxcs"
      via: "server-side fetch"
      pattern: "fetch.*api/proxmox/lxcs"
    - from: "proxmox-list.client.tsx"
      to: "/api/proxmox/lxcs"
      via: "setInterval(10000) + fetch"
      pattern: "setInterval.*10000"
    - from: "proxmox/[vmid]/page.tsx"
      to: "/api/proxmox/lxcs/[vmid] + /api/proxmox/lxcs/[vmid]/tasks"
      via: "parallel server-side fetch"
      pattern: "fetch.*api/proxmox/lxcs"
    - from: "(auth)/layout.tsx"
      to: "/proxmox"
      via: "nav link"
      pattern: "href=.{1,3}/proxmox"
---

<objective>
Build the operator-facing UI for Proxmox observability: list page at `/proxmox` and detail page at `/proxmox/[vmid]`. Wire polling, error banners, nav link. Verify with Playwright smoke and grep gates. Visual parity with existing /audit and /alerts pages.

Purpose: Per D-04/D-06/D-09 — read-only observability surface; remains usable when tower is unreachable; matches existing dashboard look-and-feel.
Output: Two pages live on `homelab.makscee.ru`, Playwright smoke green, NODE_TLS_REJECT_UNAUTHORIZED grep clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-proxmox-ops-readonly/19-CONTEXT.md
@.planning/phases/19-proxmox-ops-readonly/19-RESEARCH.md
@.planning/phases/19-proxmox-ops-readonly/19-02-SUMMARY.md
@apps/admin/app/(auth)/audit/page.tsx
@apps/admin/app/(auth)/alerts/page.tsx
@apps/admin/app/(auth)/layout.tsx
@apps/admin/components/ui/table.tsx
@apps/admin/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build /proxmox list page (server + client) + nav link</name>
  <files>apps/admin/app/(auth)/proxmox/page.tsx, apps/admin/app/(auth)/proxmox/proxmox-list.client.tsx, apps/admin/app/(auth)/layout.tsx</files>
  <action>
    Per D-06 — pages live under `apps/admin/app/(auth)/`. Mirror the existing `/audit` and `/alerts` page structure verbatim (Server Component fetches initial data, Client Component handles polling + interactive state).

    1. **page.tsx (Server Component):**
       - Fetch `/api/proxmox/lxcs` server-side using internal URL pattern (same approach as `/audit/page.tsx` — read PORT/host from env or use relative path with absolute origin from headers).
       - Render shell with title "Proxmox LXCs (tower)", page header, and pass initial data + any initial error to `<ProxmoxList initialData={...} initialError={...} />`.
       - On fetch failure (502 PVE_UNREACHABLE), pass `initialError={code: 'PVE_UNREACHABLE'}` rather than throwing — page must render with banner, not 500.

    2. **proxmox-list.client.tsx (Client Component, "use client"):**
       - State: `data`, `error`, `lastFetched` (Date).
       - `useEffect` with `setInterval(refetch, 10000)` per D-04. Cleanup on unmount.
       - Manual "Refresh now" button → triggers refetch + resets interval.
       - Red error banner (shadcn `Alert` or matching component used in `/alerts`) on error: "Tower unreachable via Tailnet — check tower status" for PVE_UNREACHABLE; generic message otherwise. Banner does NOT replace the table — show last-known-good data + banner above per D-09.
       - Table columns (use `components/ui/table.tsx`): VMID, Hostname, Status (Badge: green/running, gray/stopped), CPUs, Mem (formatted MiB→GiB), Uptime (humanized), Link to detail (`/proxmox/{vmid}`).
       - Last-fetched timestamp shown in subtle text near refresh button.

    3. **(auth)/layout.tsx:**
       - Add nav link `<Link href="/proxmox">Proxmox</Link>` alongside existing /audit, /alerts, /tokens links. Match exact styling of siblings (read existing layout file before editing).

    Use existing shadcn components from Phase 15 — do NOT introduce new UI primitives. Use `tailwind-merge` if combining classes (per Phase 15 stack).
  </action>
  <verify>
    <automated>cd apps/admin && bun run lint && bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>Lint + build clean; page renders in dev server (`bun run dev`) and shows actual LXC table when tower reachable.</done>
</task>

<task type="auto">
  <name>Task 2: Build /proxmox/[vmid] detail page (server + client)</name>
  <files>apps/admin/app/(auth)/proxmox/[vmid]/page.tsx, apps/admin/app/(auth)/proxmox/[vmid]/proxmox-detail.client.tsx</files>
  <action>
    Per D-08 — detail panel: config dump (collapsible), recent tasks (click-to-expand log), network info.

    1. **page.tsx (Server Component):**
       - Receives `params: { vmid: string }`.
       - Parallel `Promise.all` server-side fetch of `/api/proxmox/lxcs/{vmid}` and `/api/proxmox/lxcs/{vmid}/tasks`.
       - On error (PVE_UNREACHABLE or invalid vmid), render banner-only state with breadcrumb back to /proxmox.
       - Pass initial data to `<ProxmoxDetail vmid={vmid} initial={...} />`.

    2. **proxmox-detail.client.tsx ("use client"):**
       - State: `detail`, `tasks`, `expandedUpid` (string | null), `taskLogs` (Map<upid, string[]>), `error`, `lastFetched`.
       - `useEffect` with `setInterval(refetch, 30000)` per D-04. Refetches detail + tasks; does NOT auto-refetch already-expanded log (only on user re-expand).
       - **Network info block** at top: parsed net0 fields (name, bridge, hwaddr, ip, gw) in a key-value grid. If a field is null, render "—" (defensive per A3).
       - **Config dump** in `<details>` element (collapsed by default): pretty-printed key/value table of all PVE config fields.
       - **Recent tasks table:** columns: starttime (humanized), type, status (Badge), user. Each row clickable → toggles inline log expansion. On expand, fetch `/api/proxmox/lxcs/{vmid}/tasks/{encodeURIComponent(upid)}/log` once, cache in `taskLogs` map, render `<pre>` with the log lines. Show spinner while fetching.
       - Manual refresh button + last-fetched timestamp.
       - Red banner on detail-fetch failure (same pattern as Task 1).

    Reuse existing Badge/Table/Alert components. Do not install xterm.js or anything fancy — that's Phase 21.
  </action>
  <verify>
    <automated>cd apps/admin && bun run lint && bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>Detail page renders for CT 101 in dev; config dump expands; clicking a task row reveals its log.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy + Playwright MCP smoke + grep gate + visual parity check</name>
  <files>apps/admin/e2e/proxmox-list.spec.ts, apps/admin/e2e/proxmox-detail.spec.ts</files>
  <action>
    1. **Deploy:** `cd ansible && ansible-playbook -i inventory/homelab.yml playbooks/deploy-homelab-admin.yml`. Verify /api/health 200.

    2. **Grep gate (PROXMOX-06 success criterion):**
       Run: `grep -r "NODE_TLS_REJECT_UNAUTHORIZED" apps/admin/ ansible/` — MUST return empty.

    3. **Write Playwright specs** (mirror existing `apps/admin/e2e/*.spec.ts` setup if present, else add minimal config):
       - `proxmox-list.spec.ts`: Auth via existing test session helper → goto `/proxmox` → assert page title visible → assert table has ≥3 rows → assert rows include vmid 100, 101, 204 (text content) → assert no console errors collected → assert no 5xx in network log.
       - `proxmox-detail.spec.ts`: goto `/proxmox/101` → assert "Network" block visible → assert net0 fields render (ip should match `10.10.20.11/24` per Phase 17.1 SUMMARY or current value) → assert "Recent Tasks" table renders → click first task row → assert `<pre>` log block appears → no console errors, no 5xx.

    4. **Run Playwright via MCP** (per project memory feedback_test_with_playwright + feedback_playwright_screenshot_path):
       - Drive browser via Playwright MCP against `https://homelab.makscee.ru` (Tailnet).
       - Use Playwright MCP `browser_take_screenshot` with explicit out-of-repo path (`/tmp/proxmox-list-19-03.png`, `/tmp/proxmox-detail-19-03.png`) per memory note.
       - Also screenshot `/audit` and `/alerts` for visual parity comparison (`/tmp/audit-19-03.png`, `/tmp/alerts-19-03.png`). Inspect screenshots manually: spacing, typography, badge colors, table density should match.

    5. **Specs as automated regression:** commit the .spec.ts files even if MCP smoke is the primary gate — they serve as future-run guardrails.
  </action>
  <verify>
    <automated>cd apps/admin && (! grep -r "NODE_TLS_REJECT_UNAUTHORIZED" . 2>/dev/null) && bun run build</automated>
  </verify>
  <done>Deploy clean; grep gate empty; Playwright MCP smoke green for both pages; screenshots show visual parity with /audit and /alerts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator visual sign-off on /proxmox + /proxmox/101</name>
  <what-built>
    - `/proxmox` list page with ≥3 LXC rows (CT 100, 101, 204+), 10s polling, manual refresh, status badges
    - `/proxmox/[vmid]` detail page with parsed network info, collapsible config dump, recent tasks with click-to-expand log, 30s polling
    - Nav link added to main auth layout
    - All read-only; no power buttons, no spawn/destroy
  </what-built>
  <how-to-verify>
    1. Visit https://homelab.makscee.ru/proxmox — confirm:
       - Table loads within ~2s
       - At least CT 100 (docker-tower), CT 101 (jellyfin), CT 204 (cc-worker) visible with running status
       - Status badges color-correct (green=running, gray=stopped)
       - Wait 10s — table should silently refresh (uptime increments)
       - Click "Refresh now" — immediate refetch
    2. Click into CT 101 → confirm:
       - Network block shows IP (likely 10.10.20.11/24), bridge vmbr0, MAC
       - Config dump expands when clicked
       - Recent Tasks table populated; click first row → log lines appear in `<pre>` block
       - 30s passes — page refreshes silently
    3. Visual check: layout, spacing, font sizes match `/audit` and `/alerts` (open all three in adjacent tabs)
    4. Resilience check (optional): if you want to test tower-down banner, briefly block tower:8006 from mcow (`ssh root@mcow 'iptables -A OUTPUT -d 100.101.0.7 -p tcp --dport 8006 -j DROP'`); reload /proxmox; confirm red banner appears with "Tower unreachable" message and table shows last-known-good data; then `iptables -D ...` to restore.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → mcow Next.js | NextAuth session cookie (existing) |
| client component → /api/proxmox/* | same-origin fetch with credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Information Disclosure | Token leaks via client-side fetch | mitigate | Client only ever calls own /api/proxmox/*; server proxy never serializes secrets to response (D-07, inherited from Plan 02) |
| T-19-13 | Tampering | Client manipulates vmid path to access unintended LXC | accept | Token role is read-only across all LXCs (D-03); no privilege escalation possible |
| T-19-14 | DoS | Aggressive polling overwhelms tower | mitigate | 10s list / 30s detail (D-04); single-tab assumption acceptable for solo operator dashboard |
| T-19-15 | Spoofing | XSS via unescaped PVE config values | mitigate | React auto-escapes; render config as text in cells, never `dangerouslySetInnerHTML` |
</threat_model>

<verification>
- `bun run build` clean
- `! grep -r "NODE_TLS_REJECT_UNAUTHORIZED" apps/admin/ ansible/` returns empty
- Playwright MCP: /proxmox loads with ≥3 LXC rows including CT 100/101/204
- Playwright MCP: /proxmox/101 detail panel opens; tasks expand to logs
- No console errors, no 5xx in normal operation
- Visual parity vs /audit and /alerts (operator sign-off in checkpoint)
- Tower-down state: red banner, layout intact (D-09)
</verification>

<success_criteria>
- /proxmox renders live LXC list with auto-refresh
- /proxmox/[vmid] renders config + tasks + network with auto-refresh
- Click-to-expand task log works
- Nav link present in main layout
- Repo grep clean of NODE_TLS_REJECT_UNAUTHORIZED
- Playwright smoke green
- Operator visually approves layout parity
- Tower-down case shows banner without breaking layout
</success_criteria>

<output>
After completion, create `.planning/phases/19-proxmox-ops-readonly/19-03-SUMMARY.md` with:
- Final URLs verified live (/proxmox, /proxmox/101)
- Playwright spec file paths + last run result
- Screenshot paths (/tmp/proxmox-list-19-03.png etc.)
- Operator sign-off note from checkpoint
- Grep gate result (must be empty)
</output>
