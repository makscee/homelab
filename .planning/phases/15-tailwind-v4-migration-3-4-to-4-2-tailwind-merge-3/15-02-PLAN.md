---
phase: 15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/admin/package.json
  - apps/admin/bun.lock
autonomous: false
requirements:
  - D-3
  - D-5
user_setup: []

must_haves:
  truths:
    - "tailwind-merge is pinned at ^3.0.0 in apps/admin/package.json"
    - "apps/admin builds successfully after the bump (bun run build exits 0)"
    - "cn() helper in lib/utils.ts continues to work unchanged (no signature or API break)"
    - "No visual regression on focus rings, dialogs, dropdowns, tooltips, sonner toasts"
    - "No console errors on /, /audit, /tokens, /alerts"
  artifacts:
    - path: "apps/admin/package.json"
      provides: "tailwind-merge v3 dep"
      contains: "\"tailwind-merge\": \"^3"
    - path: "apps/admin/lib/utils.ts"
      provides: "cn() helper (unchanged)"
      contains: "twMerge"
  key_links:
    - from: "apps/admin/lib/utils.ts"
      to: "tailwind-merge v3 runtime"
      via: "import { twMerge } from 'tailwind-merge'"
      pattern: "from ['\"]tailwind-merge['\"]"
    - from: "tailwind-merge v3 class-group tables"
      to: "Tailwind v4 utility set (from Plan 15-01)"
      via: "v3 of tailwind-merge ships v4-aware class groups"
      pattern: "tailwind-merge.*3"
---

<objective>
Bump `tailwind-merge` from v2.5 to v3.0 in apps/admin, per locked decision D-3 (fix-on-break, no pre-audit). 15-01 delivered Tailwind v4.2 — tailwind-merge v3 is the paired upgrade that ships v4-aware class-group tables. Because our codebase has a single `cn()` helper in `lib/utils.ts` with no `extendTailwindMerge` or custom validators (per 15-RESEARCH.md §tailwind-merge v3 Breaking Changes), the surface area is near zero. This plan: bump the dep, build, Playwright-verify the class-merge hotspots (conflicting utility combinations in focus rings and state variants), and fix only if something breaks.

Purpose: Close the v4 migration by aligning the class-merger with the utility set it operates on. Running tailwind-merge v2 against Tailwind v4 would mismerge (per research §tailwind-merge v3 Breaking Changes #2).

Output: tailwind-merge ^3.0.0, build green, visual parity confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3/15-CONTEXT.md
@.planning/phases/15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3/15-RESEARCH.md
@.planning/phases/15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3/15-01-SUMMARY.md

@apps/admin/package.json
@apps/admin/lib/utils.ts

<interfaces>
<!-- Current cn() helper — MUST remain unchanged per D-5 (upgrade-only, no refactors). -->
<!-- From apps/admin/lib/utils.ts: -->

```ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<!-- v3 breaking changes per 15-RESEARCH.md that DO NOT affect us: -->
<!-- - ClassValidator signature change (we have no custom validators) -->
<!-- - ClassGroup shape change (we have no custom groups) -->
<!-- - createTailwindMerge / extendTailwindMerge API tweaks (we don't call them) -->
<!-- - Deprecated join/mergeConfigs removed (we don't use them) -->
<!-- The only surface is the default twMerge() import, which preserves its call signature. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump tailwind-merge to ^3.0.0 and verify build</name>
  <files>apps/admin/package.json, apps/admin/bun.lock</files>
  <action>
Per D-3 (fix-on-break, no pre-audit):

1. Edit `apps/admin/package.json`: change `"tailwind-merge": "^2.5.0"` (or whatever 2.x pin is present) to `"tailwind-merge": "^3.0.0"`. No other edits.
2. Run from `apps/admin/`:
   ```bash
   bun install
   ```
   Confirm `bun.lock` updates with tailwind-merge 3.x entry.

3. Run `bun run build`. It MUST pass. If the TypeScript build errors on the `twMerge` import or `cn()` signature:
   - Check 15-RESEARCH.md §tailwind-merge v3 Breaking Changes — ClassValidator and ClassGroup signature changes apply ONLY to code that calls `extendTailwindMerge`/`createTailwindMerge`. Our `cn()` does not.
   - If the error is `ClassValue` type conflict from clsx, verify clsx version; do NOT change clsx in this plan (out of scope D-5).
   - If truly broken in a way the `cn()` helper can't absorb → surface and stop.

4. Do NOT touch `lib/utils.ts` unless v3 literally fails to import. Per D-5 (upgrade-only), the cn() helper stays identical.

Commit: `chore(admin): bump tailwind-merge to v3 (15-02)`
  </action>
  <verify>
    <automated>cd apps/admin &amp;&amp; grep -E '"tailwind-merge":\s*"\^3' package.json &amp;&amp; bun run build</automated>
  </verify>
  <done>package.json pins tailwind-merge ^3; bun.lock reflects v3.x; `bun run build` exits 0; lib/utils.ts unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Playwright visual spot-check on class-merge hotspots + fix-on-break</name>
  <what-built>
tailwind-merge v3 is installed and the build is green. Per 15-RESEARCH.md §tailwind-merge v3, the only realistic regression mode is class-group drift: v3 knows Tailwind v4's class set (including `outline-hidden`, `ring-3`, `shadow-xs`), while v2 did not. Running v2 against v4 would have mismerged — v3 fixes that. This checkpoint confirms nothing mismerges in the opposite direction on our actual code.
  </what-built>
  <how-to-verify>
Claude spins up the dev server in the background:
```bash
cd apps/admin &amp;&amp; bun run dev
```

Then Claude drives Playwright MCP to hit the class-merge hotspots. The 64 `cn()` call sites across 22 files all use static string literals (per 15-RESEARCH.md §tailwind-merge v3 Codebase Surface), so regressions would appear as conflicting-utility mismerges — typically visible as "both old and new classes present" or "wrong utility won."

**Pages (navigate and screenshot):**
1. `/` (Overview) — cards, host tiles, AlertsCard, ClaudeSummary
2. `/audit` — audit table, filter controls
3. `/tokens` — token list, gauges, action buttons
4. `/alerts` — alert list, severity badges

**Conflict-merge hotspots (Claude triggers, user eyeballs):**
5. **Focus ring stacking** — tab to a Button, Input, Select, Dropdown trigger. Ring class typically comes from base (`ring-3 ring-ring`) plus overrides in variants. v3 must pick the last one. Red flag: doubled rings or no ring at all.
6. **State-variant conflicts** — open a Dialog, Dropdown, or Tooltip. Components use `data-[state=open]:bg-accent` style variants layered with base colors. Red flag: open-state bg leaks to closed state or vice versa.
7. **Size variants on Button** — compare sm/default/lg buttons side-by-side if available on the page. Red flag: padding from two size tokens both apply.
8. **Badge color variants** — severity badges on /alerts or audit. Red flag: color mismatch vs pre-bump.

**Fix-on-break protocol (per D-3):** If a mismerge surfaces, Claude:
- Isolates the call site (grep for the specific conflicting class combo)
- Applies the minimum fix: either reorder classes (trailing class wins in tailwind-merge) or restructure the cn() call so the intended class comes last
- Does NOT add `extendTailwindMerge` config or custom groups (out of scope per D-5 — would be a refactor)
- If fix would require anything bigger than a class reorder → surface as a backlog item (999.x in ROADMAP) and keep the regression visible to the user for a call on accept vs block

**Expected outcome:** Zero mismerges. The class-merge surface here is static-literal-only; v3's default groups cover everything we use.
  </how-to-verify>
  <resume-signal>Type "approved" if visuals match. Describe the element + page + specific class conflict if any mismerge is visible.</resume-signal>
  <files>(verification only — no file modifications in this task; fix-on-break class reorders may touch individual component files under apps/admin/components or app/)</files>
  <action>Start dev server, drive Playwright MCP through pages and class-merge conflict hotspots listed in <how-to-verify>, capture screenshots, and request human approval. On mismerge, apply minimum fix (class reorder in the offending cn() call) per D-3 fix-on-break; do NOT add extendTailwindMerge per D-5.</action>
  <verify><automated>MISSING — human visual verification required; mismerge regressions cannot be asserted by automated command</automated></verify>
  <done>User types "approved"; no visual or console regression across the four pages and merge hotspots; any mismerge resolved by minimum in-scope class reorder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | tailwind-merge is a build-time/runtime className utility; no trust boundary change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | tailwind-merge v3 dep | accept | Popular well-maintained package (dcastil/tailwind-merge); pin is `^3.0.0`, bun.lock captures exact resolved version |
| T-15-04 | Denial of Service | runtime className merging | mitigate | Task 1 requires `bun run build` green before checkpoint; Task 2 human-verify catches runtime visual regressions |
</threat_model>

<verification>
- `grep -E '"tailwind-merge":\s*"\^3' apps/admin/package.json` matches
- `bun run build` exits 0 from `apps/admin/`
- `lib/utils.ts` unchanged (diff vs main shows no modification)
- Human-verify checkpoint approved: no visual or console regression across `/`, `/audit`, `/tokens`, `/alerts` and on focus / dialog / dropdown / tooltip / sonner hotspots
</verification>

<success_criteria>
- tailwind-merge bumped to ^3.0.0 per D-3
- No cn() refactor or extendTailwindMerge config introduced (D-5 upgrade-only honored)
- Build passes, site renders with visual parity
- Phase 15 scope (Tailwind v4 + tailwind-merge v3) fully shipped across 15-01 and 15-02
</success_criteria>

<output>
After completion, create `.planning/phases/15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3/15-02-SUMMARY.md`
</output>
