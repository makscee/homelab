---
phase: 15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/package.json
  - apps/admin/postcss.config.mjs
  - apps/admin/tailwind.config.ts
  - apps/admin/app/globals.css
  - apps/admin/bun.lock
autonomous: false
requirements:
  - D-1
  - D-2
  - D-4
  - D-5
user_setup: []

must_haves:
  truths:
    - "apps/admin builds successfully on Tailwind v4.2 (bun run build exits 0)"
    - "Dev server renders / , /audit, /tokens, /alerts with no console errors"
    - "No JS Tailwind config file remains in apps/admin"
    - "autoprefixer is not installed and not referenced in PostCSS config"
    - "Global `* { @apply border-border }` rule preserves v3 default-border behavior under v4"
    - "shadcn CSS-var-in-HSL color mapping continues to resolve (bg-background, text-foreground, border-border)"
    - "Focus rings, dropdowns, dialog shadows, and tooltip shadows render pixel-equivalent to pre-upgrade"
  artifacts:
    - path: "apps/admin/package.json"
      provides: "Tailwind v4 deps + autoprefixer removal"
      contains: "\"tailwindcss\": \"^4"
    - path: "apps/admin/app/globals.css"
      provides: "CSS-first @theme config + @import tailwindcss + preserved base layer"
      contains: "@import \"tailwindcss\""
    - path: "apps/admin/postcss.config.mjs"
      provides: "Simplified v4 PostCSS pipeline"
      contains: "@tailwindcss/postcss"
  key_links:
    - from: "apps/admin/app/globals.css"
      to: "shadcn CSS vars (--background, --foreground, --border, --ring, --radius)"
      via: "@theme --color-* = hsl(var(--*)) mapping"
      pattern: "hsl\\(var\\(--(background|foreground|border|ring)"
    - from: "apps/admin/app/globals.css"
      to: "v4 default-border compat"
      via: "@layer base { * { @apply border-border } }"
      pattern: "@apply border-border"
    - from: "apps/admin/postcss.config.mjs"
      to: "@tailwindcss/postcss plugin"
      via: "plugins export"
      pattern: "@tailwindcss/postcss"
---

<objective>
Migrate apps/admin from Tailwind v3.4 to v4.2 using the official `@tailwindcss/upgrade` codemod plus the CSS-first config model (per D-1, D-2, D-4, D-5). This plan covers the core migration: run codemod, clean up the @theme block, delete the JS config, drop autoprefixer, verify build, and human-verify visual fidelity on the shadcn primitives that are the class-rename hotspots (outline-none → outline-hidden, shadow/rounded shifts, ring defaults).

Purpose: Ship the v4 upgrade with zero visual regression. Scope is strictly upgrade-only — no refactors, no new utilities (D-5).

Output: apps/admin builds on v4.2, renders pixel-equivalent, tailwind.config.ts deleted, autoprefixer removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3/15-CONTEXT.md
@.planning/phases/15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3/15-RESEARCH.md

@apps/admin/package.json
@apps/admin/postcss.config.mjs
@apps/admin/tailwind.config.ts
@apps/admin/app/globals.css
@apps/admin/lib/utils.ts

<interfaces>
<!-- CSS-first @theme skeleton (target post-codemod shape — use as reference if codemod mangles HSL-var mapping). -->
<!-- Full skeleton is in 15-RESEARCH.md §CSS-First @theme Skeleton. Key invariants: -->

```css
@import "tailwindcss";
@custom-variant dark (&:where(.dark, .dark *));

@theme {
  --color-background: hsl(var(--background));
  --color-foreground: hsl(var(--foreground));
  --color-card: hsl(var(--card));
  --color-card-foreground: hsl(var(--card-foreground));
  --color-popover: hsl(var(--popover));
  --color-popover-foreground: hsl(var(--popover-foreground));
  --color-primary: hsl(var(--primary));
  --color-primary-foreground: hsl(var(--primary-foreground));
  --color-secondary: hsl(var(--secondary));
  --color-secondary-foreground: hsl(var(--secondary-foreground));
  --color-muted: hsl(var(--muted));
  --color-muted-foreground: hsl(var(--muted-foreground));
  --color-accent: hsl(var(--accent));
  --color-accent-foreground: hsl(var(--accent-foreground));
  --color-destructive: hsl(var(--destructive));
  --color-destructive-foreground: hsl(var(--destructive-foreground));
  --color-border: hsl(var(--border));
  --color-input: hsl(var(--input));
  --color-ring: hsl(var(--ring));
  --radius-lg: var(--radius);
  --radius-md: calc(var(--radius) - 2px);
  --radius-sm: calc(var(--radius) - 4px);
}

@layer base {
  :root { /* existing --background, --foreground, ..., --radius definitions preserved verbatim */ }
  * { @apply border-border; }
  body { @apply bg-background text-foreground; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run @tailwindcss/upgrade codemod + commit raw diff</name>
  <files>apps/admin/package.json, apps/admin/postcss.config.mjs, apps/admin/tailwind.config.ts, apps/admin/app/globals.css, apps/admin/bun.lock, and any *.{ts,tsx,css} files the codemod rewrites (shadcn primitives under apps/admin/components/ui/*, focus-ring consumers)</files>
  <action>
From `apps/admin/`, run the official codemod per D-4:

```bash
cd apps/admin
npx @tailwindcss/upgrade@latest --force
```

The codemod will (per 15-RESEARCH.md §Codemod Behavior):
- Bump `tailwindcss` to v4 in package.json and install `@tailwindcss/postcss`
- Rewrite `postcss.config.mjs` (replace `tailwindcss` + `autoprefixer` entries with `@tailwindcss/postcss`)
- Migrate `tailwind.config.ts` → `@theme` block inside `app/globals.css`, then delete `tailwind.config.ts`
- Rewrite `@tailwind base/components/utilities` → `@import "tailwindcss";`
- Rewrite renamed utilities across `*.{ts,tsx,css}`: `shadow-sm`→`shadow-xs`, `shadow`→`shadow-sm`, `rounded-sm`→`rounded-xs`, `outline-none`→`outline-hidden`, `ring`→`ring-3`, etc.

Do NOT hand-edit anything in this task. Commit the codemod output verbatim (per D-4: "commit output as-is, then manual adjustments only where the codemod leaves gaps").

Run `bun install` to sync lockfile if the codemod didn't. Do NOT run build yet — Task 2 handles manual cleanup before build verification.

Review the diff with `git diff --stat` for surprises, but do not revert anything. If the codemod fails to complete (error mid-run), stop and surface — do not half-commit.

Commit with: `chore(admin): apply @tailwindcss/upgrade codemod (15-01)`
  </action>
  <verify>
    <automated>cd apps/admin &amp;&amp; test ! -f tailwind.config.ts &amp;&amp; grep -q '@tailwindcss/postcss' postcss.config.mjs &amp;&amp; grep -q '@import "tailwindcss"' app/globals.css</automated>
  </verify>
  <done>tailwind.config.ts deleted; postcss.config.mjs references @tailwindcss/postcss; globals.css starts with @import "tailwindcss"; package.json has tailwindcss ^4.x; codemod output committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Manual cleanup — @theme sanity + autoprefixer removal + border-border compat</name>
  <files>apps/admin/package.json, apps/admin/postcss.config.mjs, apps/admin/app/globals.css</files>
  <behavior>
    - `package.json` has no `autoprefixer` entry in dependencies or devDependencies (per D-2)
    - `postcss.config.mjs` exports exactly `{ plugins: { "@tailwindcss/postcss": {} } }` (no `tailwindcss`, no `autoprefixer`)
    - `app/globals.css` contains `@import "tailwindcss"` as the first directive
    - `app/globals.css` `@theme` block maps shadcn CSS vars to Tailwind color tokens via `hsl(var(--X))` wrapping (matches skeleton in 15-RESEARCH.md §CSS-First @theme Skeleton)
    - `app/globals.css` preserves `@layer base { * { @apply border-border; } body { @apply bg-background text-foreground; } }` (v4 changed default border color from `gray-200` to `currentColor`; this rule restores v3 behavior per 15-RESEARCH.md)
    - `bun run build` succeeds with zero Tailwind/PostCSS errors
  </behavior>
  <action>
Cleanup pass on the codemod output. Do not add new utilities or patterns (D-5) — only ensure the v4 target shape matches the skeleton.

1. **package.json** — remove `autoprefixer` from `devDependencies` if the codemod left it (per D-2). Verify `tailwindcss` is `^4.x` and `@tailwindcss/postcss` is present. Run `bun install` after edit.

2. **postcss.config.mjs** — ensure exact shape:
   ```js
   export default { plugins: { "@tailwindcss/postcss": {} } };
   ```
   Remove any leftover `tailwindcss: {}` or `autoprefixer: {}` entries.

3. **app/globals.css** — compare against skeleton in 15-RESEARCH.md §CSS-First @theme Skeleton:
   - If codemod mangled the `hsl(var(--X))` wrapping on theme colors, replace the @theme block with the skeleton's mapping (keeping all var names used by the existing shadcn theme).
   - Verify `@layer base { * { @apply border-border; } }` is present. If codemod omitted it, add it (required — v4 default border changed to currentColor; without this rule, every Card/Input/Separator renders with a different border color).
   - Verify `@custom-variant dark (&:where(.dark, .dark *));` exists (preserves shadcn `class` dark-mode strategy).
   - Preserve the `:root` block with `--background`, `--foreground`, ..., `--radius` definitions verbatim — do not alter HSL values (D-5 upgrade-only).

4. Run `bun install` to ensure lockfile is synced post-package.json edit.

5. Run `bun run build` from `apps/admin/`. It MUST succeed. If it fails:
   - PostCSS errors → check postcss.config.mjs shape
   - Unknown utility errors → check @theme mappings resolve (e.g., `border-border` requires `--color-border` in @theme)
   - Do NOT add new plugins or workarounds; fix the config shape to match the skeleton.

Commit: `feat(admin): complete Tailwind v4 CSS-first cleanup (15-01)`
  </action>
  <verify>
    <automated>cd apps/admin &amp;&amp; ! grep -q autoprefixer package.json &amp;&amp; grep -c '@tailwindcss/postcss' postcss.config.mjs &amp;&amp; grep -q '@apply border-border' app/globals.css &amp;&amp; bun run build</automated>
  </verify>
  <done>autoprefixer absent from package.json; postcss.config.mjs minimal; globals.css has border-border compat rule + hsl-var @theme mapping; `bun run build` exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify visual fidelity on shadcn primitives</name>
  <what-built>
apps/admin now runs on Tailwind v4.2 with CSS-first config, autoprefixer removed, and the codemod-rewritten utility classes (outline-none → outline-hidden, shadow-sm → shadow-xs, ring → ring-3, rounded-sm → rounded-xs). Per 15-RESEARCH.md, visual regressions — if any — will surface on shadcn UI primitives: focus rings, dialog/dropdown/tooltip shadows, sonner toasts.
  </what-built>
  <how-to-verify>
Before starting, Claude spins up the dev server in the background:
```bash
cd apps/admin &amp;&amp; bun run dev
```

Then Claude drives Playwright MCP (per user memory: `Test UI with Playwright`) to spot-check each hotspot and captures screenshots for the user to compare against pre-upgrade baseline if available.

**Pages to verify (Claude runs via Playwright, user eyeballs result):**
1. `/` (Overview) — host tiles, AlertsCard, ClaudeSummary — check card borders, shadows, spacing
2. `/audit` — table borders, row hover, pagination controls
3. `/tokens` — token list, gauge bars, action buttons (focus rings)
4. `/alerts` — alert rows, severity badges

**Interaction hotspots (Claude triggers, user verifies):**
5. Click a button with keyboard focus → focus ring visible (was `ring-2 ring-ring ring-offset-2`; now `ring-3` per codemod). Must still appear as a solid ring, not a stray outline.
6. Open a dropdown menu (topbar user menu) → shadow on the popover content. Was `shadow-md`, now `shadow-sm`. Must still have a visible drop-shadow.
7. Open a dialog (if any available on current pages) → backdrop + shadow on dialog card.
8. Hover a tooltip-triggering element → tooltip shadow + rounded corners.
9. Trigger a sonner toast (if trivially triggerable) → toast shadow + rounded corners.

**Expected outcome:** All the above render with visually equivalent styling to v3. Borders on Cards/Inputs/Separators must use the shadcn `border` color (not `currentColor` — that would indicate the `* { @apply border-border }` compat rule is missing).

**Red flags (block approval):**
- Bare `border` utilities showing wrong color (currentColor leak) → Task 2 border-border rule missing
- Focus ring invisible or displaced → `outline-hidden` vs `outline-none` issue
- Dialog/dropdown with no shadow → shadow class rename not applied
- Console errors on any page → build succeeded but runtime CSS broken

If any red flag appears, Claude fixes in-task (manual edit in globals.css or targeted class tweak — upgrade-only scope, no refactors) and re-runs the check.
  </how-to-verify>
  <resume-signal>Type "approved" if visuals match. Describe the specific element + page if any regression is visible.</resume-signal>
  <files>(verification only — no file modifications in this task; fix-on-break edits may touch apps/admin/app/globals.css)</files>
  <action>Start dev server, drive Playwright MCP through pages / interaction hotspots listed in <how-to-verify>, capture screenshots, and request human approval. On red-flag regression, apply minimum fix in apps/admin/app/globals.css (scope-bound: no refactors per D-5) and re-verify.</action>
  <verify><automated>MISSING — human visual verification required; no automated substitute for shadcn focus ring / shadow pixel fidelity</automated></verify>
  <done>User types "approved"; all four pages render without visual or console regression; any red flags resolved by minimum in-scope fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Phase 15 is a build-toolchain upgrade; no new request/response paths, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | node_modules install via codemod | accept | `npx @tailwindcss/upgrade` is a first-party Tailwind Labs tool; codemod output reviewed via git diff before commit |
| T-15-02 | Denial of Service | build pipeline | mitigate | `bun run build` must pass in Task 2 before human checkpoint; broken build is caught pre-merge |
</threat_model>

<verification>
- `bun run build` succeeds from `apps/admin/`
- `test ! -f apps/admin/tailwind.config.ts` (JS config deleted, D-1)
- `! grep -q autoprefixer apps/admin/package.json` (D-2)
- `grep -q '@tailwindcss/postcss' apps/admin/postcss.config.mjs`
- `grep -q '@import "tailwindcss"' apps/admin/app/globals.css`
- `grep -q '@apply border-border' apps/admin/app/globals.css` (v4 default-border compat)
- Human-verify checkpoint approved: `/`, `/audit`, `/tokens`, `/alerts` render with no visual regression on focus rings, dialog/dropdown shadows, card borders
</verification>

<success_criteria>
- Tailwind v3.4 → v4.2 migration complete per D-1 (CSS-first), D-2 (no autoprefixer), D-4 (codemod-driven), D-5 (upgrade-only, no refactors)
- Production build passes on v4
- Visual parity with pre-upgrade state confirmed by human-verify
- Zero new dependencies introduced beyond `@tailwindcss/postcss` (codemod-added)
- `tailwind.config.ts` deleted
</success_criteria>

<output>
After completion, create `.planning/phases/15-tailwind-v4-migration-3-4-to-4-2-tailwind-merge-3/15-01-SUMMARY.md`
</output>
