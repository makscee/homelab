---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/inventory.yml
  - ansible/playbooks/node-exporter.yml
  - ansible/playbooks/cadvisor.yml
  - ansible/group_vars/all.yml
  - servers/docker-tower/monitoring/prometheus/targets/nodes.yml
  - servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml
autonomous: false
requirements: [DASH-01]
tags: [ansible, prometheus, monitoring, node-exporter, cadvisor]

must_haves:
  truths:
    - "node_exporter on :9100 responds on all 6 Tailnet hosts (tower, docker-tower, cc-worker, mcow, nether, animaya-dev)"
    - "cAdvisor on Docker hosts (docker-tower, mcow) responds with container_last_seen metric"
    - "Prometheus on docker-tower scrapes job='node' and job='cadvisor' with up=1 for all targets"
    - "Exporters bind to Tailscale IP only, not 0.0.0.0"
  artifacts:
    - path: "ansible/playbooks/cadvisor.yml"
      provides: "cAdvisor Docker container provisioning"
      contains: "google/cadvisor OR gcr.io/cadvisor/cadvisor"
    - path: "ansible/inventory.yml"
      provides: "docker_hosts group with docker-tower + mcow"
      contains: "docker_hosts:"
    - path: "servers/docker-tower/monitoring/prometheus/targets/nodes.yml"
      provides: "All 6 host node_exporter targets"
      contains: "100.119.15.122:9100"
  key_links:
    - from: "Prometheus (docker-tower)"
      to: "node_exporter :9100 on 6 hosts"
      via: "file_sd scrape job='node'"
      pattern: "up\\{job=\"node\"\\}"
    - from: "Prometheus (docker-tower)"
      to: "cAdvisor on Docker hosts"
      via: "file_sd scrape job='cadvisor'"
      pattern: "up\\{job=\"cadvisor\"\\}"
---

<objective>
Provision node_exporter on all 6 Tailnet hosts and cAdvisor on Docker hosts (docker-tower + mcow), extend Prometheus file_sd targets, and verify `up=1` for every exporter. This is the data source for the /overview host tiles (DASH-01) in Plan 04.

Purpose: DASH-01 requires live CPU/mem/disk/container-count per host. Research confirmed vendored Ansible roles + target files exist; this plan is a verify-and-gap-fill for missing hosts (notably animaya-dev which was added 2026-04-14 after last provisioning).
Output: All 6 hosts emitting node_exporter metrics; docker-tower + mcow emitting cAdvisor metrics; Prometheus scraping all of them cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-global-overview-audit-log/14-CONTEXT.md
@.planning/phases/14-global-overview-audit-log/14-RESEARCH.md
@CLAUDE.md
@ansible/inventory.yml
@ansible/playbooks/node-exporter.yml
@servers/docker-tower/monitoring/prometheus/targets/nodes.yml
@servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml

<interfaces>
Hosts (from CLAUDE.md):
  tower         100.101.0.7    Proxmox host        (no Docker)
  docker-tower  100.101.0.8    Media stack LXC     (Docker)
  cc-worker     100.99.133.9   CC runner LXC       (no Docker per A4)
  mcow          100.101.0.9    VoidNet+admin KVM   (Docker)
  nether        100.101.0.3    VPN entry/exit      (no Docker)
  animaya-dev   100.119.15.122 Animaya dev LXC     (no Docker)

Vendored Ansible collection (verified in research):
  ansible/collections/ansible_collections/prometheus/prometheus/roles/node_exporter
  ansible/collections/ansible_collections/prometheus/prometheus/roles/cadvisor

Role variables of interest:
  node_exporter_version    (target 1.11.1)
  node_exporter_web_listen_address  (set to "{{ tailscale_ip }}:9100")
  cadvisor_version         (target v0.56.1)
  cadvisor_listen_address  ("{{ tailscale_ip }}:8080")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend inventory + targets + node-exporter playbook to cover all 6 hosts</name>
  <files>
    ansible/inventory.yml,
    ansible/group_vars/all.yml,
    ansible/playbooks/node-exporter.yml,
    servers/docker-tower/monitoring/prometheus/targets/nodes.yml,
    servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml
  </files>
  <read_first>
    ansible/inventory.yml,
    ansible/playbooks/node-exporter.yml,
    ansible/group_vars/all.yml,
    servers/docker-tower/monitoring/prometheus/targets/nodes.yml,
    servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml,
    .planning/phases/14-global-overview-audit-log/14-CONTEXT.md (§Host Metrics Provisioning D-01..D-03),
    .planning/phases/14-global-overview-audit-log/14-RESEARCH.md (§Runtime State Inventory + §Environment Availability)
  </read_first>
  <action>
    Implements D-01 and D-03.

    1. In `ansible/inventory.yml`:
       - Ensure every host is present with `ansible_host` = Tailscale IP and a `tailscale_ip` host_var equal to that IP (used by role for `web_listen_address`):
         tower (100.101.0.7), docker-tower (100.101.0.8), cc-worker (100.99.133.9), mcow (100.101.0.9), nether (100.101.0.3), animaya-dev (100.119.15.122).
       - Ensure a group `node_exporter_hosts` (or reuse `all`) contains all 6 hosts.
       - Create a `docker_hosts` group containing `docker-tower` and `mcow` ONLY (per D-02, cc-worker excluded per research Assumption A4).

    2. In `ansible/group_vars/all.yml` add (or confirm):
       `node_exporter_version: "1.11.1"`
       `node_exporter_web_listen_address: "{{ tailscale_ip }}:9100"`
       `cadvisor_version: "v0.56.1"`
       Do NOT bump any existing pinned version (Bun 1.1.38, Next.js 15.5.15) — only add new keys.

    3. In `ansible/playbooks/node-exporter.yml`:
       - Target group covering all 6 hosts (e.g. `hosts: all` or `hosts: node_exporter_hosts`).
       - Uses role `prometheus.prometheus.node_exporter` with `node_exporter_web_listen_address: "{{ tailscale_ip }}:9100"` so bind is Tailnet-only (CLAUDE.md Tailscale-only constraint + SEC-03 pattern).
       - Keep idempotent; no `--check` regressions.

    4. In `servers/docker-tower/monitoring/prometheus/targets/nodes.yml` ensure the `targets:` list contains EXACTLY these 6 entries (add any missing, notably `100.119.15.122:9100` for animaya-dev):
       `100.101.0.7:9100, 100.101.0.8:9100, 100.99.133.9:9100, 100.101.0.9:9100, 100.101.0.3:9100, 100.119.15.122:9100`.
       Preserve existing labels structure; add per-target `labels.hostname` matching inventory name (tower/docker-tower/cc-worker/mcow/nether/animaya-dev) so Prometheus carries a stable hostname label.

    5. In `servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml` ensure ONLY `100.101.0.8:8080` (docker-tower) and `100.101.0.9:8080` (mcow) are present with `labels.hostname` set accordingly. Do not include cc-worker (A4).

    6. Do NOT touch existing `claude-usage` scrape config (D-03 untouched rule).
  </action>
  <acceptance_criteria>
    - `grep -c '9100' servers/docker-tower/monitoring/prometheus/targets/nodes.yml` reports exactly 6.
    - `grep '100.119.15.122:9100' servers/docker-tower/monitoring/prometheus/targets/nodes.yml` matches (animaya-dev present).
    - `grep -c '8080' servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml` reports exactly 2.
    - `grep -E '^\s*docker_hosts:' ansible/inventory.yml` matches (group exists).
    - `grep 'node_exporter_web_listen_address.*tailscale_ip.*9100' ansible/group_vars/all.yml` OR equivalent matches (Tailnet bind).
    - `cd ansible && ansible-inventory --list --yaml | grep -c 'ansible_host'` reports >=6.
    - `cd ansible && ansible-playbook --syntax-check playbooks/node-exporter.yml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd ansible && ansible-playbook --syntax-check playbooks/node-exporter.yml && grep -c '9100' ../servers/docker-tower/monitoring/prometheus/targets/nodes.yml | grep -qx 6 && grep -q 'docker_hosts:' inventory.yml</automated>
  </verify>
  <done>All 6 hosts listed; docker_hosts group defined with 2 hosts; playbook syntax valid; Tailnet bind var set.</done>
</task>

<task type="auto">
  <name>Task 2: Create cAdvisor playbook targeting docker_hosts only</name>
  <files>ansible/playbooks/cadvisor.yml</files>
  <read_first>
    ansible/playbooks/deploy-docker-tower.yml (pattern reference for community.docker usage),
    ansible/playbooks/node-exporter.yml,
    .planning/phases/14-global-overview-audit-log/14-RESEARCH.md (§Ansible cAdvisor play, §Pitfall 3 — PromQL instance label drift)
  </read_first>
  <action>
    Implements D-02.

    Create `ansible/playbooks/cadvisor.yml` as a standalone playbook with `hosts: docker_hosts` (group from Task 1, contains docker-tower + mcow ONLY).

    Use `community.docker.docker_container` to run:
      name: cadvisor
      image: gcr.io/cadvisor/cadvisor:v0.56.1
      state: started
      restart_policy: unless-stopped
      ports: ["{{ tailscale_ip }}:8080:8080"]   # Tailnet-only bind — CRITICAL (CLAUDE.md)
      volumes:
        - /:/rootfs:ro
        - /var/run:/var/run:ro
        - /sys:/sys:ro
        - /var/lib/docker/:/var/lib/docker:ro
        - /dev/disk/:/dev/disk:ro

    Add pre_task that asserts `tailscale_ip is defined` to fail loudly if inventory is missing the var.

    Do NOT use `prometheus.prometheus.cadvisor` role — the inline `docker_container` module gives us explicit Tailnet-bind control and matches the mcow KVM + docker-tower LXC patterns already in use in `deploy-docker-tower.yml`.

    Do NOT bind on port 0.0.0.0; if the container must be addressable for debugging, it is via `ssh mcow` + `curl http://100.101.0.9:8080/metrics` (Tailnet).
  </action>
  <acceptance_criteria>
    - `test -f ansible/playbooks/cadvisor.yml` — file exists.
    - `grep 'hosts: docker_hosts' ansible/playbooks/cadvisor.yml` matches (scoped to group).
    - `grep 'gcr.io/cadvisor/cadvisor:v0.56.1' ansible/playbooks/cadvisor.yml` matches (pinned version per research §Standard Stack).
    - `grep '{{ tailscale_ip }}:8080:8080' ansible/playbooks/cadvisor.yml` matches (Tailnet bind, NOT 0.0.0.0).
    - `grep -E 'ports:\s*$' ansible/playbooks/cadvisor.yml` followed within 3 lines by the tailscale_ip binding (no `"8080:8080"` alone).
    - `cd ansible && ansible-playbook --syntax-check playbooks/cadvisor.yml` exits 0.
    - `grep -c -- '- /.*:ro' ansible/playbooks/cadvisor.yml` >= 5 (all read-only host mounts present).
  </acceptance_criteria>
  <verify>
    <automated>cd ansible && ansible-playbook --syntax-check playbooks/cadvisor.yml && grep -q 'hosts: docker_hosts' playbooks/cadvisor.yml && grep -q '{{ tailscale_ip }}:8080:8080' playbooks/cadvisor.yml && ! grep -E '"0\.0\.0\.0:8080' playbooks/cadvisor.yml</automated>
  </verify>
  <done>cadvisor.yml exists, syntax-clean, targets docker_hosts only, binds to Tailnet IP only, pinned image version.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator deploys exporters and verifies Prometheus scrape</name>
  <what-built>Ansible playbooks `node-exporter.yml` and `cadvisor.yml` ready to deploy; Prometheus target files updated.</what-built>
  <action>Operator-driven verification of previously-built artifacts; see <how-to-verify> for exact steps. No additional build work in this task.</action>
  <how-to-verify>
    Operator action (one-shot; Claude cannot auth for you):

    1. From `ansible/` run:
       `ansible-playbook -i inventory.yml playbooks/node-exporter.yml`
       expected: 6 hosts ok=changed|ok, no failed=.
    2. Run:
       `ansible-playbook -i inventory.yml playbooks/cadvisor.yml`
       expected: 2 hosts (docker-tower, mcow) ok, no failed=.
    3. Re-run both with `--check`; expected all tasks `ok` (idempotent), no changes.
    4. Reload Prometheus on docker-tower (it picks up file_sd automatically; else `curl -X POST http://100.101.0.8:9090/-/reload`).
    5. In browser or curl, hit `http://100.101.0.8:9090/api/v1/query?query=up{job="node"}` — expect 6 results all `"value":[...,"1"]`.
    6. Hit `http://100.101.0.8:9090/api/v1/query?query=up{job="cadvisor"}` — expect 2 results all `"1"`.
    7. From outside Tailnet (e.g. phone on mobile data), attempt `curl http://<public-ip>:9100/metrics` to animaya-dev's public IP — MUST fail / time out (Tailnet bind confirmed).
    8. Spot-check: `curl -s http://100.101.0.9:8080/metrics | grep -c '^container_last_seen'` from a Tailnet host — should be >0.

    Type "approved" to proceed to wave 2, or paste any failed targets / error output.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → exporter | node_exporter and cAdvisor expose raw host metrics; if bound to 0.0.0.0, any internet scanner can read them |
| Tailnet host → Prometheus | Trusted (Tailscale authenticated); scrape traffic not further authenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Information Disclosure | node_exporter bind | mitigate | `web_listen_address: "{{ tailscale_ip }}:9100"` — explicit Tailnet-only bind in group_vars (Task 1) |
| T-14-01-02 | Information Disclosure | cAdvisor bind | mitigate | Docker port mapping `"{{ tailscale_ip }}:8080:8080"` not `"8080:8080"` or `"0.0.0.0:8080:8080"` (Task 2) |
| T-14-01-03 | Tampering | Ansible role supply chain | accept | Vendored `prometheus.prometheus` collection in-tree; cAdvisor image version pinned to v0.56.1 |
| T-14-01-04 | Denial of Service | Prometheus scrape drift | mitigate | File_sd targets explicitly enumerated; labels include hostname so missing targets are visually obvious |
| T-14-01-05 | Elevation of Privilege | cAdvisor host mounts | accept | All host mounts `:ro` (read-only); container runs per `community.docker.docker_container` default (no --privileged) |
</threat_model>

<verification>
- All 6 hosts respond to `up{job="node"}` with value 1.
- docker-tower + mcow respond to `up{job="cadvisor"}` with value 1.
- No exporter listens on 0.0.0.0 (external scan confirms).
- `--check` run reports zero changed tasks (idempotent).
</verification>

<success_criteria>
node_exporter deployed to all 6 hosts on Tailnet IP; cAdvisor on docker-tower + mcow on Tailnet IP; Prometheus scrape file_sd lists current; `up` == 1 across the board; playbooks idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/14-global-overview-audit-log/14-01-SUMMARY.md`
</output>
