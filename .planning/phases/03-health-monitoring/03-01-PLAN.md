---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/docker-tower/monitoring/prometheus/prometheus.yml
  - servers/docker-tower/monitoring/prometheus/targets/nodes.yml
  - servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml
  - servers/docker-tower/monitoring/prometheus/alerts/homelab.yml
  - servers/docker-tower/monitoring/prometheus/alerts/README.md
  - scripts/tests/phase-03/lib.sh
  - scripts/tests/phase-03/smoke.sh
  - scripts/tests/phase-03/suite.sh
  - scripts/tests/phase-03/01-prometheus-config.sh
  - scripts/README.md
autonomous: true
requirements: [MON-01]
must_haves:
  truths:
    - "The live Prometheus config running on docker-tower is captured verbatim in the repo before any edits"
    - "A committed prometheus.yml uses file_sd targets and references an alertmanager on localhost:9093"
    - "Scrape target files list all 6 Tailnet hosts at :9100 and 2 cAdvisor targets at :8080"
    - "Alert rules file is syntactically valid per promtool check rules"
    - "A phase-03 test harness (smoke/suite/lib) exists and exits 0 against the committed configs"
    - "Wave 0 environment checks (promtool, amtool, shellcheck, yamllint) are recorded as satisfied"
  artifacts:
    - path: servers/docker-tower/monitoring/prometheus/prometheus.yml
      provides: "Prometheus scrape + alerting config"
      contains: "file_sd_configs"
      min_lines: 20
    - path: servers/docker-tower/monitoring/prometheus/targets/nodes.yml
      provides: "file_sd target list for node-exporter job"
      contains: "100.101.0.7:9100"
    - path: servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml
      provides: "file_sd target list for cadvisor job"
      contains: "100.101.0.8:8080"
    - path: servers/docker-tower/monitoring/prometheus/alerts/homelab.yml
      provides: "Alert rules for host/disk/memory/containers/meta"
      contains: "HostDown"
    - path: scripts/tests/phase-03/smoke.sh
      provides: "Fast config-only validation harness for phase 03"
      contains: "promtool check"
    - path: scripts/tests/phase-03/suite.sh
      provides: "Full validation harness (smoke + Tailnet probes)"
      contains: "smoke.sh"
  key_links:
    - from: servers/docker-tower/monitoring/prometheus/prometheus.yml
      to: /etc/prometheus/targets/nodes.yml
      via: "file_sd_configs files: entry"
      pattern: "file_sd_configs"
    - from: servers/docker-tower/monitoring/prometheus/prometheus.yml
      to: /etc/prometheus/alerts/*.yml
      via: "rule_files directive"
      pattern: "rule_files"
    - from: scripts/tests/phase-03/smoke.sh
      to: scripts/tests/phase-03/lib.sh
      via: "source statement"
      pattern: "source.*lib.sh"
---

<objective>
Capture the currently running Prometheus config from docker-tower into the repo without drift, then commit the full Prometheus configuration tree (prometheus.yml + file_sd targets + alert rules) that subsequent plans will deploy. Also seed the Phase 03 validation harness (`scripts/tests/phase-03/`) so every later task has an automated verify command.

Purpose: Close Risk 4 (no prometheus.yml in repo) and Wave 0 gaps. Establish the contract that later plans migrate toward.
Output: committed prometheus tree + phase-03 test harness scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-health-monitoring/03-CONTEXT.md
@.planning/phases/03-health-monitoring/03-RESEARCH.md
@.planning/phases/03-health-monitoring/03-VALIDATION.md
@CLAUDE.md
@servers/docker-tower/docker-compose.monitoring.yml
@scripts/README.md
@scripts/verify-phase02.sh
</context>

<interfaces>
<!-- Tailnet IP table — sourced from CLAUDE.md. Executor must use these exactly. -->
| Host | Tailnet IP | Role |
|------|-----------|------|
| tower | 100.101.0.7 | Proxmox host |
| docker-tower | 100.101.0.8 | LXC 100, monitoring host |
| cc-worker | 100.99.133.9 | LXC 204 |
| mcow | 100.101.0.9 | VoidNet |
| nether | 100.101.0.3 | VPN |
| animaya-dev | 100.119.15.122 | LXC 205 |

<!-- Path contract: deployed Prometheus on docker-tower expects these mounts -->
- Container: /etc/prometheus/prometheus.yml → repo ./prometheus/prometheus.yml
- Container: /etc/prometheus/targets/ → repo ./prometheus/targets/
- Container: /etc/prometheus/alerts/ → repo ./prometheus/alerts/
</interfaces>

<wave_0>

<task type="auto">
  <name>Task 0a: Verify operator tooling and PVE firewall gate</name>
  <files>scripts/tests/phase-03/00-env-check.sh</files>
  <action>
    Create `scripts/tests/phase-03/00-env-check.sh` that fails fast if Wave 0 prerequisites are not met.

    The script MUST:
    1. `set -euo pipefail`
    2. Verify the following binaries are on PATH (print version and pass, else fail with install hint):
       - `promtool` (install: `apt-get install prometheus` OR `docker run --rm -v $PWD:/x prom/prometheus:v2.53.0 promtool ...`)
       - `amtool` (install: `apt-get install prometheus-alertmanager` OR docker run prom/alertmanager)
       - `shellcheck` (install: `apt-get install shellcheck`)
       - `yamllint` (install: `pip install yamllint` or apt)
       - `jq`, `curl`, `ssh`
    3. Run `ssh -o ConnectTimeout=5 root@tower "pve-firewall status && iptables -L INPUT -n | grep -E '9100|ACCEPT' || true"` and print the output. Do NOT fail — record the state into `/tmp/phase-03-pve-firewall.txt` for the operator to review manually. If output shows firewall is `enabled` AND no rule permits TCP 9100 from `100.64.0.0/10`, print `WARNING: PVE firewall may block 9100 — see A2 in RESEARCH.md`.
    4. Run `ssh -o ConnectTimeout=5 root@animaya-dev "command -v python3 || apt-get install -y python3"` to confirm Python3 exists for Ansible (Plan 03-02 prerequisite per A6). Print version; do NOT auto-install without user confirmation — print the apt command to run.
    5. Exit 0 only if all binary checks pass.

    Mark the script executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash scripts/tests/phase-03/00-env-check.sh</automated>
  </verify>
  <done>Script exists, runs cleanly, reports all 4 binary tools present; PVE firewall and animaya-dev Python state captured to stdout. Exits 0 on a properly set up operator machine.</done>
</task>

<task type="auto">
  <name>Task 0b: SSH-capture the live prometheus.yml from docker-tower</name>
  <files>
    servers/docker-tower/monitoring/prometheus/_captured/prometheus.yml.live
    servers/docker-tower/monitoring/prometheus/_captured/README.md
  </files>
  <action>
    Before any edits, snapshot the live config so downstream changes can be diffed against reality.

    1. `mkdir -p servers/docker-tower/monitoring/prometheus/_captured`
    2. Run: `ssh root@docker-tower 'cat /opt/homestack/monitoring/prometheus/prometheus.yml' > servers/docker-tower/monitoring/prometheus/_captured/prometheus.yml.live`
       Stop condition: if SSH fails or the file is empty (<20 bytes), STOP and report the failure — do NOT write placeholder content.
    3. Also capture any existing alerts file: `ssh root@docker-tower 'cat /opt/homestack/monitoring/prometheus/alerts.yml 2>/dev/null || echo "# no alerts file present"' > servers/docker-tower/monitoring/prometheus/_captured/alerts.yml.live`
    4. Write `servers/docker-tower/monitoring/prometheus/_captured/README.md` explaining: these files are captured snapshots of the live state as of the capture date, retained ONLY for diff/forensic purposes. They are not consumed at deploy time. The canonical config is `../prometheus.yml` (committed in Task 2).
  </action>
  <verify>
    <automated>test -s servers/docker-tower/monitoring/prometheus/_captured/prometheus.yml.live &amp;&amp; yamllint -d '{extends: relaxed, rules: {line-length: disable}}' servers/docker-tower/monitoring/prometheus/_captured/prometheus.yml.live</automated>
  </verify>
  <done>Live prometheus.yml captured non-empty; alerts.yml captured (or noted absent); README explains the directory's forensic-only purpose.</done>
</task>

</wave_0>

<tasks>

<task type="auto">
  <name>Task 1: Write phase-03 test harness scaffold (smoke/suite/lib)</name>
  <files>
    scripts/tests/phase-03/lib.sh
    scripts/tests/phase-03/smoke.sh
    scripts/tests/phase-03/suite.sh
    scripts/tests/phase-03/01-prometheus-config.sh
  </files>
  <read_first>
    - scripts/verify-phase02.sh (style reference for wrapper)
    - scripts/verify-phase02.d/ (snippet layout reference)
    - .planning/phases/03-health-monitoring/03-VALIDATION.md (Per-Task Verification Map — this harness underpins every verify block in later plans)
  </read_first>
  <action>
    Establish the harness every subsequent Phase 03 task uses as its `&lt;automated&gt;` verify command.

    Directory layout:
    ```
    scripts/tests/phase-03/
    ├── lib.sh                     # shared helpers (sourced by every snippet)
    ├── smoke.sh                   # runs every 0N-*.sh and NN-*.sh (config-only, offline)
    ├── suite.sh                   # runs smoke.sh then remote curl probes over Tailnet
    ├── 00-env-check.sh            # (already written in Task 0a)
    └── 01-prometheus-config.sh    # validates committed prometheus.yml + targets + alerts
    ```

    `lib.sh` — `set -euo pipefail`; define:
    - `PHASE_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." &amp;&amp; pwd)"`
    - `MON_ROOT="$PHASE_ROOT/servers/docker-tower/monitoring"`
    - `PROM_URL="http://100.101.0.8:9090"`
    - `AM_URL="http://100.101.0.8:9093"`
    - functions: `ok()`, `fail()`, `info()` (colored stdout), `assert_file()`, `assert_cmd()` (asserts a binary is on PATH), `prom_query_must_succeed()` (curls PROM_URL and jq-asserts `.status=="success"`).

    `smoke.sh` — `source lib.sh`; iterate `scripts/tests/phase-03/[0-9][0-9]-*.sh` in lexical order excluding `00-env-check.sh` (env-check is a Wave 0 gate, not part of the sampling harness). Each snippet runs `set -e`; any non-zero exit aborts smoke. Print `SMOKE: N/N passed` at end. Exit nonzero on any failure.

    `suite.sh` — `source lib.sh`; first call `bash scripts/tests/phase-03/smoke.sh`; then run live Tailnet probes (each wrapped in `if curl -sf --max-time 5 ...` so unreachable endpoints degrade gracefully with a clear FAIL message):
    - `curl -sf "$PROM_URL/-/healthy"` expect 200
    - `curl -sf "$PROM_URL/api/v1/targets" | jq -e '.data.activeTargets | length > 0'`
    - `curl -sf "$AM_URL/-/healthy"` expect 200 (may fail until Plan 03-03 — guard with `if command -v amtool` style gate marker)
    Print `SUITE: OK` on success.

    `01-prometheus-config.sh` — the placeholder snippet for this plan:
    - `source ../lib.sh`
    - `assert_file "$MON_ROOT/prometheus/prometheus.yml"`
    - `assert_file "$MON_ROOT/prometheus/targets/nodes.yml"`
    - `assert_file "$MON_ROOT/prometheus/targets/cadvisor.yml"`
    - `assert_file "$MON_ROOT/prometheus/alerts/homelab.yml"`
    - `promtool check config "$MON_ROOT/prometheus/prometheus.yml"`
    - `promtool check rules "$MON_ROOT/prometheus/alerts/homelab.yml"`
    - `yamllint -d '{extends: relaxed}' "$MON_ROOT/prometheus/targets/"*.yml`

    All four files: `#!/usr/bin/env bash` shebang, `set -euo pipefail`, `chmod +x`.

    Update `scripts/README.md` — append a section under "Inventory":
    ```
    ## tests/phase-03/
    Phase 3 monitoring validation. `smoke.sh` = config-only (<30s). `suite.sh` = config + Tailnet probes (<90s). Add new `NN-<name>.sh` snippets as later plans land; `smoke.sh` auto-discovers them.
    ```
  </action>
  <verify>
    <automated>shellcheck scripts/tests/phase-03/lib.sh scripts/tests/phase-03/smoke.sh scripts/tests/phase-03/suite.sh scripts/tests/phase-03/01-prometheus-config.sh</automated>
  </verify>
  <done>Four harness files exist, all pass shellcheck, smoke.sh script structure auto-discovers numbered snippets, scripts/README.md documents the new directory.</done>
</task>

<task type="auto">
  <name>Task 2: Commit prometheus.yml, target files, and alert rules</name>
  <files>
    servers/docker-tower/monitoring/prometheus/prometheus.yml
    servers/docker-tower/monitoring/prometheus/targets/nodes.yml
    servers/docker-tower/monitoring/prometheus/targets/cadvisor.yml
    servers/docker-tower/monitoring/prometheus/alerts/homelab.yml
    servers/docker-tower/monitoring/prometheus/alerts/README.md
  </files>
  <read_first>
    - servers/docker-tower/monitoring/prometheus/_captured/prometheus.yml.live (from Task 0b — compare to avoid losing any existing scrape targets)
    - .planning/phases/03-health-monitoring/03-RESEARCH.md section "Prometheus Config" and "Alert Rules"
  </read_first>
  <action>
    Write the canonical Prometheus tree per RESEARCH.md (exact content — do not paraphrase).

    **`prometheus.yml`** — exactly the block from 03-RESEARCH.md "Full prometheus.yml (to commit at …)". Preserve:
    ```yaml
    global:
      scrape_interval: 15s
      evaluation_interval: 15s
    alerting:
      alertmanagers:
        - static_configs:
            - targets: ['localhost:9093']
    rule_files:
      - /etc/prometheus/alerts/*.yml
    scrape_configs:
      - job_name: 'prometheus'
        static_configs:
          - targets: ['localhost:9090']
      - job_name: 'node'
        file_sd_configs:
          - files: [/etc/prometheus/targets/nodes.yml]
            refresh_interval: 5m
      - job_name: 'cadvisor'
        file_sd_configs:
          - files: [/etc/prometheus/targets/cadvisor.yml]
            refresh_interval: 5m
    ```

    **`targets/nodes.yml`** — exactly:
    ```yaml
    - targets:
        - '100.101.0.7:9100'   # tower
        - '100.101.0.8:9100'   # docker-tower
        - '100.99.133.9:9100'  # cc-worker
        - '100.101.0.9:9100'   # mcow
        - '100.101.0.3:9100'   # nether
        - '100.119.15.122:9100' # animaya-dev
      labels:
        job: node
    ```

    **`targets/cadvisor.yml`** — exactly:
    ```yaml
    - targets:
        - '100.101.0.8:8080'   # docker-tower cAdvisor
        - '100.101.0.9:8080'   # mcow cAdvisor
      labels:
        job: cadvisor
    ```

    **`alerts/homelab.yml`** — exactly the block from 03-RESEARCH.md "Alert Rules" (all three groups: homelab.nodes, homelab.containers, homelab.prometheus). Include HostDown, DiskUsageCritical, MemoryPressure, ContainerRestartLoop, CAdvisorDown, PrometheusSelfScrapeFailure, AlertmanagerDown. Do NOT include the cert expiry alert — instead add it commented-out at the bottom with the banner below.

    **`alerts/README.md`** — Explain:
    - Cert expiry alert (per CONTEXT.md) is deferred: no cert-expiry exporter is in scope for Phase 3 (see RESEARCH.md Risk 7). A commented placeholder appears in `homelab.yml` and will be enabled when a cert exporter is added in a future phase.
    - All other required alerts (HostDown, DiskUsageCritical, ContainerRestartLoop, PrometheusSelfScrapeFailure) are live and match CONTEXT.md thresholds.

    Cross-check against `_captured/prometheus.yml.live` from Task 0b: if the live file contains any scrape targets not represented in nodes.yml/cadvisor.yml, STOP and surface them to user — do NOT silently drop.
  </action>
  <verify>
    <automated>bash scripts/tests/phase-03/smoke.sh</automated>
  </verify>
  <done>prometheus.yml passes `promtool check config`; homelab.yml passes `promtool check rules`; target YAMLs pass yamllint; all CONTEXT.md required alerts present (cert expiry as documented placeholder); smoke.sh green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → docker-tower SSH | Claude Code runs SSH over Tailnet; docker-tower root shell trusted |
| committed YAML → Prometheus container | prometheus.yml bind-mounted ro; tampering requires repo write access |
| Tailnet scrape | Prometheus → node-exporter :9100 over Tailscale (encrypted, ACL-scoped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | prometheus.yml bind mount | mitigate | Mount read-only (`:ro`) in Plan 03-03 compose; file owned by root in repo |
| T-03-01-02 | Info disclosure | targets file exposes Tailnet IPs | accept | Tailnet IPs are not sensitive; ACL enforces access |
| T-03-01-03 | DoS (scrape storm) | scrape_interval too aggressive | mitigate | 15s interval per RESEARCH.md (standard, not aggressive); refresh_interval 5m for file_sd |
| T-03-01-04 | Repudiation | alert rule edits | mitigate | All rule changes via git commits (SEC-02 pattern); `promtool check` in pre-commit harness |
| T-03-01-05 | Information disclosure | `_captured/` leaks live config | accept | Live config contains no secrets (only scrape targets); retained for diff only |
</threat_model>

<verification>
- `bash scripts/tests/phase-03/00-env-check.sh` exits 0 (operator tooling present)
- `bash scripts/tests/phase-03/smoke.sh` exits 0 (all 4 config files parse/check cleanly)
- No destructive changes to live docker-tower — Wave 0 is read-only SSH
- PVE firewall state captured to `/tmp/phase-03-pve-firewall.txt` for operator review before Plan 03-02
</verification>

<success_criteria>
- prometheus.yml, targets/nodes.yml, targets/cadvisor.yml, alerts/homelab.yml all committed and pass promtool + yamllint
- `_captured/prometheus.yml.live` preserves live state pre-migration
- scripts/tests/phase-03/{lib,smoke,suite}.sh scaffold exists and passes shellcheck
- scripts/README.md indexes the new test tree
- Every later plan in Phase 3 can append `NN-*.sh` snippets into scripts/tests/phase-03/ without touching the wrapper
</success_criteria>

<dependencies>
- Upstream: none (Wave 1 root)
- Downstream: 03-02 (consumes targets/nodes.yml), 03-03 (consumes prometheus.yml + alerts/), 03-04 (consumes scripts/tests/phase-03/ harness), 03-05 (extends harness)
</dependencies>

<output>
After completion, create `.planning/phases/03-health-monitoring/03-01-SUMMARY.md`
</output>
