---
phase: 20
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml
  - servers/docker-tower/monitoring/prometheus/alerts/homelab.yml
  - servers/docker-tower/monitoring/prometheus/tests/claude-usage_test.yml
  - servers/docker-tower/monitoring/prometheus/tests/homelab_test.yml
  - ansible/playbooks/tasks/homelab-admin-secrets.yml
autonomous: true
requirements: [ALERT-03, ALERT-04]
tags: [prometheus, alertmanager, ansible, claude-usage, monitoring]

must_haves:
  truths:
    - "Three new alert rules ClaudeWeeklyQuotaHigh / ClaudeWeeklyQuotaCritical / ClaudeExporterDown are defined in claude-usage.yml and load into Prometheus."
    - "The old homelab.claude group is removed from homelab.yml so no duplicate firing occurs (Pitfall 1)."
    - "promtool test rules runs green against claude-usage_test.yml and the updated homelab_test.yml."
    - "ansible-playbook deploy-docker-tower.yml renders the new rule file onto docker-tower and reloads Prometheus without errors."
    - "ALERTMANAGER_URL env var is rendered into /etc/homelab-admin/env on mcow (defaults to http://docker-tower:9093)."
  artifacts:
    - path: "servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml"
      provides: "3 alert rules per ALERT-03 + D-04"
      contains: "ClaudeWeeklyQuotaHigh"
    - path: "servers/docker-tower/monitoring/prometheus/tests/claude-usage_test.yml"
      provides: "promtool fixtures: pending→firing transitions for each rule"
      contains: "rule_files"
    - path: "ansible/playbooks/tasks/homelab-admin-secrets.yml"
      provides: "ALERTMANAGER_URL line rendered next to PROMETHEUS_URL"
      contains: "ALERTMANAGER_URL"
  key_links:
    - from: "servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml"
      to: "claude_usage_7d_utilization metric"
      via: "PromQL expr >= 0.80 / 0.95"
      pattern: "claude_usage_7d_utilization"
    - from: "servers/docker-tower/monitoring/prometheus/tests/claude-usage_test.yml"
      to: "../alerts/claude-usage.yml"
      via: "rule_files reference"
      pattern: "rule_files:\\s*\\n\\s*-\\s*\\.\\./alerts/claude-usage\\.yml"
    - from: "ansible/playbooks/tasks/homelab-admin-secrets.yml"
      to: "/etc/homelab-admin/env"
      via: "ALERTMANAGER_URL line appended after PROMETHEUS_URL"
      pattern: "ALERTMANAGER_URL=\\{\\{ alertmanager_url \\| default"
---

<objective>
Migrate Claude-usage alert rules from the legacy `homelab.claude` group in `homelab.yml` into a new `claude-usage.yml` file with the names required by ALERT-03 + D-04, unit-test them via `promtool test rules`, deploy via the canonical `deploy-docker-tower.yml` playbook, and render `ALERTMANAGER_URL` into the mcow homelab-admin env (consumed by Plan 01's /alerts link-out).

Purpose: Stop duplicate firing (old `ClaudeUsage7dHigh/Critical` + new names would double-page Telegram), tighten severities (`ClaudeExporterDown` warning → critical), and wire the ansible env var so Plan 01's link-out resolves in production.

Output: 1 new rules file, 1 new promtool test file, surgical edits to the existing rules+tests files to remove migrated rules, 1 ansible line insertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-alerts-panel-rules/20-CONTEXT.md
@.planning/phases/20-alerts-panel-rules/20-RESEARCH.md
@.planning/phases/20-alerts-panel-rules/20-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md + RESEARCH.md. Executor does not need to re-explore. -->

Existing homelab.yml `homelab.claude` group (lines 90-140) to REMOVE (migrate to new file):
- `ClaudeUsage7dHigh`  (expr claude_usage_7d_utilization > 0.8, for 15m, severity warning)  → rename → ClaudeWeeklyQuotaHigh, tighten to `>= 0.80`
- `ClaudeUsage7dCritical` (expr > 0.95, for 5m, critical) → rename → ClaudeWeeklyQuotaCritical, tighten to `>= 0.95`, bump for to 15m (D-04)
- `ClaudeExporterDown` (up{job="claude-usage"} == 0, for 10m, warning) → KEEP name, upgrade severity to critical (D-04)

5h rules: `ClaudeUsage5hHigh` / `ClaudeUsage5hCritical` remain in homelab.yml UNCHANGED (out of ALERT-03 scope).

Existing homelab_test.yml — remove any test blocks referencing `ClaudeUsage7dHigh`, `ClaudeUsage7dCritical`, or `ClaudeExporterDown` (they're migrated).

Existing ansible/playbooks/tasks/homelab-admin-secrets.yml line 82 PROMETHEUS_URL pattern to mirror:
```yaml
      # Non-secret: Prometheus base URL. docker-tower resolves via Tailnet MagicDNS on mcow.
      # Override in inventory via host_var `prometheus_url` if Prometheus relocates.
      PROMETHEUS_URL={{ prometheus_url | default('http://docker-tower:9090') }}
```

Canonical deploy: `ansible-playbook ansible/playbooks/deploy-docker-tower.yml` (SoT path /opt/homelab/ per memory `project_docker_tower_canonical_path`).

promtool in-container invocation: `docker exec prometheus promtool test rules /etc/prometheus/tests/claude-usage_test.yml`. Locally: `docker run --rm -v $PWD/servers/docker-tower/monitoring/prometheus:/etc/prometheus prom/prometheus:v2 promtool test rules /etc/prometheus/tests/claude-usage_test.yml`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create claude-usage.yml + claude-usage_test.yml; remove migrated rules from homelab.yml + homelab_test.yml; run promtool check + test</name>
  <read_first>
    - servers/docker-tower/monitoring/prometheus/alerts/homelab.yml (full file — identify exact line range of `homelab.claude` group to delete; preserve `homelab.nodes`, `homelab.containers`, `homelab.prometheus`, `ClaudeUsage5h*` rules)
    - servers/docker-tower/monitoring/prometheus/tests/homelab_test.yml (identify test blocks referencing ClaudeUsage7dHigh / ClaudeUsage7dCritical / ClaudeExporterDown to remove)
    - .planning/phases/20-alerts-panel-rules/20-RESEARCH.md §"Code Examples" Example 2 (exact rule YAML for claude-usage.yml, lines 402-433)
    - .planning/phases/20-alerts-panel-rules/20-PATTERNS.md §claude-usage_test.yml (fixture template, HostDown block lines 12-29)
    - .planning/phases/20-alerts-panel-rules/20-CONTEXT.md §D-04 (locked rule specs)
  </read_first>
  <files>
    servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml,
    servers/docker-tower/monitoring/prometheus/alerts/homelab.yml,
    servers/docker-tower/monitoring/prometheus/tests/claude-usage_test.yml,
    servers/docker-tower/monitoring/prometheus/tests/homelab_test.yml
  </files>
  <action>
    **1. Create `servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml`** with EXACTLY three rules (D-04 locked):
    ```yaml
    groups:
      - name: claude-usage
        interval: 1m
        rules:
          - alert: ClaudeWeeklyQuotaHigh
            expr: claude_usage_7d_utilization >= 0.80
            for: 15m
            labels:
              severity: warning
            annotations:
              summary: "Claude weekly quota high for {{ $labels.name }} ({{ $value | humanizePercentage }})"
              description: "Token {{ $labels.name }} 7-day utilization at or above 80% for 15 minutes."

          - alert: ClaudeWeeklyQuotaCritical
            expr: claude_usage_7d_utilization >= 0.95
            for: 15m
            labels:
              severity: critical
            annotations:
              summary: "Claude weekly quota CRITICAL for {{ $labels.name }} ({{ $value | humanizePercentage }})"
              description: "Token {{ $labels.name }} 7-day utilization at or above 95% — effectively rate-limited."

          - alert: ClaudeExporterDown
            expr: up{job="claude-usage"} == 0
            for: 10m
            labels:
              severity: critical
            annotations:
              summary: "Claude usage exporter unreachable"
              description: "Prometheus cannot scrape mcow:9101 for 10 minutes — quota visibility lost."
    ```

    **2. Edit `servers/docker-tower/monitoring/prometheus/alerts/homelab.yml`** — delete the entire `- name: homelab.claude` group block containing `ClaudeUsage7dHigh`, `ClaudeUsage7dCritical`, AND `ClaudeExporterDown`. PRESERVE the `ClaudeUsage5hHigh` and `ClaudeUsage5hCritical` rules (different window; not in ALERT-03 scope). If 5h rules share the `homelab.claude` group, move them to a renamed group `homelab.claude-5h` within homelab.yml (so we don't lose them). If they're already in a different group, leave untouched.

    **3. Edit `servers/docker-tower/monitoring/prometheus/tests/homelab_test.yml`** — delete any test block whose `alertname:` is `ClaudeUsage7dHigh`, `ClaudeUsage7dCritical`, or `ClaudeExporterDown`. Preserve all other tests (HostDown, node/container tests, 5h tests).

    **4. Create `servers/docker-tower/monitoring/prometheus/tests/claude-usage_test.yml`**:
    ```yaml
    # promtool rule unit tests for claude-usage alert rules (Phase 20 ALERT-04).
    # Run: docker exec prometheus promtool test rules /etc/prometheus/tests/claude-usage_test.yml
    rule_files:
      - ../alerts/claude-usage.yml

    tests:
      # ClaudeWeeklyQuotaHigh: 0.85 sustained 16m → fires at 16m (for:15m satisfied)
      - interval: 1m
        input_series:
          - series: 'claude_usage_7d_utilization{name="prod"}'
            values: '0.5 0.5 0.85x20'
        alert_rule_test:
          - eval_time: 14m
            alertname: ClaudeWeeklyQuotaHigh
            exp_alerts: []   # still pending (has been 0.85 for 12m)
          - eval_time: 18m
            alertname: ClaudeWeeklyQuotaHigh
            exp_alerts:
              - exp_labels:
                  severity: warning
                  name: "prod"
                exp_annotations:
                  summary: "Claude weekly quota high for prod (85%)"
                  description: "Token prod 7-day utilization at or above 80% for 15 minutes."

      # ClaudeWeeklyQuotaCritical: 0.97 sustained 16m → fires; critical severity
      - interval: 1m
        input_series:
          - series: 'claude_usage_7d_utilization{name="prod"}'
            values: '0.5x2 0.97x20'
        alert_rule_test:
          - eval_time: 18m
            alertname: ClaudeWeeklyQuotaCritical
            exp_alerts:
              - exp_labels:
                  severity: critical
                  name: "prod"
                exp_annotations:
                  summary: "Claude weekly quota CRITICAL for prod (97%)"
                  description: "Token prod 7-day utilization at or above 95% — effectively rate-limited."

      # ClaudeExporterDown: up==0 sustained 11m → fires; critical severity (upgraded from warning)
      - interval: 1m
        input_series:
          - series: 'up{job="claude-usage",instance="100.101.0.9:9101"}'
            values: '1 1 0x15'
        alert_rule_test:
          - eval_time: 11m
            alertname: ClaudeExporterDown
            exp_alerts:
              - exp_labels:
                  severity: critical
                  job: "claude-usage"
                  instance: "100.101.0.9:9101"
                exp_annotations:
                  summary: "Claude usage exporter unreachable"
                  description: "Prometheus cannot scrape mcow:9101 for 10 minutes — quota visibility lost."
    ```
    Note: the `humanizePercentage` output format in `exp_annotations` must match promtool's rendering exactly (`85%` — no decimal if integer). If promtool produces `85.0%`, adjust the fixture accordingly — iterate until green.

    **5. Run promtool** locally to validate before any deploy:
    ```bash
    docker run --rm -v "$PWD/servers/docker-tower/monitoring/prometheus:/etc/prometheus" prom/prometheus:v2 promtool check rules /etc/prometheus/alerts/claude-usage.yml /etc/prometheus/alerts/homelab.yml
    docker run --rm -v "$PWD/servers/docker-tower/monitoring/prometheus:/etc/prometheus" prom/prometheus:v2 promtool test rules /etc/prometheus/tests/claude-usage_test.yml /etc/prometheus/tests/homelab_test.yml
    ```
    Both must exit 0.
  </action>
  <verify>
    <automated>docker run --rm -v "$PWD/servers/docker-tower/monitoring/prometheus:/etc/prometheus" prom/prometheus:v2 promtool check rules /etc/prometheus/alerts/claude-usage.yml /etc/prometheus/alerts/homelab.yml && docker run --rm -v "$PWD/servers/docker-tower/monitoring/prometheus:/etc/prometheus" prom/prometheus:v2 promtool test rules /etc/prometheus/tests/claude-usage_test.yml /etc/prometheus/tests/homelab_test.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'alert: ClaudeWeeklyQuotaHigh' servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml` returns `1`.
    - `grep -c 'alert: ClaudeWeeklyQuotaCritical' servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml` returns `1`.
    - `grep -c 'alert: ClaudeExporterDown' servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml` returns `1`.
    - `grep -q 'severity: critical' servers/docker-tower/monitoring/prometheus/alerts/claude-usage.yml` for ExporterDown block (manual verify block-scope).
    - `grep -c 'alert: ClaudeUsage7dHigh\|alert: ClaudeUsage7dCritical\|alert: ClaudeExporterDown' servers/docker-tower/monitoring/prometheus/alerts/homelab.yml` returns `0`.
    - `grep -q 'alert: ClaudeUsage5hHigh\|alert: ClaudeUsage5hCritical' servers/docker-tower/monitoring/prometheus/alerts/homelab.yml` passes (5h rules preserved).
    - `grep -q 'rule_files:' servers/docker-tower/monitoring/prometheus/tests/claude-usage_test.yml` passes.
    - `grep -q '../alerts/claude-usage.yml' servers/docker-tower/monitoring/prometheus/tests/claude-usage_test.yml` passes.
    - `promtool check rules` + `promtool test rules` both exit 0.
  </acceptance_criteria>
  <done>claude-usage.yml + claude-usage_test.yml exist and pass promtool; legacy rules purged from homelab.yml; 5h rules preserved; homelab_test.yml still green.</done>
</task>

<task type="auto">
  <name>Task 2: Render ALERTMANAGER_URL in ansible homelab-admin env + deploy docker-tower (rules) + deploy homelab-admin (env)</name>
  <read_first>
    - ansible/playbooks/tasks/homelab-admin-secrets.yml (find PROMETHEUS_URL block at/near line 82 — exact formatting + comment style to mirror)
    - ansible/playbooks/deploy-docker-tower.yml (confirm rule-file deploy path + Prometheus reload handler)
    - ansible/playbooks/deploy-homelab-admin.yml (confirm it includes homelab-admin-secrets.yml at Stage 4)
    - .planning/phases/20-alerts-panel-rules/20-PATTERNS.md §ansible/playbooks/tasks/homelab-admin-secrets.yml
  </read_first>
  <files>
    ansible/playbooks/tasks/homelab-admin-secrets.yml
  </files>
  <action>
    **1. Edit `ansible/playbooks/tasks/homelab-admin-secrets.yml`** — locate the line containing `PROMETHEUS_URL={{ prometheus_url | default('http://docker-tower:9090') }}` (around line 82). Immediately after the `PROMETHEUS_URL` line (inside the same `content:` / `blockinfile` / env-rendering block it lives in — preserve indentation exactly), insert:
    ```
          # Non-secret: Alertmanager base URL for /alerts link-out (ALERT-02).
          # Override via host_var `alertmanager_url` if AM relocates.
          ALERTMANAGER_URL={{ alertmanager_url | default('http://docker-tower:9093') }}
    ```
    Match the leading whitespace of the PROMETHEUS_URL line exactly (6 spaces per PATTERNS.md excerpt). Do not add a `host_var alertmanager_url` to inventory — the default is correct for current topology.

    **2. Deploy docker-tower** (rules):
    ```bash
    ansible-playbook -i ansible/inventory.yml ansible/playbooks/deploy-docker-tower.yml --tags prometheus
    ```
    (If no `prometheus` tag exists, run full playbook — it's idempotent.) Confirm in output: handler `reload prometheus` (or similar) ran OR Prometheus container restarted. Then verify rules loaded:
    ```bash
    ssh root@docker-tower 'curl -s http://localhost:9090/api/v1/rules | grep -o ClaudeWeeklyQuotaHigh | head -1'
    ```
    Must print `ClaudeWeeklyQuotaHigh`.

    **3. Deploy homelab-admin** (env):
    ```bash
    ansible-playbook -i ansible/inventory.yml ansible/playbooks/deploy-homelab-admin.yml
    ```
    Verify env render:
    ```bash
    ssh root@mcow 'grep ALERTMANAGER_URL /etc/homelab-admin/env'
    ```
    Must print `ALERTMANAGER_URL=http://docker-tower:9093` (or the overridden value). systemd service reload handler should have fired — confirm `systemctl status homelab-admin` shows recent restart timestamp.

    **4. End-to-end sanity:**
    ```bash
    curl -s -o /dev/null -w '%{http_code}\n' https://homelab.makscee.ru/api/alerts/list
    ```
    Expected: `401` (unauthenticated — the route exists and auth gate is active from Plan 01). Plan 01 executor runs Playwright UAT after this plan ships — they authenticate and validate full response.
  </action>
  <verify>
    <automated>ansible-playbook -i ansible/inventory.yml --check ansible/playbooks/deploy-homelab-admin.yml | grep -q 'ALERTMANAGER_URL' && ssh root@docker-tower 'curl -fsS http://localhost:9090/api/v1/rules' | grep -q ClaudeWeeklyQuotaHigh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ALERTMANAGER_URL={{ alertmanager_url | default('http://docker-tower:9093') }}" ansible/playbooks/tasks/homelab-admin-secrets.yml` passes.
    - `grep -B1 ALERTMANAGER_URL ansible/playbooks/tasks/homelab-admin-secrets.yml | grep -q PROMETHEUS_URL` (ALERTMANAGER_URL is adjacent to PROMETHEUS_URL in the block).
    - On docker-tower: `curl -s http://localhost:9090/api/v1/rules` returns JSON containing `"name":"ClaudeWeeklyQuotaHigh"`, `"name":"ClaudeWeeklyQuotaCritical"`, `"name":"ClaudeExporterDown"`.
    - On docker-tower: same JSON does NOT contain `"name":"ClaudeUsage7dHigh"` or `"name":"ClaudeUsage7dCritical"`.
    - On mcow: `/etc/homelab-admin/env` contains `ALERTMANAGER_URL=http://docker-tower:9093` (or host-overridden value).
    - On mcow: `systemctl is-active homelab-admin` returns `active`.
    - `curl -s -o /dev/null -w '%{http_code}' https://homelab.makscee.ru/api/alerts/list` returns `401`.
  </acceptance_criteria>
  <done>Rules loaded live on Prometheus; old rules gone; ALERTMANAGER_URL rendered on mcow; homelab-admin service up; /api/alerts/list responds (401 unauth) — Plan 01 UAT will cover authed path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator laptop → docker-tower (ansible SSH) | root-level config writes; Tailscale + SSH key auth |
| Prometheus → Alertmanager (Tailnet HTTP) | alert notifications; internal-only |
| Alertmanager → Telegram API (docker-tower direct egress) | bot token used; secret file `/etc/alertmanager/telegram_token` mode 0600 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | T (Tampering) | Duplicate rules (old + new) firing twice | mitigate | Task 1 DELETES `ClaudeUsage7d*` and old `ClaudeExporterDown` from homelab.yml in SAME commit as new file (Pitfall 1); promtool check across both files before deploy |
| T-20-02-02 | I (Info disclosure) | ALERTMANAGER_URL leaking as `NEXT_PUBLIC_*` | accept | Rendered as plain env var in `/etc/homelab-admin/env` — server-only consumption enforced by Plan 01 RSC read; URL is Tailnet-internal anyway, low sensitivity |
| T-20-02-03 | D (DoS) | Prometheus reload failure on bad YAML | mitigate | `promtool check rules` in Task 1 verify; ansible handler restarts only on success; rollback = `git revert` + re-deploy |
| T-20-02-04 | E (Elevation) | Ansible playbook running with unintended scope | mitigate | Deploy via canonical `deploy-docker-tower.yml` + `deploy-homelab-admin.yml` only; `--check` dry-run available for verify step |
| T-20-02-05 | R (Repudiation) | Who deployed what when | accept | Git log on rule files + ansible stdout in terminal history; no separate audit log needed for IaC files |
| T-20-02-06 | I | Telegram bot token path exposure via rule file | mitigate | Rule files contain no secrets; bot token stays in `/etc/alertmanager/telegram_token` (file mode 0600) unchanged |
</threat_model>

<verification>
- `promtool check rules` on both rule files exits 0.
- `promtool test rules` on both test files exits 0.
- Live Prometheus `/api/v1/rules` endpoint contains all 3 new rules, no `ClaudeUsage7d*` rules.
- /etc/homelab-admin/env on mcow contains ALERTMANAGER_URL.
- homelab-admin.service is active after deploy.
- Ansible idempotency: re-running both playbooks produces no `changed` tasks on a clean deploy.
</verification>

<success_criteria>
- Three required rules deployed and visible in Prometheus rule API.
- Legacy overlapping rules removed — no duplicate firing possible.
- promtool unit tests cover pending→firing transition for each rule at correct thresholds + for-durations.
- ALERTMANAGER_URL env var live on mcow, consumed by Plan 01's /alerts link-out.
- deploy-docker-tower.yml idempotent: second run shows `changed=0`.
</success_criteria>

<output>
After completion, create `.planning/phases/20-alerts-panel-rules/20-02-SUMMARY.md`. Include: exact line ranges removed from homelab.yml, final claude-usage.yml contents, promtool output, disposition of `ClaudeUsage5h*` rules (kept in homelab.yml vs moved to claude-usage.yml), any operator confirmation on severity upgrade (A3 assumption).
</output>
