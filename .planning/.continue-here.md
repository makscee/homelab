# Continue Here — 04-03 Resume Checklist (2026-04-15 update)

**Status:** Paused — awaiting operator admin-console actions + bot-token rotation.
**Last agent action:** Ran `tailscale set --advertise-connector=true` on nether (Tailscale 1.96.4). Command accepted, no error. Admin-console enablement + ACL tagging + domain mapping still required.

---

## Observed state on nether (post-CLI)

- Tailscale version: `1.96.4` (commit `8cf541dfd`)
- Current tags: `['tag:personal']` (NOT `tag:connector` — usually required for clean ACLs)
- TailscaleIPs: `100.101.0.3`, `fd7a:115c:a1e0::9b37:6ea`
- `CapMap` does NOT yet list `app-connectors` capability → admin-console enablement pending.
- App-connector role is orthogonal to AmneziaWG / Xray on nether; it will not disrupt existing VPN services.

---

## Operator Actions Required (in order)

### A. Tailscale Admin Console — App Connector

Open https://login.tailscale.com/admin and do the following:

1. **DNS tab** (sidebar) → scroll to **"App connectors"** section.
   *(If absent, go to Settings → Feature Previews and enable App Connectors.)*

2. Click **"Add app connector"** (or **Configure app connectors**).

3. **Provider / Type:** choose **Custom** (not the Microsoft/Google presets).

4. **Select node:** `nether`.
   *(Should appear because we ran `tailscale set --advertise-connector=true`.)*

5. **Domains to route** (one per line):
   ```
   api.telegram.org
   core.telegram.org
   ```
   Optional fallback if the minimum set doesn't fix dispatch: add `telegram.org` and `*.t.me`.
   Do **NOT** paste CIDRs like `149.154.160.0/20` — app-connectors resolve by domain, not by route.

6. **Save.**

### B. ACL Tagging (likely needed)

Still in the admin console:

7. **Access Controls** tab. Ensure `tagOwners` includes `tag:connector`:
   ```jsonc
   "tagOwners": {
     "tag:personal":  ["autogroup:admin"],
     "tag:connector": ["autogroup:admin"]
   }
   ```

8. Ensure `acls` permits tailnet nodes → connector:
   ```jsonc
   { "action": "accept", "src": ["*"], "dst": ["tag:connector:*"] }
   ```

9. **Re-tag nether:** Machines tab → click `nether` → **Edit ACL tags** → add `tag:connector` (keep `tag:personal`). Save.

10. Save ACLs.

### C. Telegram Bot Token Rotation (security blocker)

Old token leaked in cleartext into mcow docker logs during the failed-dispatch URL retries.

11. On Telegram, open **@BotFather** chat.

12. `/mybots` → select the monitoring bot → **API Token** → **Revoke current token**. BotFather returns a new token `NNNNN:AA...`.

13. **Paste the new token into this chat.** Executor will immediately:
    - Update `secrets/mcow.sops.yaml` (`TELEGRAM_BOT_TOKEN`), sops-re-encrypt, commit.
    - Deploy to `/run/secrets/telegram_token` on mcow (`0440 root:65534`).
    - Restart Alertmanager.
    - Truncate the leaked docker logs: `truncate -s 0 $(docker inspect alertmanager --format '{{.LogPath}}')` on mcow.

### D. Confirm back to executor

When A + B + C are done, reply with:

- `"app connector live"` (admin console saved, ACLs updated, nether re-tagged)
- New bot token pasted inline (will be sops-encrypted immediately)

---

## What Executor Will Do On Resume

1. Verify on mcow:
   - `tailscale status | grep -i connector` → expect `nether ... connector`
   - `getent hosts api.telegram.org` → expect a `100.x` magic IP
   - `curl -4 -m 8 -sSIL https://api.telegram.org/` → expect HTTP/2 302 or 200
2. Rotate bot token (sops edit → deploy → restart AM → truncate leaked logs).
3. Resume 04-03 smoke:
   - Confirm `node_exporter` still stopped on cc-worker.
   - Wait for Alertmanager HostDown re-notify on `group_interval`.
   - **Checkpoint 1:** operator confirms FIRING Telegram message received.
   - Restart `node_exporter` on cc-worker.
   - **Checkpoint 2:** operator confirms RESOLVED Telegram message received.
   - Unwind any remaining DiskUsageCritical silences.
4. Write `04-03-SUMMARY.md` with egress-diagnosis + app-connector-setup + token-rotation + E2E smoke artifacts.
5. Flip ROADMAP, close plan.

---

## Files Written This Turn

- `.planning/.continue-here.md` — this file (overwrote prior investigation checklist)
- `.planning/phases/04-operator-dashboard/04-03-APP-CONNECTOR-SETUP.md` — nether-side action log + CLI output + rollback

## Commit

`chore(04-03): advertise nether as tailscale app connector` — nether CLI change committed atomically, separate from 04-03 smoke work.
