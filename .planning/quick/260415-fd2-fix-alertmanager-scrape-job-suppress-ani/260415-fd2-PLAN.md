---
phase: 260415-fd2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/docker-tower/monitoring/prometheus/prometheus.yml
  - servers/docker-tower/monitoring/prometheus/targets/nodes.yml
autonomous: true
requirements: [MON-01]

must_haves:
  truths:
    - "AlertmanagerDown alert stops firing (rule can evaluate up{job=\"alertmanager\"})"
    - "HostDown alert for 100.119.15.122:9100 stops firing (target no longer scraped)"
    - "Prometheus config parses cleanly (promtool check) after edits"
    - "Prometheus on docker-tower reloads and picks up new config without restart"
    - "up{job=\"alertmanager\"} target appears UP in Prometheus /api/v1/targets"
  artifacts:
    - path: "servers/docker-tower/monitoring/prometheus/prometheus.yml"
      provides: "alertmanager scrape job"
      contains: "job_name: 'alertmanager'"
    - path: "servers/docker-tower/monitoring/prometheus/targets/nodes.yml"
      provides: "node scrape targets with animaya-dev suppressed"
      contains: "DEFERRED"
  key_links:
    - from: "prometheus.yml scrape_configs[alertmanager]"
      to: "alerts/homelab.yml AlertmanagerDown rule"
      via: "up{job=\"alertmanager\"} metric label"
      pattern: "job_name:\\s*['\"]alertmanager['\"]"
    - from: "targets/nodes.yml"
      to: "homelab.yml HostDown rule (up{job=\"node\"})"
      via: "file_sd_configs reload on docker-tower"
      pattern: "100\\.119\\.15\\.122"
---

<objective>
Silence two persistent false-positive Telegram alerts:
1. **AlertmanagerDown** — add a missing scrape job so `up{job="alertmanager"}` metric exists.
2. **HostDown (100.119.15.122:9100)** — remove animaya-dev from node scrape targets (node-exporter deferred per v1.0 audit, SSH blocked).

Purpose: Stop alert fatigue. Both alerts fire continuously due to config gaps, not real outages. Monitoring stack is otherwise healthy.

Output: Two edited config files on docker-tower + live Prometheus reload, verified via /api/v1/targets and promtool.
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@servers/docker-tower/monitoring/prometheus/prometheus.yml
@servers/docker-tower/monitoring/prometheus/targets/nodes.yml
@servers/docker-tower/monitoring/prometheus/alerts/homelab.yml
@servers/docker-tower/docker-compose.monitoring.yml

<interfaces>
<!-- Relevant shapes extracted from the repo -->

Current prometheus.yml scrape_configs has three jobs: `prometheus`, `node` (file_sd), `cadvisor` (file_sd).
The `alerting:` block lists `localhost:9093` for delivery but does NOT create a scrape target.
Alertmanager runs with `network_mode: host` on docker-tower, so `localhost:9093` is reachable from the prometheus container (also host-networked).

Alert rule from alerts/homelab.yml (group homelab.prometheus):
```yaml
- alert: AlertmanagerDown
  expr: absent(up{job="alertmanager"}) or up{job="alertmanager"} == 0
  for: 5m
```
→ requires a scrape job literally named `alertmanager`.

Current nodes.yml has 6 targets; only 100.119.15.122:9100 (animaya-dev) lacks a running node-exporter.

Prometheus runs with `--web.enable-lifecycle`, so `curl -X POST http://localhost:9090/-/reload` on docker-tower is the supported reload path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alertmanager scrape job + suppress animaya-dev target</name>
  <files>
    servers/docker-tower/monitoring/prometheus/prometheus.yml
    servers/docker-tower/monitoring/prometheus/targets/nodes.yml
  </files>
  <action>
**Edit 1 — prometheus.yml**
Append a new scrape job after the existing `prometheus` self-scrape block, before the `node` job (keep ordering: self, alertmanager, node, cadvisor). Add exactly:

```yaml
  # Alertmanager self-scrape (satisfies AlertmanagerDown rule in alerts/homelab.yml)
  - job_name: 'alertmanager'
    static_configs:
      - targets: ['localhost:9093']
```

Job name MUST be literal string `alertmanager` (matches `up{job="alertmanager"}` in the rule). `localhost:9093` works because both containers use `network_mode: host`.

**Edit 2 — targets/nodes.yml**
Comment out (do NOT delete — preserves history + makes re-enable trivial once SSH is fixed) the animaya-dev line. Add a DEFERRED comment referencing the audit. Final file contents:

```yaml
- targets:
    - '100.101.0.7:9100'    # tower (Proxmox host)
    - '100.101.0.8:9100'    # docker-tower (LXC 100)
    - '100.99.133.9:9100'   # cc-worker (LXC 204)
    - '100.101.0.9:9100'    # mcow
    - '100.101.0.3:9100'    # nether
    # DEFERRED: node-exporter blocked on animaya-dev (SSH unavailable) — see .planning/v1.0-MILESTONE-AUDIT.md
    # - '100.119.15.122:9100' # animaya-dev (LXC 205)
  labels:
    job: node
```

Preserve 2-space indent, trailing newline, YAML validity.
  </action>
  <verify>
    <automated>docker run --rm -v "$PWD/servers/docker-tower/monitoring/prometheus:/etc/prometheus:ro" --entrypoint promtool prom/prometheus:latest check config /etc/prometheus/prometheus.yml</automated>
  </verify>
  <done>
- prometheus.yml contains `job_name: 'alertmanager'` with target `localhost:9093`.
- nodes.yml has animaya-dev entry commented out with DEFERRED marker referencing audit doc.
- `promtool check config` prints `SUCCESS` for prometheus.yml AND `SUCCESS` for both rule files referenced.
- YAML parses (yq or python -c "import yaml; yaml.safe_load(open(...))" succeeds on both files).
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy edited configs to docker-tower and reload Prometheus</name>
  <files>
    (remote) /opt/homestack/prometheus/prometheus.yml
    (remote) /opt/homestack/prometheus/targets/nodes.yml
  </files>
  <action>
The repo is the source of truth; runtime configs live at `/opt/homestack/` on docker-tower (per compose file header: `# Source: /opt/homestack/docker-compose.monitoring.yml`). No existing ansible playbook for this specific sync — scp is acceptable for a 2-file fix (document in final commit).

Steps:

1. Copy updated files to docker-tower via Tailscale SSH:
   ```
   scp servers/docker-tower/monitoring/prometheus/prometheus.yml root@100.101.0.8:/opt/homestack/prometheus/prometheus.yml
   scp servers/docker-tower/monitoring/prometheus/targets/nodes.yml root@100.101.0.8:/opt/homestack/prometheus/targets/nodes.yml
   ```

2. Reload Prometheus without restart (prefer HTTP reload since `--web.enable-lifecycle` is set — preserves in-memory TSDB state, avoids scrape gap):
   ```
   ssh root@100.101.0.8 'curl -fsS -X POST http://localhost:9090/-/reload && echo RELOADED'
   ```

3. If HTTP reload fails for any reason (non-200), fall back to SIGHUP:
   ```
   ssh root@100.101.0.8 'docker kill -s HUP prometheus'
   ```

Do NOT restart the container (`docker compose restart prometheus`) — it loses scrape continuity and is unnecessary.
  </action>
  <verify>
    <automated>ssh root@100.101.0.8 'curl -fsS http://localhost:9090/api/v1/targets?state=active' | python3 -c "import sys,json; d=json.load(sys.stdin); jobs={t['labels']['job'] for t in d['data']['activeTargets']}; assert 'alertmanager' in jobs, f'alertmanager job missing from: {jobs}'; am=[t for t in d['data']['activeTargets'] if t['labels']['job']=='alertmanager'][0]; assert am['health']=='up', f'alertmanager target not UP: {am}'; anim=[t for t in d['data']['activeTargets'] if '100.119.15.122' in t['scrapeUrl']]; assert not anim, f'animaya-dev still scraped: {anim}'; print('OK: alertmanager UP, animaya-dev suppressed')"</automated>
  </verify>
  <done>
- `curl http://100.101.0.8:9090/api/v1/targets` shows job `alertmanager` with `health: "up"`.
- No target with host `100.119.15.122` present in active targets.
- No `promtool` errors in prometheus container logs: `ssh root@100.101.0.8 'docker logs --since 2m prometheus 2>&1 | grep -iE "error|fail" || echo clean'` returns `clean` or only pre-existing, unrelated lines.
- Reload path used (HTTP or SIGHUP) recorded in commit message.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm alerts clear + commit repo changes</name>
  <files>
    (verification only — then git)
  </files>
  <action>
1. Wait ~6 minutes (rule `for: 5m` + scrape_interval 15s) for AlertmanagerDown and HostDown{instance="100.119.15.122:9100"} to transition out of firing.

2. Query Alertmanager API to confirm no active alerts for these two:
   ```
   ssh root@100.101.0.8 'curl -fsS http://localhost:9093/api/v2/alerts' | python3 -c "import sys,json; a=json.load(sys.stdin); bad=[x for x in a if x['labels'].get('alertname')=='AlertmanagerDown' or (x['labels'].get('alertname')=='HostDown' and '100.119.15.122' in x['labels'].get('instance',''))]; assert not bad, f'still firing: {[(x[\"labels\"][\"alertname\"], x[\"labels\"].get(\"instance\")) for x in bad]}'; print('OK: both alerts cleared')"
   ```

3. Also spot-check Prometheus `ALERTS` metric:
   ```
   ssh root@100.101.0.8 "curl -fsS 'http://localhost:9090/api/v1/query?query=ALERTS{alertname=~\"AlertmanagerDown|HostDown\",alertstate=\"firing\"}'" | python3 -c "import sys,json; r=json.load(sys.stdin)['data']['result']; anim=[x for x in r if '100.119.15.122' in x['metric'].get('instance','')]; am=[x for x in r if x['metric'].get('alertname')=='AlertmanagerDown']; assert not am and not anim, f'still firing: {r}'; print('OK')"
   ```

4. Commit both repo files together with a single Conventional Commit:
   ```
   git add servers/docker-tower/monitoring/prometheus/prometheus.yml servers/docker-tower/monitoring/prometheus/targets/nodes.yml
   git commit -m "fix(monitoring): add alertmanager scrape job + suppress animaya-dev target

   - Add 'alertmanager' scrape job (localhost:9093) so the AlertmanagerDown
     rule in alerts/homelab.yml can evaluate — previously absent(up{job=alertmanager})
     always returned true, firing forever.
   - Comment out animaya-dev (100.119.15.122:9100) in nodes.yml; node-exporter
     deferred per .planning/v1.0-MILESTONE-AUDIT.md (SSH blocked to LXC 205).
   - Deployed to docker-tower:/opt/homestack/prometheus/ via scp + HTTP reload.
   "
   ```
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -qE "^fix\(monitoring\): add alertmanager scrape job"</automated>
  </verify>
  <done>
- Alertmanager API shows neither AlertmanagerDown nor HostDown{instance=~".*100.119.15.122.*"} active.
- Prometheus `ALERTS{alertstate="firing"}` query returns 0 results for both alertnames.
- Commit landed on `main` with Conventional Commit `fix(monitoring): …`.
- No new Telegram alerts received in the 10 minutes following reload (manual confirmation).
  </done>
</task>

</tasks>

<verification>
End-to-end sanity after all tasks:
1. `promtool check config` → SUCCESS (Task 1 automated).
2. `/api/v1/targets` shows `alertmanager` UP, no `100.119.15.122` (Task 2 automated).
3. `/api/v2/alerts` + `ALERTS` metric → both target alerts cleared (Task 3 automated).
4. Telegram chat silent for 10+ min post-reload (manual, passive — no action required if 1-3 pass).
</verification>

<success_criteria>
- Repo: both config files edited, valid YAML, promtool-clean, committed.
- Runtime: docker-tower Prometheus reloaded (no restart), targets endpoint shows correct state.
- Alerts: AlertmanagerDown + HostDown(animaya-dev) no longer firing in Alertmanager.
- Zero regressions: other 5 node targets still UP, cadvisor + prometheus self-scrape unaffected.
- Deferred work unchanged: animaya-dev node-exporter remains deferred (re-enable = uncomment one line once SSH is restored).
</success_criteria>

<output>
After completion, create `.planning/quick/260415-fd2-fix-alertmanager-scrape-job-suppress-ani/260415-fd2-SUMMARY.md` documenting:
- Final state of both edited files (diff excerpts).
- Reload path used (HTTP vs SIGHUP).
- Timestamp when each alert cleared.
- Confirmation that animaya-dev remains on the deferred list in v1.0-MILESTONE-AUDIT.md (no audit edit needed — tech_debt entry already accurate).
</output>
