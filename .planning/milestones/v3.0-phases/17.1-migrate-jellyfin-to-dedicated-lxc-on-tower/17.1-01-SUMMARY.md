---
phase: 17.1-migrate-jellyfin-to-dedicated-lxc-on-tower
plan: 01
subsystem: infra/proxmox
tags: [lxc, proxmox, tailscale, jellyfin, ansible-inventory]
requires: [phase-17 complete, tower reachable, operator tailscale auth approval]
provides:
  - CT 101 (unprivileged Debian 12) on tower with iGPU + RO media passthrough
  - Tailnet host `jellyfin` (100.77.246.74) reachable over SSH
  - `servers/tower/lxc-101-jellyfin.conf` snapshot as SoT
  - `scripts/verify-jellyfin-lxc.sh` Wave-0 probe stub
  - `jellyfin:` entry in `ansible/inventory/homelab.yml` under `monitored_hosts`
affects:
  - ansible/inventory/homelab.yml
tech-stack:
  added: [Proxmox dev0 passthrough, Tailscale CT-side client]
  patterns: [pct create + pct set (never hand-edit conf), unprivileged LXC + render gid mapping, RO Proxmox bindmounts]
key-files:
  created:
    - servers/tower/lxc-101-jellyfin.conf
    - scripts/verify-jellyfin-lxc.sh
    - .planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-01-SUMMARY.md
  modified:
    - ansible/inventory/homelab.yml
decisions:
  - HOST_RENDER_GID resolved to 993 (Debian 12 `render` group on tower)
  - Tailscale IP 100.77.246.74 assigned to `jellyfin` on first `tailscale up`; operator approved node
  - Inventory placed under `monitored_hosts` only (NOT `docker_hosts`) — enforces D-05 (no Docker on CT 101)
  - Accepted jellyfin host key via `ssh-keyscan` on controller so `ansible jellyfin -m ping` succeeds
metrics:
  duration: ~multi-session (planning + provisioning + operator auth gate)
  tasks: 3
  completed: 2026-04-18
---

# Phase 17.1 Plan 01: LXC 101 Provisioning Summary

**One-liner:** CT 101 (`jellyfin`) provisioned on tower as unprivileged Debian 12 with Intel iGPU via `dev0:`, RO media bindmounts, Tailscale-joined, and added to Ansible `monitored_hosts`.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Probe tower for render GID + template | (no-commit discovery) | HOST_RENDER_GID=993 captured |
| 2 | Create CT 101 + iGPU + bindmounts + Tailscale + snapshot | `c14d2f3` | `servers/tower/lxc-101-jellyfin.conf`, `scripts/verify-jellyfin-lxc.sh` |
| 3 | Add `jellyfin` to ansible inventory | (this commit) | `ansible/inventory/homelab.yml` |

## Captured Facts

- **HOST_RENDER_GID:** `993` (render group on tower host)
- **TS_IP (jellyfin):** `100.77.246.74`
- **Template used:** `debian-12-standard` (Proxmox local template store; see live `pct config 101` rootfs `toshi:101/vm-101-disk-0.raw`)
- **Tailscale status:** `100.77.246.74  jellyfin  userid:5330236094926404  linux  -` (operator approved node this session)

## Verification

- `ssh root@tower 'pct exec 101 -- tailscale ip -4'` → `100.77.246.74` ✓
- `diff <(ssh root@tower 'pct config 101') servers/tower/lxc-101-jellyfin.conf` → empty (snapshot current) ✓
- `ansible -i ansible/inventory/homelab.yml jellyfin -m ping` → `SUCCESS` (pong) ✓
- Host key for `100.77.246.74` accepted into controller `~/.ssh/known_hosts` (prerequisite for ansible)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocker] SSH host key not pre-trusted on controller**
- **Found during:** Task 3 verification (`ansible jellyfin -m ping` returned `Host key verification failed`).
- **Fix:** Ran `ssh-keyscan -H 100.77.246.74 >> ~/.ssh/known_hosts` on controller, then re-ran ping → SUCCESS.
- **Files modified:** None in repo (controller-local `~/.ssh/known_hosts`).
- **Commit:** N/A (out-of-band operator environment state).

No other deviations. pct create flags and Proxmox-side config match RESEARCH §Key Findings #1-#2 and PATTERNS guidance.

## Authentication Gates

- **Tailscale node authorization (1x):** CT 101 first `tailscale up` emitted a login URL; operator approved the node out-of-band this session. Resumed afterward; TS_IP now stable as `100.77.246.74`.

## Wave 2 Readiness

All Wave 1 preconditions for Plan 17.1-02 (Jellyfin deb install + systemd tmpfs + vainfo probe) are satisfied:

- `ansible jellyfin -m ping` green — playbooks can target the host.
- `/dev/dri/renderD128` + `/media/wdc` + `/media/sea` visible inside CT 101 per Task 2 acceptance.
- `servers/tower/lxc-101-jellyfin.conf` committed as SoT; any future `pct set` changes should re-snapshot.
- Probe stub `scripts/verify-jellyfin-lxc.sh` intentionally exits 1 — Plan 02 extends it with vainfo + health checks.

## Self-Check: PASSED

- `servers/tower/lxc-101-jellyfin.conf` — FOUND
- `scripts/verify-jellyfin-lxc.sh` — FOUND
- `ansible/inventory/homelab.yml` contains `jellyfin:` under `monitored_hosts` — FOUND
- Task 2 commit `c14d2f3` — FOUND in git log
- Ansible ping SUCCESS — VERIFIED
