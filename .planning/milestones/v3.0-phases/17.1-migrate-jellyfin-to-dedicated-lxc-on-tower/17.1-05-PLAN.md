---
phase: 17.1-migrate-jellyfin-to-dedicated-lxc-on-tower
plan: 05
type: execute
wave: 4
depends_on: ["17.1-03", "17.1-04"]
files_modified:
  - CLAUDE.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - servers/tower/README.md
  - servers/docker-tower/README.md
  - servers/jellyfin/inventory.md
autonomous: false
requirements: [D-01, D-15, D-17, D-18]

must_haves:
  truths:
    - "CLAUDE.md servers table has a `jellyfin` row; docker-tower role description drops 'Jellyfin'"
    - "ROADMAP.md Phase 17.1 goal is the real boundary statement, not 'TBD'"
    - "STATE.md has a 2026-04-25 pending todo for old Docker container purge + 2026-05-02 for config dir purge"
    - "servers/tower/README.md LXC inventory has CT 101 row"
    - "User confirms: 1080p + 4K playback with NO buffering from real client (D-17 primary motivation)"
    - "User confirms: Jellyfin dashboard shows Transcoding (VA-API / QSV), not (Software), during live playback (D-17)"
  artifacts:
    - path: "servers/jellyfin/inventory.md"
      provides: "New per-LXC service inventory doc"
    - path: ".planning/STATE.md"
      provides: "Scheduled cleanup todos for 2026-04-25 and 2026-05-02"
  key_links:
    - from: "VALIDATION.md rows 17.1-04-01, 17.1-04-02"
      to: "User UAT confirmation"
      via: "explicit checkpoint task in this plan"
      pattern: "User confirmation"
---

<objective>
Close Phase 17.1: run final automated verification, coordinate MANUAL UAT with operator (D-17 primary success criterion), update all documentation to reflect new topology, and schedule the 2026-04-25 cleanup todo. No code changes; doc + UAT + scheduling only.

Purpose: Prove D-17 green (buffering relief is the phase's reason for existing) and leave the repo accurately describing the new topology.
Output: Updated docs; STATE.md cleanup todos; VALIDATION.md UAT rows marked only if operator explicitly confirms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-CONTEXT.md
@.planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-VALIDATION.md
@.planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-03-SUMMARY.md
@.planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-04-SUMMARY.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@servers/tower/README.md
@servers/docker-tower/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final automated verification sweep</name>
  <files>(no commits)</files>
  <read_first>
    - .planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-VALIDATION.md §Per-Task Verification Map (rows 17.1-01..03)
    - scripts/verify-jellyfin-lxc.sh
  </read_first>
  <action>
    Run the full automated validation:
    ```bash
    bash scripts/verify-jellyfin-lxc.sh
    ssh root@jellyfin '/usr/lib/jellyfin-ffmpeg/vainfo --display drm --device /dev/dri/renderD128 2>&1 | tail -20'
    curl -sI https://jellyfin.makscee.ru/
    ansible-playbook -i ansible/inventory/homelab.yml ansible/playbooks/deploy-jellyfin.yml --check  # idempotence gate
    ```
    All must pass. If anything is red, STOP and return to the owning plan (02/03/04).
  </action>
  <verify>
    <automated>bash scripts/verify-jellyfin-lxc.sh && curl -sf -o /dev/null https://jellyfin.makscee.ru/</automated>
  </verify>
  <acceptance_criteria>
    - `verify-jellyfin-lxc.sh` exits 0
    - vainfo shows `iHD` Intel driver
    - `curl -sI https://jellyfin.makscee.ru/` returns 200 or 302
    - Ansible `--check` reports `changed=0 failed=0` (idempotence)
  </acceptance_criteria>
  <done>All automated checks green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING]: Manual UAT — real-client playback (D-17)</name>
  <what-built>Automated probes are all green. D-17 is the primary phase motivation and requires human observation on a real client — cannot be automated (depends on user's network + client + TV/iOS behavior).</what-built>
  <how-to-verify>
    1. Open Jellyfin at `https://jellyfin.makscee.ru/` on the operator's normal client (TV/iOS/web).
    2. Log in with existing credentials (validates D-18 — users preserved).
    3. Pick a 1080p title — play for 3 min. Observe: no rebuffering spinner, no stutter.
    4. Pick a 4K HEVC title that requires transcoding — play for 5 min. Observe: no rebuffering.
    5. While #4 is playing, open Jellyfin admin → Dashboard → Active Devices → click the session → confirm the stream shows `Transcoding (VA-API)` or `Transcoding (Intel QSV)`, NOT `Transcoding (Software)`.
    6. Optionally: spot-check "Continue Watching" row for one user — watch progress should survive migration (D-18).
    7. Report results back:
       - 1080p no-buffer: yes / no
       - 4K no-buffer: yes / no
       - Dashboard shows HW transcode: yes / no
       - Watch progress preserved: yes / no / didn't check
  </how-to-verify>
  <resume-signal>Report UAT results as four yes/no answers. Only all-yes proceeds; any `no` returns to the relevant earlier plan (02/03/04).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md, ROADMAP.md, servers/tower/README.md, servers/docker-tower/README.md, create servers/jellyfin/inventory.md</name>
  <files>
    CLAUDE.md
    .planning/ROADMAP.md
    servers/tower/README.md
    servers/docker-tower/README.md
    servers/jellyfin/inventory.md
  </files>
  <read_first>
    - CLAUDE.md §Servers (existing table rows — same column structure)
    - .planning/ROADMAP.md lines 159-167 (Phase 17.1 entry with "[Urgent work - to be planned]" placeholder)
    - servers/tower/README.md (existing LXC inventory table)
    - servers/docker-tower/README.md (Jellyfin QSV section — now obsolete for docker-tower)
    - .planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-PATTERNS.md §CLAUDE.md, §ROADMAP.md, §servers/tower/README.md
  </read_first>
  <action>
    **CLAUDE.md §Servers table:**
    - Add a new row (after docker-tower): `| jellyfin | Moscow (LXC 101) | <TS_IP_FROM_PLAN_01> | Jellyfin (native deb, iGPU QSV) |`
    - Edit the `docker-tower` row role cell: remove "Jellyfin" — becomes `Media stack: Navidrome, *arr` (Jellyfin moved to CT 101).

    **.planning/ROADMAP.md Phase 17.1:**
    Replace:
    ```
    ### Phase 17.1: Migrate Jellyfin to dedicated LXC on tower (INSERTED)
    **Goal:** [Urgent work - to be planned]
    ```
    With the boundary statement from CONTEXT.md §Phase Boundary:
    ```
    ### Phase 17.1: Migrate Jellyfin to dedicated LXC on tower (INSERTED)
    **Goal:** Jellyfin runs natively on its own Proxmox LXC (CT 101) on tower with exclusive /dev/dri ownership and RO media bindmounts, serving 1080p + 4K without buffering via tower:22098 → 10.10.20.11:8096 reverse proxy. Old Docker container kept as hot standby through 2026-04-25.
    **Requirements**: D-01..D-18 (see 17.1-CONTEXT.md)
    **Depends on:** Phase 17
    **Plans:** 5/5 plans complete

    Plans:
    - [x] 17.1-01-PLAN.md — LXC 101 provisioning (pct create, dev0:, mp0/mp1 RO, Tailscale join, inventory)
    - [x] 17.1-02-PLAN.md — Jellyfin deb install + systemd tmpfs + vainfo probe
    - [x] 17.1-03-PLAN.md — state rsync + cutover (user-gated maintenance window)
    - [x] 17.1-04-PLAN.md — tower:22098 ingress swap with WebSocket headers
    - [x] 17.1-05-PLAN.md — verification + docs + 2026-04-25 cleanup scheduling
    ```
    (Keep checkbox state accurate — mark `[x]` only for plans whose SUMMARY.md exists.)

    **servers/tower/README.md LXC inventory:** Add `| 101 | jellyfin | 10.10.20.11 | Jellyfin (native deb, iGPU QSV) |` following existing column structure.

    **servers/docker-tower/README.md:** Add a note at top of the "Jellyfin QSV" section: `> **Moved 2026-04-18:** Jellyfin migrated to CT 101 (jellyfin LXC) on tower per Phase 17.1. This section is kept for historical reference through 2026-04-25 hot-standby window.` Do NOT remove the section — that happens in the 2026-04-25 cleanup commit (D-15).

    **Create servers/jellyfin/inventory.md** (new file):
    ```markdown
    # jellyfin (CT 101 on tower)

    - **Role:** Jellyfin media server (native Debian deb)
    - **Host:** Moscow, Proxmox CT 101 on tower
    - **Network:** vmbr1 10.10.20.11/24, Tailscale hostname `jellyfin`
    - **Services:** jellyfin.service (8096), tailscaled.service, var-cache-jellyfin-transcodes.mount (tmpfs 4G)
    - **iGPU:** /dev/dri/renderD128 via Proxmox dev0: passthrough
    - **Media:** /media/wdc + /media/sea (mp0/mp1 RO bindmounts from /mnt/pve/*)
    - **External:** jellyfin.makscee.ru → router :22098 → tower → 10.10.20.11:8096
    - **Deploy:** `ansible-playbook -i ansible/inventory/homelab.yml ansible/playbooks/deploy-jellyfin.yml`
    - **Verify:** `bash scripts/verify-jellyfin-lxc.sh`
    - **LXC conf SoT:** `servers/tower/lxc-101-jellyfin.conf`
    - **Phase:** 17.1 (migrated from docker-tower LXC 100 on 2026-04-18)
    ```
  </action>
  <verify>
    <automated>git grep -n '^| jellyfin ' CLAUDE.md | grep -q 'LXC 101' && ! grep -A2 'docker-tower' CLAUDE.md | grep -q 'Jellyfin' && grep -q '^**Goal:** Jellyfin runs natively' .planning/ROADMAP.md && grep -q '^| 101 ' servers/tower/README.md && test -f servers/jellyfin/inventory.md && grep -q 'Moved 2026-04-18' servers/docker-tower/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n '^| jellyfin ' CLAUDE.md` returns a row with `LXC 101`
    - `CLAUDE.md` docker-tower row no longer contains "Jellyfin"
    - `.planning/ROADMAP.md` Phase 17.1 Goal line is the full boundary statement (no "TBD", no "Urgent work - to be planned")
    - `servers/tower/README.md` contains a CT 101 row
    - `servers/jellyfin/inventory.md` exists with the expected headings
    - `servers/docker-tower/README.md` §Jellyfin QSV has the "Moved 2026-04-18" banner
  </acceptance_criteria>
  <done>Docs accurately describe new topology; ROADMAP placeholder closed.</done>
</task>

<task type="auto">
  <name>Task 4: Schedule cleanup todos in STATE.md (D-15, D-17)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md §Pending Todos
    - .planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-CONTEXT.md §D-15 (cleanup cadence)
  </read_first>
  <action>
    Add to `.planning/STATE.md` §Pending Todos:
    ```
    - 2026-04-25: Phase 17.1 cleanup step 1 — remove `jellyfin:` service block from `servers/docker-tower/media/docker-compose.yml` in a dedicated commit; run `ansible-playbook deploy-docker-tower.yml`; confirm old Jellyfin container gone; delete `Jellyfin QSV` banner from `servers/docker-tower/README.md` (D-15).
    - 2026-05-02: Phase 17.1 cleanup step 2 — purge `/opt/docker-configs/shared/jellyfin/` on docker-tower LXC 100 (`ssh root@docker-tower 'rm -rf /opt/docker-configs/shared/jellyfin'`) after 1 week of offline retention (D-15).
    - Phase 17.1 rollback drill (D-16, optional but recommended before 2026-04-25): exercise the rollback block in `17.1-03-CUTOVER-LOG.md` + `17.1-04-INGRESS-LOG.md` once during the soak window, confirm service restores in <5 min.
    ```
    Also update §Current Position if appropriate (Phase 17.1 → complete/ready_for_verification).

    Update VALIDATION.md rows ONLY IF the operator explicitly confirmed UAT in Task 2:
    - 17.1-04-01 (HW transcode confirmed end-to-end): `✅` only if operator said yes
    - 17.1-04-02 (no buffering 1080p + 4K): `✅` only if operator said yes
    If either was "no", leave `⬜ pending` and append a "Gap captured" note — do NOT silently mark green.
  </action>
  <verify>
    <automated>grep -q '2026-04-25: Phase 17.1 cleanup step 1' .planning/STATE.md && grep -q '2026-05-02: Phase 17.1 cleanup step 2' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md §Pending Todos contains both 2026-04-25 and 2026-05-02 entries with D-15 references
    - STATE.md contains a rollback drill reminder
    - VALIDATION.md rows 17.1-04-01 / 17.1-04-02 ONLY green if Task 2 confirmation captured yes/yes
  </acceptance_criteria>
  <done>Future cleanups are scheduled; UAT statuses reflect operator reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new introduced by this plan — doc + scheduling only.

## STRIDE Threat Register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-17.1-21 | Repudiation | Old container purged without confirmed UAT | mitigate | D-15 cleanup is gated by D-17 UAT; cleanup commit scheduled for 2026-04-25 is documented in STATE.md, not automated. |
| T-17.1-22 | Repudiation | VALIDATION.md marked green without user confirmation | mitigate | Task 4 action explicitly forbids auto-green — only if operator's Task 2 answers were all-yes. |
</threat_model>

<verification>
- verify-jellyfin-lxc.sh green.
- External URL works.
- Operator Task 2 UAT answers captured.
- Docs match topology.
- 2026-04-25 + 2026-05-02 cleanup scheduled.
</verification>

<success_criteria>
- All D-XX from CONTEXT.md (D-01..D-18) materially complete or scheduled.
- Operator has confirmed D-17 (buffering relief) OR a gap is logged.
- Repo documentation reflects Jellyfin on CT 101.
</success_criteria>

<output>
Create `.planning/phases/17.1-migrate-jellyfin-to-dedicated-lxc-on-tower/17.1-05-SUMMARY.md` with: Task 2 UAT answers (verbatim), doc files updated, STATE.md todos added, VALIDATION.md final row statuses.
</output>
