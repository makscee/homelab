---
phase: 17-eslint-10-node-types-24-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/package.json
  - apps/admin/bun.lock
autonomous: false
requirements:
  - D-1
  - D-2
  - D-3
  - D-4
  - D-5
user_setup: []

must_haves:
  truths:
    - "apps/admin lints clean on ESLint 10.2.x (bun run lint exits 0)"
    - "apps/admin typechecks clean with @types/node ^24 on TypeScript 6.0.3 (bun run typecheck exits 0)"
    - "apps/admin production build succeeds on ESLint 10 + @types/node 24 (bun run build exits 0)"
    - "typescript-eslint 8.58.2 operates against ESLint 10 without peer-range conflicts (no sibling bump shipped)"
    - "eslint-config-next stays 15.5.15 (not imported by eslint.config.mjs — irrelevant peer range confirmed in RESEARCH)"
    - "eslint-plugin-server-only continues to load under ESLint 10; if it breaks, the single server-only rule is inlined into eslint.config.mjs and the plugin devDep dropped"
    - "Deployed admin instance on mcow renders /, /audit, /alerts without console errors on the new bundle"
    - "No scope creep: no new rules, no mass --fix, no Prettier or next-lint-CLI changes (D-5)"
  artifacts:
    - path: "apps/admin/package.json"
      provides: "eslint bumped to ^10.2.0 and @types/node bumped to ^24"
      contains: "\"eslint\": \"^10"
    - path: "apps/admin/bun.lock"
      provides: "Lockfile resolved against ESLint 10.2.x and @types/node 24.x"
      contains: "eslint"
  key_links:
    - from: "apps/admin/package.json"
      to: "eslint 10.2.x in node_modules"
      via: "bun install resolution"
      pattern: "\"eslint\": \"\\^10"
    - from: "apps/admin/package.json"
      to: "@types/node 24.x in node_modules"
      via: "bun install resolution"
      pattern: "\"@types/node\": \"\\^24"
    - from: "apps/admin eslint.config.mjs"
      to: "eslint 10 rule runtime"
      via: "bun run lint → next lint → resolved eslint"
      pattern: "server-only/server-only"
---

<objective>
Upgrade `apps/admin` from ESLint `^9` to `^10.2.0` and `@types/node` `^22` to `^24` per 17-CONTEXT.md + 17-RESEARCH.md. Research confirms the baseline is clean, the repo is already on flat config (`eslint.config.mjs`, no `.eslintrc*`), `typescript-eslint@8.58.2` already declares `eslint ^10.0.0` in its peer range, and `eslint-config-next` is **not imported** by the active config — so D-2 and D-4 are verified no-ops. This is a single mechanical bump with verification + deploy + smoke.

Purpose: Keep the frontend lint + types stack current on the heels of Phase 16 (TS 6.0.3). Scope is strictly upgrade-only per D-5 — no new rules, no `--fix` sweeps, no Prettier changes, no sibling dep bumps unless a verification step explicitly demands it.

Output: apps/admin running on ESLint 10.2.x + @types/node 24.x, lint + typecheck + build all green, deployed to mcow, Playwright smoke clean on `/`, `/audit`, `/alerts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-eslint-10-node-types-24-upgrade/17-CONTEXT.md
@.planning/phases/17-eslint-10-node-types-24-upgrade/17-RESEARCH.md

@apps/admin/package.json
@apps/admin/eslint.config.mjs
@apps/admin/tsconfig.json
@ansible/playbooks/deploy-homelab-admin.yml

<interfaces>
<!-- P-1 fallback (per 17-RESEARCH.md): if eslint-plugin-server-only@0.1.1 fails to load on ESLint 10, -->
<!-- inline the rule directly into eslint.config.mjs (~20 LOC) and drop the devDep. The plugin exports -->
<!-- a single rule that forbids imports from `server-only` modules in client components. Minimum-viable -->
<!-- inline shape: -->

```js
// apps/admin/eslint.config.mjs — P-1 fallback (only apply if plugin breaks)
const serverOnlyRule = {
  meta: {
    type: "problem",
    docs: { description: "Disallow importing server-only modules from client code" },
    schema: [],
  },
  create(context) {
    return {
      ImportDeclaration(node) {
        if (node.source.value === "server-only") {
          context.report({ node, message: "server-only import outside server context" });
        }
      },
    };
  },
};

// Register as a local plugin in the flat config:
//   plugins: { "server-only": { rules: { "server-only": serverOnlyRule } } },
//   rules: { "server-only/server-only": "error" }
```

<!-- Expected package.json diff (Task 1 happy path — no P-1): -->
<!--   "eslint": "^9"           → "eslint": "^10.2.0" -->
<!--   "@types/node": "^22"     → "@types/node": "^24" -->
<!-- Do NOT touch: typescript-eslint (8.58.2), @typescript-eslint/parser (8.58.2), -->
<!-- eslint-config-next (15.5.15), @eslint/eslintrc (3.3.5), eslint-plugin-server-only (0.1.1), -->
<!-- typescript (6.0.3). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump eslint to ^10.2.0 and @types/node to ^24, install, lint, typecheck, build</name>
  <files>apps/admin/package.json, apps/admin/bun.lock, apps/admin/eslint.config.mjs (only on P-1 fallback)</files>
  <action>
Per D-3 (@types/node 22 → 24 straight bump) and D-5 (upgrade-only scope). Research (17-RESEARCH.md) verified D-2 and D-4 are no-ops: `typescript-eslint@8.58.2` already supports ESLint 10, and `eslint-config-next` is not imported by the flat config. Expected to be a single-edit bump.

1. **Edit `apps/admin/package.json`** — in `devDependencies`:
   - change `"eslint": "^9"` (or whatever `^9.x` spec is pinned) to `"eslint": "^10.2.0"`
   - change `"@types/node": "^22"` (or whatever `^22.x` spec is pinned) to `"@types/node": "^24"`

   Do NOT touch `typescript-eslint` (stays `^8.58.2` — peer range `eslint ^8.57.0 || ^9.0.0 || ^10.0.0` covers ESLint 10 per 17-RESEARCH.md; D-2 is a verified no-op).
   Do NOT touch `@typescript-eslint/parser` (same peer range).
   Do NOT touch `eslint-config-next` (stays `15.5.15` — not imported by `eslint.config.mjs`; its `eslint ^7 || ^8 || ^9` peer is irrelevant; D-4 is a verified no-op).
   Do NOT touch `@eslint/eslintrc` (stays `^3.3.5` — unused at runtime; leave pinned to avoid churn per D-5; RESEARCH §P-3).
   Do NOT touch `eslint-plugin-server-only` (stays `^0.1.1` unless P-1 triggers — see step 3).
   Do NOT touch `typescript` (Phase 16 locked `^6.0.3`).

2. **Install:**
   ```bash
   cd apps/admin && bun install
   ```
   Expect: lockfile updates `eslint` to a `10.2.x` resolution and `@types/node` to a `24.x` resolution. Verify:
   ```bash
   cd apps/admin && bun x eslint --version   # → v10.2.x
   grep -A1 '"@types/node"' bun.lock | head  # → 24.x entry present
   ```
   Per RESEARCH §P-2: confirm `bun.lock` has a reasonably recent `24.x` (not `24.0.0` from a stale cache). If resolution looks wrong, `rm -rf node_modules bun.lock && bun install` to force a fresh resolve.

3. **Lint:**
   ```bash
   cd apps/admin && bun run lint
   ```
   MUST exit 0.

   **P-1 fallback (per RESEARCH §P-1):** If lint throws a plugin-load error (e.g. `context.getScope is not a function` or similar API-removal error) from `eslint-plugin-server-only`:
   - Inline the `server-only` rule into `eslint.config.mjs` using the shape shown in the `<interfaces>` block above (~20 LOC).
   - Remove `eslint-plugin-server-only` from `devDependencies` in `package.json`.
   - Re-run `bun install` to drop the now-unused dep from `bun.lock`.
   - Re-run `bun run lint` — MUST exit 0 with the inline rule active.
   - Document the fallback in the commit message (one line: "P-1 triggered: inlined server-only rule, dropped plugin").

   Do NOT run `--fix` sweeps (D-5). If any genuine new lint error surfaces on source (not on the plugin), fix only the reported lines with the minimum mechanical change and nothing else.

4. **Typecheck:**
   ```bash
   cd apps/admin && bun run typecheck
   ```
   MUST exit 0. `@types/node@^24` on TS 6.0.3 is expected to typecheck cleanly per RESEARCH §`@types/node` implications (narrow server-side surface in `app/api/**`, `lib/**`). If errors surface:
   - Apply mechanical fixes on the reported lines only (narrow casts, explicit types) — do NOT refactor surrounding code.
   - Do NOT silence with `@ts-ignore`; do NOT bump sibling deps unless the error explicitly demands it (record which + why in the commit message).

5. **Build:**
   ```bash
   cd apps/admin && bun run build
   ```
   MUST exit 0. Watch for Next.js plugin / lint warnings — the pre-existing "next lint is deprecated" + "Next.js plugin was not detected" informationals are out of scope (D-5, RESEARCH §Baseline State); any new warnings go in the commit message.

6. **Commit atomically:**
   ```
   chore(admin): upgrade eslint 9 → 10.2, @types/node 22 → 24 (17-01)
   ```
   One commit covers `package.json` + `bun.lock` (+ `eslint.config.mjs` only if P-1 fallback was applied). Do NOT split unless a sibling dep bump was genuinely forced (commit that separately with rationale).
  </action>
  <verify>
    <automated>cd apps/admin &amp;&amp; grep -q '"eslint": "\^10' package.json &amp;&amp; grep -q '"@types/node": "\^24' package.json &amp;&amp; bun x eslint --version | grep -q '^v10\.' &amp;&amp; bun run lint &amp;&amp; bun run typecheck &amp;&amp; bun run build</automated>
  </verify>
  <done>package.json has `"eslint": "^10.2.0"` and `"@types/node": "^24"`; `bun x eslint --version` reports `v10.x`; `bun.lock` resolves a recent `24.x` for `@types/node`; lint, typecheck, and build all exit 0; no sibling dep bumps beyond what verification forced (or the P-1-triggered removal of `eslint-plugin-server-only`); atomic commit on main.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to mcow via Ansible</name>
  <files>(no repo files modified — deploy only)</files>
  <action>
Deploy the ESLint-10 / @types/node-24 admin bundle to mcow using the existing playbook, scoped to the `mcow` host per the user request:

```bash
ansible-playbook ansible/playbooks/deploy-homelab-admin.yml -l mcow
```

Expect: playbook rsyncs `apps/admin/` artifacts to mcow, triggers remote `bun install` + build (if the playbook does remote build), restarts `homelab-admin.service`. Watch for:
- Remote `bun install` picking up the new `eslint@10.2.x` and `@types/node@24.x`
- systemd restart success: `ssh root@mcow 'systemctl is-active homelab-admin'` → `active`
- Journal clean: `ssh root@mcow 'journalctl -u homelab-admin -n 50 --no-pager'` → no new runtime errors on startup

If the playbook fails, surface the error — do NOT attempt manual remote fixes outside the playbook (Ansible is the deployment source of truth per repo convention).
  </action>
  <verify>
    <automated>ssh root@mcow 'systemctl is-active homelab-admin'</automated>
  </verify>
  <done>Playbook run completes without errors; `homelab-admin.service` is active on mcow; journal shows no new runtime errors post-restart.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Playwright smoke — /, /audit, /alerts render without console errors</name>
  <what-built>
apps/admin now runs on ESLint 10.2.x (lint-time only) and `@types/node` 24 (compile-time only), deployed to mcow. Neither change should affect runtime behavior, but the smoke check catches any edge case where `@types/node@24`'s reshape of `node:*` module types altered an emit-relevant narrowing in server code (API routes, audit/alerts data-loader paths).
  </what-built>
  <how-to-verify>
Claude drives Playwright MCP (per user memory: `Test UI with Playwright`) against `https://homelab.makscee.ru` over Tailnet from the operator machine.

**Pages to verify (Claude runs via Playwright, captures console logs):**
1. `/` (Overview) — host tiles, AlertsCard, ClaudeSummary render; no console errors
2. `/audit` — audit table renders with rows; pagination works; no console errors
3. `/alerts` — alerts list renders (may be empty); no console errors

**Checks per page:**
- `page.on('console')` captures ZERO `error`-level messages (warnings acceptable if pre-existing)
- `page.on('pageerror')` captures ZERO uncaught exceptions
- Visible render: at least one data-bearing element per page (host tile, audit row, alert row or empty-state message)

**Red flags (block approval):**
- Any console `error` that didn't exist in Phase 16-01 smoke baseline
- Digest errors (Next.js runtime exception hashes) on any page
- Blank page / hydration mismatch

If any red flag appears, Claude checks the journal (`ssh root@mcow 'journalctl -u homelab-admin -n 100 --no-pager'`), diagnoses, and applies the minimum in-scope fix (typically a narrowing cast on a server-side call site tripped by `@types/node@24`). Re-deploy via Task 2 playbook and re-verify.

User only needs to weigh in if Playwright output is ambiguous or if aesthetic regression is suspected (not expected — lint + types only).
  </how-to-verify>
  <resume-signal>Type "approved" if Playwright smoke is clean on all three pages. Describe the failing page + error if regression visible.</resume-signal>
  <files>(verification only — no file modifications unless red-flag fix required in apps/admin source)</files>
  <action>Run Playwright MCP against /, /audit, /alerts on homelab.makscee.ru over Tailnet. Capture console + pageerror streams. Report pass/fail per page. On red flag: diagnose via mcow journal, apply minimum in-scope fix, re-deploy via Task 2, re-verify. Request approval once all three pages are clean.</action>
  <verify><automated>MISSING — Playwright MCP smoke is the automated substitute for manual browser check; no pure-CLI replacement for rendered-page console capture</automated></verify>
  <done>User types "approved"; Playwright smoke reports zero console errors and zero pageerrors on /, /audit, /alerts; any red-flag regression resolved by minimum in-scope fix committed atomically on top of Task 1 commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Phase 17 is a lint + types toolchain bump; no new request/response paths, no new trust boundaries, no runtime behavior change intended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | eslint 10.2.x + @types/node 24.x npm packages | accept | Both are first-party ecosystem packages (ESLint project, DefinitelyTyped); `bun install` verifies integrity against registry hashes in `bun.lock` |
| T-17-02 | Denial of Service | production build pipeline | mitigate | `bun run lint && bun run typecheck && bun run build` must all pass in Task 1 before deploy; broken build caught pre-merge; deploy playbook fails fast on remote build errors |
| T-17-03 | Denial of Service | homelab-admin.service on mcow | mitigate | Playwright smoke (Task 3) confirms runtime render on /, /audit, /alerts before approval; journal checked post-deploy |
| T-17-04 | Tampering | eslint-plugin-server-only (client/server boundary enforcement) | mitigate | P-1 fallback inlines the rule directly into `eslint.config.mjs` if the plugin breaks on ESLint 10 — enforcement of the server-only import boundary is preserved either way |
</threat_model>

<verification>
- `grep -q '"eslint": "\^10' apps/admin/package.json`
- `grep -q '"@types/node": "\^24' apps/admin/package.json`
- `cd apps/admin && bun x eslint --version` reports `v10.x`
- `cd apps/admin && bun run lint` exits 0
- `cd apps/admin && bun run typecheck` exits 0
- `cd apps/admin && bun run build` exits 0
- `ssh root@mcow 'systemctl is-active homelab-admin'` returns `active`
- Playwright smoke on /, /audit, /alerts: zero console errors, zero pageerrors
- No sibling dep bumps committed unless verification forced them (rationale in commit message); P-1 plugin-drop is an acceptable documented exception
- `eslint.config.mjs` unchanged unless P-1 fallback was applied (then inline-rule added + plugin dep dropped, documented in commit)
</verification>

<success_criteria>
- ESLint 9 → 10.2.0 and @types/node 22 → 24 upgrades complete per D-1 (flat-config — verified no-op), D-2 (typescript-eslint no-op confirmed), D-3 (straight bump), D-4 (eslint-config-next no-op confirmed), D-5 (upgrade-only, no refactors)
- Lint + typecheck + production build all pass on the new toolchain
- Deployed instance on mcow renders three live pages without regression (Playwright-verified)
- No new runtime errors in homelab-admin journal
- Atomic commit on main; Phase 17 closes the frontend tooling upgrade trio (15-tailwind, 16-typescript, 17-eslint/node-types)
</success_criteria>

<output>
After completion, create `.planning/phases/17-eslint-10-node-types-24-upgrade/17-01-SUMMARY.md`
</output>
