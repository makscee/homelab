---
phase: 12-infra-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - servers/mcow/lxc-probe.md
  - .planning/STATE.md
autonomous: false
requirements: [INFRA-06]
must_haves:
  truths:
    - "The mcow LXC privilege level (privileged vs unprivileged) is documented in-repo"
    - "The systemd hardening flag set for Plan 07 is derived from probe output, not guessed"
    - "STATE.md no longer lists obsolete pre-phase todos (Cloudflare token, tailscale-nginx-auth socket)"
    - "STATE.md lists the LXC probe todo as resolved"
  artifacts:
    - path: "servers/mcow/lxc-probe.md"
      provides: "LXC privilege level + systemd hardening recommendation for Plan 07"
      contains: "privilege_level:"
    - path: ".planning/STATE.md"
      provides: "Cleaned pending todos"
  key_links:
    - from: "servers/mcow/lxc-probe.md"
      to: "servers/mcow/homelab-admin.service (Plan 07)"
      via: "Hardening flag set recommendation"
      pattern: "ProtectSystem|PrivateTmp"
---

<objective>
Probe mcow's LXC privilege level (via `ssh root@tower "pct config <mcow-vmid>"`) so Plan 07 can author the systemd unit with the CORRECT set of hardening directives. Close obsolete STATE.md todos (Cloudflare token, tailscale-nginx-auth socket — both superseded by CONTEXT.md D-12-12 and D-12-06).

Purpose: Resolve STATE.md pre-phase pending todos. Feed Plan 07 a definitive hardening recommendation rooted in evidence (P-15: PrivateTmp/ProtectSystem break inside certain unprivileged LXC configurations).
Output: `servers/mcow/lxc-probe.md` committed; `STATE.md` cleaned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-infra-foundation/12-CONTEXT.md
@.planning/research/PITFALLS.md
@/Users/admin/hub/workspace/homelab/CLAUDE.md
@ansible/inventory/homelab.yml

<interfaces>
<!-- CONTEXT: D-12-26 (LXC probe → hardening flags), D-12-38/39/40 (STATE.md cleanup) -->
<!-- PITFALLS: P-15 — PrivateTmp/ProtectSystem=strict can break inside unprivileged Proxmox LXC (overlay filesystem + user namespace translation quirks) -->

STATE.md pending todos this plan resolves:
- [OBSOLETE] "verify Cloudflare API token exists" — HTTP-01 (D-12-12), not DNS-01 → close with reason
- [OBSOLETE] "confirm tailscale-nginx-auth socket path" — GitHub OAuth (D-12-06), not Tailscale headers → close with reason
- [ACTIVE] "confirm mcow LXC privilege level" — this probe resolves it

mcow's vmid is unknown from repo context. Probe includes discovery:
```
ssh root@tower "pct list | grep -iE 'mcow|100' || pct list"
```
then pick the matching VMID and run `pct config <vmid>`.

Hardening decision table (P-15):
- privileged LXC: all strict flags safe → `ProtectSystem=strict` `PrivateTmp=yes` `NoNewPrivileges=yes` `ProtectHome=yes` `ProtectKernelTunables=yes` `ProtectControlGroups=yes`
- unprivileged LXC: degrade `ProtectSystem=full` (not `strict`), drop `PrivateTmp` if journalctl shows mount failures; keep `NoNewPrivileges=yes` `ProtectHome=yes` unconditionally
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Probe mcow LXC privilege level via tower</name>
  <what-built>N/A — operator executes SSH probe (Claude does not have SSH access to Proxmox in this context)</what-built>
  <how-to-verify>
Operator runs on their laptop (or from `cc-worker`):

```bash
# Step 1: find mcow's VMID on tower
ssh root@tower "pct list"
# Expected output shows a row with hostname column containing 'mcow'. Note the VMID.

# Step 2: dump the LXC config (replace <VMID>)
ssh root@tower "pct config <VMID>"

# Step 3: capture to a local tmp file for the next task to read
ssh root@tower "pct config <VMID>" > /tmp/mcow-lxc-config.txt
cat /tmp/mcow-lxc-config.txt
```

Look for these lines in output:
- `unprivileged: 1` → UNPRIVILEGED (degrade hardening per P-15)
- `unprivileged: 0` OR the key absent entirely → PRIVILEGED (all hardening flags safe)
- `features: nesting=1` or `features: keyctl=1,nesting=1` → matters for Plan 07 if Bun's JIT needs keyctl
- `rootfs:` and `mp0:` lines → mount points (determines if PrivateTmp is safe)

After capturing output, paste the full `pct config` output into the resume message so Claude can author `lxc-probe.md` in Task 2.
  </how-to-verify>
  <resume-signal>Paste the full `pct config <VMID>` output (including `unprivileged:` line or confirmation it's missing). Also confirm the VMID.</resume-signal>
  <acceptance_criteria>
    - Operator has run `pct config <VMID>` on tower and captured output
    - Output pasted into resume message for Claude to consume in Task 2
    - `unprivileged:` value (or absence) is unambiguous from output
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write servers/mcow/lxc-probe.md with hardening recommendation</name>
  <files>servers/mcow/lxc-probe.md</files>
  <read_first>
    - `.planning/research/PITFALLS.md` §P-15 (systemd PrivateTmp/ProtectSystem in unprivileged LXC)
    - `.planning/phases/12-infra-foundation/12-CONTEXT.md` §decisions D-12-26 (hardening conditional on probe), D-12-40 (probe resolves this)
    - `/Users/admin/hub/workspace/homelab/servers/mcow/inventory.md` — mcow service inventory context
    - Task 1 operator-pasted `pct config` output
  </read_first>
  <action>
Using the `pct config` output from Task 1, create `servers/mcow/lxc-probe.md`:

```markdown
# mcow LXC Configuration Probe

**Date:** <YYYY-MM-DD of execution>
**Source:** `ssh root@tower "pct config <VMID>"`
**VMID:** <from Task 1>

## Raw Output

```
<paste the full pct config output here, unredacted except any secret fields>
```

## Parsed Facts

| Key | Value | Implication |
|-----|-------|-------------|
| privilege_level | privileged OR unprivileged | Drives hardening flag set for systemd unit |
| unprivileged line | `unprivileged: 1` or absent | If `1` → unprivileged; if absent/`0` → privileged |
| features | <value or none> | nesting/keyctl/mknod relevance |
| rootfs | <storage:path> | Confirms rootfs mount style |

## Systemd Hardening Recommendation for Plan 07

<Apply per P-15:>

**If privileged (unprivileged: 0 or absent):**
```
[Service]
User=homelab-admin
Group=homelab-admin
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=yes
PrivateTmp=yes
ProtectKernelTunables=yes
ProtectControlGroups=yes
ReadWritePaths=/opt/homelab-admin/app/.next /tmp
```

**If unprivileged (unprivileged: 1):**
```
[Service]
User=homelab-admin
Group=homelab-admin
NoNewPrivileges=yes
ProtectSystem=full
ProtectHome=yes
# PrivateTmp=yes  # DISABLED — may break in unprivileged LXC (P-15); re-enable if Plan 09 smoke test stays green for 24h with it on
# ProtectKernelTunables disabled — requires CAP_SYS_ADMIN unavailable in unprivileged LXC
ReadWritePaths=/opt/homelab-admin/app/.next
```

## Downstream Consumer

Plan 07 (`12-07-PLAN.md`) reads this file's "Recommendation" section and transcribes the appropriate block into `servers/mcow/homelab-admin.service`.
```

Fill in the actual values from Task 1's output. Pick exactly ONE of the two `[Service]` blocks based on `unprivileged:` line.
  </action>
  <verify>
    <automated>test -f servers/mcow/lxc-probe.md && grep -qE "^privilege_level|^\| privilege_level" servers/mcow/lxc-probe.md && grep -qE "User=homelab-admin" servers/mcow/lxc-probe.md</automated>
  </verify>
  <acceptance_criteria>
    - `servers/mcow/lxc-probe.md` exists
    - Contains raw `pct config` output (evidence trail)
    - Contains either "If privileged" OR "If unprivileged" block marked as the ACTIVE recommendation (not both left ambiguous)
    - Contains `User=homelab-admin` in the hardening recommendation (P-anti-3: not root)
    - VMID documented
  </acceptance_criteria>
  <done>Plan 07 has a copy-paste-ready systemd `[Service]` block matched to mcow's actual LXC config.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Clean STATE.md — close obsolete todos, mark probe resolved</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` (current §Pending Todos section)
    - `.planning/phases/12-infra-foundation/12-CONTEXT.md` §decisions D-12-38, D-12-39, D-12-40
  </read_first>
  <action>
Edit `.planning/STATE.md` §Pending Todos — replace the three Phase-12 pre-flight items with resolution lines. Keep the `2026-04-22` docker-tower volume cleanup todo intact.

Before:
```
### Pending Todos

- 2026-04-22: docker-tower volume cleanup (grafana + alertmanager data volumes)
- Before Phase 12: verify Cloudflare API token exists in secrets/ (zone:dns:edit for makscee.ru)
- Before Phase 12: confirm tailscale-nginx-auth socket path on mcow (`/run/tailscale/` or `/var/run/tailscale/`)
- Before Phase 12: confirm mcow LXC privilege level (privileged/unprivileged)
```

After:
```
### Pending Todos

- 2026-04-22: docker-tower volume cleanup (grafana + alertmanager data volumes)

### Resolved Pre-Phase-12 Todos (closed 2026-04-17 by Plan 12-03)

- ~~Before Phase 12: verify Cloudflare API token exists~~ — OBSOLETE. Phase 12 CONTEXT.md D-12-12 switched from LE DNS-01 to LE HTTP-01 (mirrors `vibe.makscee.ru`); no Cloudflare API integration needed.
- ~~Before Phase 12: confirm tailscale-nginx-auth socket path on mcow~~ — OBSOLETE. Phase 12 CONTEXT.md D-12-06 switched from Tailscale header auth to GitHub OAuth (Auth.js v5); tailscale-nginx-auth not used.
- ~~Before Phase 12: confirm mcow LXC privilege level~~ — RESOLVED. See `servers/mcow/lxc-probe.md`; hardening recommendation wired into Plan 12-07.
```

Also update `last_activity` line in frontmatter to reflect the plan work (optional — depends on whether STATE.md frontmatter convention preserves this).
  </action>
  <verify>
    <automated>! grep -q "Before Phase 12: verify Cloudflare" .planning/STATE.md && grep -q "lxc-probe.md" .planning/STATE.md && grep -q "Resolved Pre-Phase-12 Todos" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` no longer contains "Before Phase 12: verify Cloudflare API token exists"
    - `.planning/STATE.md` no longer contains "Before Phase 12: confirm tailscale-nginx-auth socket path"
    - `.planning/STATE.md` no longer contains "Before Phase 12: confirm mcow LXC privilege level" as active
    - `.planning/STATE.md` contains a "Resolved Pre-Phase-12 Todos" subsection referencing `lxc-probe.md`
    - The `2026-04-22` docker-tower volume todo is still present (no accidental deletion)
  </acceptance_criteria>
  <done>STATE.md pending todos reflect reality; no phantom todos block future phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Proxmox host → LXC runtime | LXC privilege level determines kernel capability exposure |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Elevation of Privilege | systemd unit running homelab-admin service | mitigate | Probe confirms LXC privilege; Plan 07 transcribes hardening block matched to probe output; NoNewPrivileges=yes unconditional |

Residual: INFO — probe is read-only, no runtime exposure created here.
</threat_model>

<verification>
- `lxc-probe.md` is committed with real `pct config` evidence
- Plan 07 can read the file and copy the [Service] block verbatim
- STATE.md is clean
</verification>

<success_criteria>
- All three STATE.md pre-phase todos are resolved
- SC #5 (idempotent deploy) has a derisked hardening path
</success_criteria>

<output>
After completion, create `.planning/phases/12-infra-foundation/12-03-SUMMARY.md` documenting:
- mcow VMID + privilege level (evidence-based fact)
- Which hardening block (privileged vs unprivileged) Plan 07 should transcribe
</output>
