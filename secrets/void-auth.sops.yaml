# HMB-9b — void-auth runtime secrets.
#
# THIS FILE IS A PLACEHOLDER. Before deploying:
#   1. Replace the value below with the real bearer token (or leave empty
#      string "" if void-mail does not enforce auth — current Phase 1 default
#      per void-auth/CLAUDE.md "no app-level auth between intra-mcow services
#      in v1; the compose network / tailnet is the trust boundary").
#   2. Encrypt in place:
#        sops --encrypt --in-place secrets/void-auth.sops.yaml
#   3. Commit the encrypted form alongside this playbook.
#
# Loaded by playbooks/void-platform-mcow.yml via community.sops.load_vars
# (NOT vars_files — vars_files does not decrypt SOPS, see feedback memory
# ansible_sops_decrypt + HMB-8 root cause).
void_auth_mail_token: "REPLACE_ME_OR_EMPTY"
